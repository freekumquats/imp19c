# Migration / Claim-Hostility / De Jure Irredentism / Wargoal — 4-Layer Program

_Migrated from memory imp19c-migration-claims-program per the research/design-digest-location rule. Design spec — see the memory pointer for status._

Approved 2026-07 (full program, build layer-by-layer with review after each, generic-first then Qing extends — same pattern as currency stress). Richer/costlier Qing is explicitly OK.

**Reference mods (user, firm):** github.com/dementive/Terra-Indomita AND github.com/SnowletTV/Invictus are **feasibility oracles ONLY** — they answer "can the engine mod X?" (both cloned sparse to /tmp for local grep). NEVER copy or assume their mechanics: imp19c's currency/trade/industry/economy are bespoke and totally different. Confirmed via them: `decade_country_pulse` is a real, country-scope-moddable engine on-action (both use it).

**Layer 1 — claim-hostility engine** (`se_CLAIM_HOSTILITY.txt`, NEW) — ✅ DONE: when a claimed province (`is_core_of` read-back of `add_claim`) is taken/occupied by a non-claimant, the claimant's OPINION of the taker drops. Opinion is the per-pair hostility store because actors are DYNAMIC — per-pair named counters/modifiers are impossible (parameterized modifier NAMES unproven in repo). Wire the two empty province-scope on_actions in `common/on_action/00_specific_from_code.txt`: `on_ownership_change` (has existing holding-cleanup effect — PRESERVE it) and `on_province_occupied` (empty). Qing routes GP takers through `QING_gp_react` to keep 以夷制夷 richness.

**Layer 2 — generic migration→claims** (`se_MIGRATION.txt`, `MIGRATION_svalues.txt`, `00_decade_country.txt`) — ✅ DONE 2026-07. Bottom-up. `MIGRATION_country_pulse` (on `decade_country_pulse` via `MIGRATION_decade_pulse` child on-action, `delay = { days = { 1 3650 } }`): `ordered_owned_province order_by=MIGRATION_push_province max=3` (gated push>5 + has movable pop) → `ordered_neighbor_province order_by=MIGRATION_pull_province max=1` (owned, pull>0, pull > source's pull; cross-border eligible) → one `random_pops_in_province` with grievance `weight` (minority ×3, religious-minority ×2, prole/lower ×2, `pop_hapiness<0.4` ×2; excl slaves+nobles) `move_pop`. Then `MIGRATION_seed_claim_if_flipped`: if dest `dominant_province_culture != owner.culture`, the country whose `primary_culture` = that plurality (not owner, not already core) gets `add_claim` = organic settler colonialism → feeds L1/L3. Push/pull svalues read ONLY cached econ fields (JOBS_unemployed_pops_province, JOBS_available_slots, province_unrest, civilization_value, `governorship.ECON_governorship_food_shortage`, war/occupation). sys=MIGR. **Bug caught in review:** `is_culture` is ONLY valid inside a culture iterator (`random_country_culture`), NEVER on a country scope — use `primary_culture = <culture scope>` (proven send_settlers/se_QING_DECLINE). **KEY PHILOSOPHY (user, firm): migration is BOTTOM-UP.** Government/player = ONE factor (modifiers into push/pull), never orders moves.

**Layer 2b — Qing colonisation reworked to drive L2** (`se_QING_COLON.txt`, NEW; mission + event rewire) — ✅ DONE 2026-07. Bottom-up bridge: the old settle-by-fiat colonisation missions now INCENTIVISE the generic engine instead of teleporting settlers. `QING_COLON_frontier_pull = {province=<id>}` sets `migr_gov_pull=12` on an OWNED frontier province (guarded `owns`; `LOG_fail` + deferred if not held — the mission's `add_claim` covers the aspiration until conquest). `QING_COLON_heartland_push = {count=<n>}` tags the n most-populous owned provinces `migr_gov_push=6` (turns a demographic bulge into out-migrants). `QING_COLON_clear_heartland_push` removes the push when crowding is relieved. Rewired: `qing_colonization_missions.txt` — Xinjiang task's two `create_state_pop=freemen` → `frontier_pull` on 2930 & 2977 (fortress/city kept); Taiwan → `frontier_pull` 6799 & 6781; Amur → 6170. `qing_frontier_migration_events.txt` — crop-boom `.20.a`/`.20.b` → `heartland_push count=3`, `.23.b` → `count=4`, `.22.a`/`.23.a` → `clear`; crop-boom's own `create_state_pop` (pop GROWTH, not migration) KEPT. ALL pre-existing `add_claim` calls PRESERVED (they feed L1). Inline literals 12/6 NOT @-constants (no precedent; @ doesn't substitute in quoted strings). sys=QING. Braces se_QING_COLON 23/23, missions 312/312, events 152/152. All 10 call-sites resolve to 3 defs. `migr_gov_pull`/`migr_gov_push` read ONLY in MIGRATION_svalues behind `has_variable` guards.

**Layer 3 — de jure irredentism + deep Qing ethnic-tension re-derivation** (`se_DEJURE.txt` NEW; `dejure_on_actions.txt` NEW; `se_QING_DECLINE.txt` extended) — ✅ DONE 2026-07. **RUNTIME-BRIDGE KEY FACT:** the §10 de jure table (`map_data/de_jure_output.txt`) is GENERATED data the engine NEVER loads at runtime, and L2 migration mutates live `dominant_province_culture` — so de jure had to be materialised into script state & frozen. `DEJURE_freeze_setup` (on_game_initialized, global-var guard `dejure_baseline_frozen`, `every_province`) stores each settled province's 1815 plurality culture SCOPE into `var:dejure_culture` (proven: `set_variable value=<scope link>`, e.g. `value=owner`; store a culture via `value = dominant_province_culture`). Generic consumer `DEJURE_on_ownership_change` (fired from existing `on_ownership_change` — NO new polling): if new owner hasn't INTEGRATED the rightful culture (`is_integrated` auto-excludes owner's own primary culture) → decaying `dejure_disaffection` province modifier (local_unrest 3, local_monthly_civilization -0.05, ~20yr) + `DEJURE_seed_dejure_claim` (kin-state via `primary_culture = scope:dj_rightful` gets add_claim). Keyed to FROZEN culture not live plurality. **Qing DEEP re-derivation:** `qing_ethnic_tension` now COMPUTED from ethnic geography — `QING_DECLINE_scan_ethnic_target` (ANNUAL, yearly_country_pulse, CHI+human) does the heavy `every_owned_province` scan → weighted restive tally (base1 + unrest≥4 + displacement) → freezes 1815 `qing_ethnic_restive_base` on first scan → target = `20 + (current-base)/2` clamped. `QING_DECLINE_drift_ethnic_tension` (quarterly, O(1)) drifts live counter ±3 toward target. **Perf decision (user): annual heavy scan + quarterly cheap drift** — tension moves only via slow processes, annual near-lossless, ~4× cheaper than per-pulse; generic layer adds ZERO polling. **Calibration handles manchu-primary/Han-realm:** baseline nets the static un-integrated-Han mass to zero so start≈20, counter tracks CHANGE. **BUG caught in review:** `prev.var:dejure_culture` inside `any_country_culture` resolves prev→country (var lives on province); fixed to `save_scope_as = dj_rightful_c` + `scope:`. sys=DEJURE/QING. All idioms verified proven (`is_culture = scope:` inside any_country_culture per on_state_secession; count→`divide = var:` per se_QING_COUNCIL).

**Layer 3 (superseded plan text):** ~~Generic consumer: unrest/claims where owner culture ≠ de jure culture~~ — see DONE above.

**Layer 4 — reconquest wargoal + separatist rebellions** — ✅ DONE 2026-07. `reconquest_wargoal` (`00_default.txt`): cheap take_province, allow-block `scope:defender = { any_owned_province = { is_core_of = root } }` — **in a wargoal allow block ROOT = the attacker** (verified vs Invictus maurya_revival_wargoal); add_claim read back via is_core_of. Loc `war_goal_reconquest_wargoal(_desc)`. **`se_SEPARATISM.txt`** (sys=SEPAR): `SEPARATISM_country_pulse` (generic, piggybacks existing `decade_country_pulse` via new `SEPARATISM_decade_pulse` child — NO new hook; cheap guard + `separatism_cd` decade cooldown + 20% roll) → `SEPARATISM_try_secession` (angriest disaffected prov, frozen `var:dejure_culture` = banner, gather all owned disaffected provs of that culture into `sep_secession_provinces`) → `SEPARATISM_spawn_breakaway` (`LAND_release_from_list` spawns rebel state — economy init handled by existing `ECON_events.7`/`monthly_setup_new_countries`, NO inline FUNC_setup_new_country; clear working list after per se_SUBJECT_QING convention; parent wages reconquest via `FUNC_declare_war_with_wargoal_province` → saves `scope:current_war_scope`; backer join). **BACKER RULE (firm, user, see [[imp19c-separatism-backer-rule]]):** must be a NEIGHBOURING country where the rebel culture LIVES — `scope:sep_rebel_state = { random_neighbour_country = { any_country_culture = { is_culture = scope:sep_culture } ... } }`, add_to_war attacker=no. Qing ext `SEPARATISM_qing_pulse` (quarterly Qing pulse, CHI+human): extra tension-scaled risk gated `qing_ethnic_tension >= 45`, ~3yr cooldown, chance 30/50/75 banded at 60/80; reuses generic try_secession core. **SUPPORT_REBELS FEASIBILITY (user q):** vanilla support_rebels is a HARDCODED engine diplomatic action — neither ref mod defines any custom diplomatic action, and `common/faction_impact/diplomatic_actions/00_default.txt` is a 3-byte empty stub → cannot register a new action or rewrite its core behaviour; only its price (prices/00_hardcoded.txt), 3 applied modifiers (foreign_support_for_rebels / foreign_rebels / supporting_rebels_abroad in modifiers/00_hardcoded.txt) and party-approval impact are tunable; NO on_support_rebels hook. So covert backing = bespoke diplomatic-play event chain (agitator_sponsorship model in se_DIPLOMACY), NOT engine-action extension.
- **CROSS-BORDER LIBERATION (Option A, ✅ DONE 2026-07):** `SEPARATISM_liberate_across_borders` — a culture split across several empires rises as ONE new national state and also declares reconquest/liberation wars on its OTHER neighbouring parents. Per neighbouring other empire (not parent, not rebel, non-subject) holding frozen `var:dejure_culture = scope:sep_culture` land: grant rebel `add_claim` on those provs + `FUNC_declare_war_with_wargoal_province` (angriest such prov = target). Capped `@separatism_max_extra_fronts=3`; **cap MUST be an `if` INSIDE the loop body, NOT the every_ limit** — Clausewitz builds the every_ candidate list ONCE (evaluating limit up front, counter still 0), so a mutating-counter test in the limit never bites. Skipped fronts logged (no silent truncation). **KEY SCOPE-SAFETY FIX:** `FUNC_declare_war_with_wargoal_province` OVERWRITES `scope:current_war_scope` every call → pin the PRIMARY reconquest war to `scope:sep_primary_war` BEFORE the liberation loop; backer joins `sep_primary_war` (else it'd attach the kin-state to the last liberation war on the WRONG side). `set_primary_culture` accepts a culture scope (se_LAND.txt:709/717) so `primary_culture=scope:sep_culture` is valid.
- **Braces:** se_SEPARATISM **106/106** (post cross-border), 00_default wargoals 23/23, 00_decade_country 8/8, 00_monthly_country 36/36. `SE_qing_armies` (imp19c_effects_legion_setup.txt) = the existing Eight Banner / Green Standard implementation (found while verifying raise_legion/create_unit primitives).
- **#50 Qing civic (national) identity ✅ DONE 2026-07** (§11 L4 ext, all in `se_QING_DECLINE.txt` + modifiers/effect/GUI/loc). New 0..100 CHI meter `qing_civic_identity` — the POSITIVE counterweight to `qing_ethnic_tension`. Drift target = `qing_selfstr_progress` (civic can't outrun self-strengthening — its schools/press ARE the connective tissue); hollow flag halves ceiling, dyarchy +15, banner/han −20; ±2/quarterly. **CIVIC RELIEF (user chose "strong"):** effective tension target = scanned demographic target − `civic_identity/2`, so maxed civic shaves 50pts → drops below SEPARATISM_qing_pulse's `>=45` gate = nation-building really defeats separatism. Annual `QING_DECLINE_civic_assimilation` (00_yearly_country, CHI+human): **(A) integration bridge** (≥50) integrates ONE rightful culture/yr — iterate restive de jure provinces, positive `any_country_culture{is_integrated=no is_culture=scope:}` (both presence+un-integrated test, skips fully-displaced), body-side `qing_civic_bridge_done` sentinel cap; **(B) sinicization melting pot** (≥70, user KEY POINT: applies to HAPPILY-integrated pops too, function of MAJORITY WEIGHT not strife) — ≤4 most-populous provinces where Sinitic already ≥50%, `set_pop_culture` one non-slave/non-noble pop into local Sinitic plurality, +second if ≥75%, NO unrest/happiness filter. Bands nascent/emerging/national at 25/50/85. Player lever `QING_promote_civic_identity` + GUI `qing_action_promote_civic` (£90, shown selfstr>=10). Reuses existing `chinese_group` (culture groups are STATIC — no runtime set_culture_group). **REVIEW FIXES:** scope-direction bug (part A originally nested any_owned_province inside a culture scope — the reverse-scope trap; the province var doesn't live on the culture), part-B silent-no-op sentinel+LOG_fail (set on ROOT, loop body scope=province), noble filter on outer gate to match helper, presence-guard on part-A integrate. Braces se_QING_DECLINE 609/609, se_QING_MECHANICS 89/89, QING_mechanics_actions 155/155.
- **OPEN follow-ups (user, queued):** (#48) IMPLEMENT diplomatic-play wiring to L3/L4 (agitator_sponsorship → dejure_disaffection target-selector + L4 backer-precommit) — NEXT; (#49) IMPLEMENT Eight Banner/Green Standard depth + Ten Great Campaigns commemorative legacy tree (AVOID raise_legion/create_unit until SE_qing_armies errors fixed) — after #48; (#51) cross-border ethnic rebellion — ✅ DONE (see above). **Known assumption flagged to user:** neighbour queries run on the same-tick freshly-spawned rebel; if engine adjacency isn't refreshed until after the tick, cross-border + backer no-op on the spawning secession — needs -debug_mode confirmation (shared with already-shipped backer code).

**CADENCE (decided):** 5-yearly, on the PROVEN `yearly_country_pulse` (defined in BOTH `common/on_action/00_yearly_country.txt` AND `oa_wealth_changes.txt` — they merge; engine-fired) with a self-gating 5-year cooldown var. Do NOT use `biyearly_country_pulse` (empty, never invoked, firing unverifiable). **REUSE PRINCIPLE (user, firm): don't recompute — read the economy's already-cached push/pull fields.** Efficiency comes from reuse, not cadence.

**Layer 2 starts from EXISTING work:** `common/script_values/MIGRATION_svalues.txt` ALREADY exists with `MIGRATION_push_province` (= total_population + num_of_proletariat + province_unrest×10). Extend it, don't duplicate. Not wired anywhere yet. (Vanilla `MIGRATION_TARGET_*` defines in 00_defines.txt are unrelated engine AI-migration constants.) Verified reuse tokens: `JOBS_unemployed_pops_province` (pull), `dominant_province_culture` (cheap culture read), `pop_hapiness` (order_by to move unhappiest first), `owner.culture`, `primary_culture`. `ordered_neighbor_province {order_by=<script_value> max=N}` proven (naval_raiding.txt); `order_by=<named script_value>` proven (DIPLOMACY_global_power_score).

Engine facts verified: `move_pop`, `create_state_pop=<poptype>`, `set_pop_type`, `kill_pop`, `random_pops_in_province{limit=...move_pop=...}`, `MOVE_POP_TO_EMPTY_MONTHS_COOLDOWN`, `move_pop_cost_modifier_inv`. on_actions are province scope; `on_military_annex` scope=annexed country, scope:actor=annexer. Opinions declared in `common/opinions/imp19c_opinions.txt` (value + yearly_decay), loc in `opinions_l_english.yml` (BOM, LF). `00_specific_from_code.txt` is BOM+CRLF (Edit tool corrupts — use byte-exact Python). Log via se_LOG (sys tags free-text; use CLAIM / MIGR).
