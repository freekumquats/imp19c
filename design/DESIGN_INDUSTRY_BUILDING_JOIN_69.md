# DESIGN — join the concrete Modern Industry buildings to the goods sim (#69) — v2 (premise corrected)

**Status:** design v2, 2026-08-10. **v1 was built on a FALSE premise and is REJECTED** (see below). Rests on: DIAGNOSIS_INDUSTRY_TWO_SYSTEMS_69.md (Stage-0) + RESEARCH_INDUSTRIAL_REVOLUTION_69.md + the adversarial review of v1 (5 findings, 3 CRITICAL) + DESIGN_MG_BUILDING_PRODUCTION_HOOKS.md (the 2026-08-04 MG-hook, commit 873c4af99).

TERMINOLOGY: "BOM" = byte-order-mark file header ONLY. The manufacturing recipe is "bill of materials".

## WHY v1 WAS WRONG (the review's premise inversion — verified in source)
v1 (and the Stage-0 diagnosis) claimed the concrete industrial buildings "**never** produce in the goods sim." **That is FALSE for 3 of the 4**, and my own memory index records why: the **2026-08-04 MG-hook (873c4af99)** already wires them. Verified in source:
- **textile_mill → clothing**: `GOODS_svalues.txt:3064` adds `GOODS_governorship_clothing_infra_output` INTO the clothing `produced_mechanised` svalue; `:3090` = `num_of_qing_textile_mill_building × GOODS_textile_mill_clothing_output(=2)`.
- **machine_works → (early_)munitions**: `GOODS_svalues.txt:2799` (`num_of_qing_machine_works_building × …_munitions_output`) via `munitions_infra_output`.
- **navy_yard → naval_supplies**: `GOODS_svalues.txt:2895/2920` via `naval_supplies_infra_output`.

These three are **PURE PRODUCERS — supply only** (`GOODS_svalues.txt:3062`: "Pure producer — supply only… no double-count"). This was a **deliberate, reviewed** design choice: DESIGN_MG_BUILDING_PRODUCTION_HOOKS.md:258-266 — the works add SUPPLY, add **no** demand for the finished good (demand-capped so they can't inflate the military topbar income `MILITARY_supplies_income_country`), and the ONE invariant is *"a producing building must not consume the finished good it makes."*

So v1's plan to ADD `building_factories → INDUSTRY_<good>_factories` for those three would have **double-produced** (infra_output + factory path) — the review's C1, a literal same-good double count. v1 is dead.

**Only `steel` is genuinely disconnected** — no infra_output hook exists (`GOODS_governorship_steel_produced_mechanised`, `GOODS_svalues.txt:2990`, sums only the cottage var + the factory-count path; the steel_works/blast_furnace buildings touch neither).

## The CORRECTED gap #69 actually targets
The literal title: *"Connect the live Modern Industry buildings to the goods sim (blast furnace **actually consumes iron → produces steel**, etc.)."*
- 3 of the 4 buildings ARE already connected (they produce) — the "connect to the goods sim" verb is satisfied for them by the MG-hook. What they don't do is CONSUME inputs (produce-only by deliberate design).
- **`steel` is the one building fully disconnected AND is the title's literal example** ("consumes iron → produces steel"). This is the core, unambiguous #69 deliverable.

## The design — TWO honest parts, scoped by regression risk

### PART 1 (CORE, ships now) — wire steel via the single factory-count read point (H1)
The review's H1 correction: the numeric factory-count read is NOT in `produced_mechanised` (that's only a `has_variable` gate); it's one layer down in `INDUSTRY_steel_factories` (`INDUSTRY_svalues.txt:165` = `var:INDUSTRY_factories_assigned_steel`). Both PRODUCE (`INDUSTRY_production_steel_base` `multiply = INDUSTRY_steel_factories`, `:2651`) AND CONSUME (`INDUSTRY_demand_steel_iron` `value = INDUSTRY_steel_factories`, `:2699`; `_steel_coal` `:2726`) funnel through it. **So editing ONE svalue makes a steel works both produce steel AND consume its iron+coal bill of materials — automatically consistent.** This is exactly "blast furnace actually consumes iron → produces steel."

**Impl (v2-review findings #1-#6 folded):**
1. **`INDUSTRY_steel_factories` (`INDUSTRY_svalues.txt:165`)** → `value = var:INDUSTRY_factories_assigned_steel` **+** `add = INDUSTRY_building_factories_steel` (the effective count). The single-point edit; produce (`:2651`) + BOTH consume paths (iron `:2700`, coal `:2727`) read this one svalue → automatically consistent. **v2-review CONFIRMED** the only two DIRECT `var:INDUSTRY_factories_assigned_steel` reads that bypass this svalue — `used_industry_slots` (`INDUSTRY_svalues.txt:92`) + the GUI remove-button gate (`industrial_goods_buttons.txt:830`) — MUST stay on the button-assigned count (they concern player estate slots), so not touching them is correct, not a desync.
2. **`INDUSTRY_building_factories_steel`** (new CACHED-WRAPPER svalue) — reads `var:steel_works_infra_cached` when present, else recomputes (the proven `*_infra_cached` degrade-to-correctness idiom). **v2-review MED-2:** `INDUSTRY_steel_factories` is read 3×/quarter/governorship (produce + iron + coal), so the province walk MUST be cached, not re-walked 3×. The compute (`INDUSTRY_building_factories_steel_compute`) does the **TWO-LEVEL** walk (**v2-review MED-1:** from governorship scope you cannot reach provinces directly): `every_governorship_state = { every_state_province = { add = num_of_qing_steel_works_building } }` — matching the proven clothing hook (`GOODS_svalues.txt:3087-3088`). `num_of_qing_steel_works_building` is the proven province-scope count form (precedents `GOODS_svalues.txt:2776/3090`); the `num_of_building={}` block form does NOT exist (`audits/AUDIT_IDIOM.md:37`). Cache WRITE rides the existing once-per-quarter pass: extend `GOODS_cache_munitions_infra` (`se_GOODS.txt:37-48`, called from `GOODS_governorship_produce_all` `:1112` BEFORE all the good reads) with `set_variable = { name = steel_works_infra_cached  value = INDUSTRY_building_factories_steel_compute }`.
3. **C3 gate — v2-review CONFIRMED a NON-ISSUE; do NOT widen.** `se_INDUSTRY_setup.txt:22` → `INDUSTRY_setup_factories_steel` sets `INDUSTRY_factories_assigned_steel = 0` (`:125-129`), run for `every_country → every_governorships` (`oa_economy_setup.txt:246-248`). So the `has_variable = INDUSTRY_factories_assigned_steel` gate (`se_GOODS.txt:1467`, `GOODS_svalues.txt:2995`, `DEMAND_svalues.txt:1131`) is ALWAYS true for every governorship. The v1-proposed `OR{...building_factories>0}` gate-widen is UNNECESSARY — leave the three gates untouched.
4. **Building→good: `qing_steel_works_building` → steel ONLY.** Per H2 + research (§1a/§1d): a blast furnace terminates at pig/wrought iron, NOT steel; `IND_blast_furnace_building` is `trade_goods = iron` + `base_resources = 2` (`IND_heavy_industry_buildings.txt:57`) — already the raw-iron layer. So `IND_blast_furnace` + both coal mines SKIPPED (raw goods, base_resources path). The integrated ironworks (`qing_steel_works_building`, 漢陽鐵廠) is the steel converter — coherent that an integrated plant mines+converts (it sits on `trade_goods = iron` + `base_resources=2`).
5. **Per-building weight** = 1 (1 steel works = 1 assigned-factory-equivalent), best-guess. **v2-review LOW-MED-4:** this is a MUCH larger unit than the MG-hooks' flat 2-3 (the works routes through the full factory RATE + demands 10 iron + 6 coal/works, `INDUSTRY_svalues.txt:2694-2696/2721-2723`). Flagged boot-tunable (ASSUMPTIONS); the LOG line (step 6) MUST emit the resulting iron+coal demand too so the magnitude is visible.
6. **-debug_mode logging** — a per-governorship LOG line each quarter the steel-works count is nonzero: emits the building count + effective steel factory count + the iron/coal demand it drives (STATIC label, no macro/#), so the boot SURFACES a steel works becoming a real steel factory AND its bill-of-materials draw.
7. **v2-review LOW-5 (ROW asymmetry — stated explicitly):** `qing_steel_works_building` is culture-gated `jurchen`/`chinese_group` (`qing_industry_buildings.txt:43-48`) and is the ONLY steel-referencing building in the repo, so ONLY the Qing get building→steel; ROW industrialisers keep steel via assigned factories only. This is INTENDED (asymmetric-fidelity: China is the mod's fine-grained subject, ROW abstracted — the standing China-granularity rule). Not a gap.
8. **v2-review LOW-6 (don't oversell "automatically consistent"):** produce is scaled by `INDUSTRY_employment_ratio` + shortage maluses; consume (iron/coal) is not — so under labour strain they aren't perfectly proportional. Safe (employment_ratio defaults to 1 for a works-only governorship, guarded `used_industry_slots>0 else 1`, `INDUSTRY_svalues.txt:3895-3908`; the works count does NOT enter `used_industry_slots` so it never cannibalises existing factories' ratio — identical to existing assigned-factory semantics). Just don't claim perfect proportionality.

### PART 2 (input-consumption for the 3 already-producing buildings) — SCOPE BOUNDARY, flagged LOUDLY, NOT silently dropped
The 3 MG-hooked buildings (clothing/munitions/naval_supplies) already PRODUCE but do NOT consume their inputs (produce-only by design). Making them ALSO consume would complete the consume→produce realism for them too. **But it is NOT a clean add** and I am NOT folding it silently. **v2-review CORRECTED the rationale (finding #3) — the boundary is legitimate but for a COHERENCE reason, not the income-regression reason I first gave:**
- v2-review found my income-regression framing OVERSTATED: the 3 goods' finished-good produce is a FLAT, unthrottled infra_output add (`GOODS_svalues.txt:3064`, outside the factory `if`, no shortage malus), and input demand scales by `factories_assigned_<good>` not the building count — so you COULD add a pure consume-side term without touching produce, and it would NOT regress `MILITARY_supplies_income_country`. So income-regression is not the true blocker.
- **The REAL reason PART 2 is separable (valid):** a pure consume-add without throttling produce is INCOHERENT ("free production that also burns inputs"). Doing it PROPERLY — input-availability-throttled produce, the way steel genuinely is in PART 1 — is a coherent-realism REFACTOR of a shipped, reviewed subsystem (the MG-hook produce-only model, DESIGN_MG_BUILDING_PRODUCTION_HOOKS.md:258-266 documents produce-only as deliberate). That refactor deserves its own diagnosis→review, for the coherence/reviewed-refactor reason.
- **Filed as a tracked follow-on task (not just this paragraph)** so it can't quietly evaporate: overnight doc records it as an explicit open item ("#69-followon: input-throttled produce for the 3 MG-hook buildings").

**#69's title ("connect … to the goods sim") is DELIVERED:** steel (the disconnected building + the literal example "blast furnace actually consumes iron → produces steel") gets full consume→produce; the other 3 are already connected (produce). PART 2 (their coherent input-throttled consume) is bounded out with the corrected coherence-refactor reason + filed as a tracked follow-on. **v2-review RULED this a legitimate boundary, NOT a disguised cut** (bounded-out WITH a real reason + flagged loudly + logged = allowed under the mod's cardinal no-silent-cuts rule).

## GLOBAL / #23-#60 safety (PART 1)
- `qing_steel_works_building` is invention/civ-gated (~1860s+, post-Bessemer era) — a 1763 game has none → `INDUSTRY_building_factories_steel` = 0 → **byte-identical to today until a country builds a steel works.** (Review M1: gating is by invention reachability, not date; verify no 1763 country starts owning it, and note the mid-game-anachronism risk is a separate calibration matter.)
- Raising steel supply LOWERS the steel price (trade/price blend), NOT the currency/silver gbip — **review CONFIRMED #23 peg reads only gold/silver reserves × metal unit-prices** (`CURRENCY_svalues.txt:234-278`); no manufactured-goods price touches the backing. Peg untouched. Div/0 CONFIRMED safe (guarded divisions, `country_unit_price min = 0.0001`).
- Also newly CONSUMES iron+coal where steel works exist → raises iron/coal demand (can create shortages that throttle steel output via the existing `INDUSTRY_malus_steel_production_*` — self-limiting, correct).
- Perf: the province-count scan rides the proven `COTTAGEIND_cache_all_values` per-governorship-per-quarter cache pattern (`se_COTTAGEIND.txt:39-116`); one `every_state_province` pass, cached to a var (review-confirmed within envelope).

## ASSUMPTIONS / GUESSES (→ overnight ASSUMPTIONS)
- Steel-works per-building weight = 1 factory-equivalent (best-guess; boot-tune vs button-factory output).
- PART 1 only (steel). PART 2 (the 3 produce-only buildings' input-consumption) deliberately bounded out — see PART 2; the review may pull it in.
- C3: assuming `INDUSTRY_setup_all_factory_assignments` inits `factories_assigned_steel=0` for every governorship (→ gate always true). VERIFY at impl; if false, widen the 3 gates.

## VERIFY (boot)
- 1763 game: `INDUSTRY_building_factories_steel` = 0 everywhere, steel sim byte-identical (no steel works exist).
- Industrialised save: building a `qing_steel_works` raises the effective steel factory count → steel appears in the stockpile AND iron+coal demand rises (bill of materials consumed); the per-governorship LOG line shows it. No accumulation (pure count read).
- Steel price falls sanely where steel works concentrate; #23 gbip flat (gold/silver only); no ROW break; no div/0.

## Rejected (logged)
- **v1 (add building_factories to all 4 via a separate store):** built on the false "never produce" premise → double-produces clothing/munitions/naval_supplies (review C1). REJECTED wholesale.
- **IND_blast_furnace → steel (v1 map):** category error — a blast furnace makes pig iron, not steel (research §1a; H2); it already raises iron via base_resources. SKIP.
- **machine_parts / ships mapping (v1):** WRONG — the MG-hook already established the correct goods (munitions/naval_supplies) for those buildings; don't re-map.
- **Separate-store recompute-not-accumulate scheme (v1):** unnecessary — a pure svalue province-count read has nothing to accumulate; the stored-var scheme only existed to dodge a nonexistent problem.
