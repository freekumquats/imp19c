# Boot Test Notes — merge-overnight

Branch: `merge-overnight` | HEAD: `ca0907d54`
Baseline: `master`. Untested commits = everything in `git log master..merge-overnight`.
Test machine: separate box (see [[imp19c-testing-on-other-machine]]) — must be pushed.

Boot with `-debug_mode` so LOG emits. Logs land in `~/Downloads` (error.log is huge — narrow with patterns).

---

## Under test (headline features)

- **[1ea45ce52] 37 new buildings** — 5 seed-only (Hanlin/Guozijian/Temple of Heaven/Ancestral Temple/Dujiangyan) + 32 buildable across scholarship/religion/garrison/agriculture/commerce/industry. New GUI section rows (Garrison/Scholarship/Religion/Agriculture/Commerce/ModernIndustry×2). Parallel capacity gate for industrial.
- **[a2d5651ba] Macro builder allowlist** — Qing buildings registered via config `includes` (root-cause fix).
- **Qing elite units** — Green Standard / Eight Banners / Bayara / EVA / Brave Battalions; spawn routing; subunit icons; Beiyang/Nanyang missions.
- **Military-law unit gating** — doctrine tiers gate traditional vs modern units.
- **DDS icons** — 367 bespoke icons + 443 modifier icons + mission headers.

---

## What to check first (buildings feature)

- [ ] Game boots to 1763 bookmark as Qing without crash
- [ ] Seed buildings present at start: Beijing (Hanlin/Guozijian/Temple of Heaven/Ancestral Temple/Confucian temple/exam hall/mint), Chengdu (Dujiangyan), Xi'an/Jingzhou/Guangzhou/Fuzhou/Hangzhou (banner garrisons), Turpan (karez), Kaifeng (river conservancy), Hohhot (horse pasture), Taiyuan (draft bank)
- [ ] Search log for `SKIP` on `SE_qing_starting_buildings` — flags guard failures / wrong province IDs
- [ ] 32 buildable buildings appear in the province build menu + macro builder
- [ ] New GUI section rows render (no overflow, no missing block/blockoverride)
- [ ] Loc: names/descriptions/tooltips all resolve (no raw keys shown)
- [ ] Icons resolve (no placeholder squares)

---

## Findings

| # | Feature | Severity | Symptom | Repro / where | Status | Date logged |
|---|---------|----------|---------|---------------|--------|-------------|
| 1 | 37 new buildings | COSMETIC | All 37 showed placeholder icons | Build menu + province panel — icons resolve by filename `gfx/interface/icons/buildings/<key>.dds`, none existed | FIXED+pushed `ac8e26374` (bespoke 200x200 BGRA8; generator consolidated into tools/gen_table_icons.py in `b8a7872da`) | (prior session) |
| 2 | strata/class modifier icons | COSMETIC | Strata "desired ratio" + "output" (+happiness) modifiers all show the SAME placeholder icon | Modifier tooltips. ROOT CAUSE: **35** modifier `.dds` files in `gfx/interface/icons/modifiers/` are byte-identical copies of one placeholder (md5 4aeb90c8): `{local,global,culture}_{upper_strata,middle_strata,lower_strata,proletariat,indentured}_{output,happiness,desired_pop_ratio,city_desired_pop_ratio}`. Format is valid so they render — but all identical. NOT the buildings work; pre-existing. | FIXED (main claim already resolved by #115 `25f7870dc`, IN-BRANCH — 40 strata tiles are 20 distinct stratum×metric images, placeholder md5 gone; the boot test predated #115). Live remnant: 3 mod-only nobles desired-ratio icons registered but missing → generated via gen_modifier_icons.py nobles tier. Pushed merge-overnight `1399a36f4`. | (prior session) |
| 3 | indentured strata loc | BROKEN | `local_indentured_output` (and all `*_indentured_*` output/desired-ratio modifiers) have NO English loc → raw key shown | `localization/english/modifiers_l_english.yml` has upper/middle/lower/proletariat rows for output+desired_pop_ratio but ZERO indentured rows (only `*_indentured_happyness` exists elsewhere). | FIXED+pushed merge-overnight `872d6827d` — report was STALE: indentured *output* loc already added by #114; no indentured *desired_pop_ratio* key is referenced anywhere. REAL gap = custom-strata `*_happyness` (local/global × upper/middle/lower/proletariat/indentured) had NO loc, heavily referenced (e.g. global_upper_strata_happyness ×67). Added all 10 rows. | (prior session) |
| 4 | cultural district | BROKEN | Cultural district shows no description (literal "ADD desc") | `localization/english/text_l_english.yml:162` → `URB_cultural_district_desc:0 "ADD desc"` placeholder. Siblings (commerce/admin district) have real `_desc`. | FIXED+pushed `fc2d5e0ec` | (prior session) |
| 5 | National Ideas | BROKEN | ~10 ideas showed blank descriptions | `ideas_I_english.yml` had `_desc:0 ""` for world_police/mercantilism/free_trade/merchant_colonialism/settler_colonialism/isolationism/counter_colonialism/civilising_mission/defender_of_the_faith/pan_nationalism. | LOC FIXED+pushed `fc2d5e0ec`; ICONS still pending (task #117) | (prior session) |
| 6 | mission tree art | COSMETIC | Specific trees should use specific real images; BOTH card+banner must change | User-supplied images for burma_war / xinjiang / colonization / central_asia. Built reusable `PHOTOS` table in `tools/gen_mission_headers.py` → writes BOTH the 300×120 selector card (`icons/missions/<icon>.dds`) AND the 624×120 header banner (`missions/mission_image_qing_<tree>.dds`), DX10 BGRA8-sRGB, cover-cropped from one source. | FIXED+pushed `ca0907d54` (4 trees) | (prior session) |
| 7 | 37 new buildings | BROKEN | Many new buildings visible but show placeholder desc "WIP: Cost of building, effects on coset, construction supplies, etc" (likely a shared `_desc` stub; also flags real cost/effects/construction-supplies data may be unset) | Building tooltip / province build menu. Reported during merge-overnight boot test. | FIXED+pushed merge-overnight `872d6827d` — was NOT per-building missing desc: all 82 building keys have real `_desc` + set cost/time/effects. The "WIP…" text was stale scaffolding appended to the shared `building_generic_info` footer (injected under ALL buildings via `BUILDING_ITEM_WITH_INSTRUCTIONS`/`tooltip_building_bottom`). Dropped the WIP text; live `[BuildingItem.GetBuildInfo]` promoter already renders real cost/effects/supplies. | **2026-07-29** |
| 8 | mission tree structure | BROKEN | New mission additions are badly shaped — laid out as ONE start with ~a dozen direct children (flat fan-out), not as a proper tree. DESIRED SHAPE: trees should be **taller than wide** — many successive layers of missions following one another (deep chains), FEW missions per layer. Each node branches to ~2–4 children, successive layers converge on a capstone. Currently the opposite (wide+shallow). Affects the trees expanded to reach the 20-mission minimum. | Mission tree GUI. Likely the `prerequisites`/positioning in the expanded mission files (trees filled for #127/#129 etc.). Reported during merge-overnight boot test. | FIXED+pushed merge-overnight `aabbb6fe3` — cause: gen_mission_tasks.py hard-coded every generated task's requires={tree root}. Layout is 100% engine-computed from the requires DAG (no position fields). Reshaped 14 trees via tools/reshape_mission_trees.py to the UPSTREAM shape (measured Invictus 58 + TI 52 trees: median root fan-out 4, depth 5): fan-out 12-18→4-6, depth→5-10, all DAG-validated (0 orphans/cycles). Military-tradition trees already fine. | **2026-07-29** |
| 9 | new technology icons | FIXED+pushed merge-overnight `782e5b55f` — replaced gen_invention_icons.py's hash-rosette emblem() with a 37-motif subject system (rifle/cannon/ship/kiln/rail/coin/telescope/cogwheel/… keyed by invention meaning) drawn on the domain-tinted disc; all 271 regenerated, pipeline unchanged, reviews passed. ORIG: New tech icons are visible (no longer vanilla/placeholder) but look "odd": they all appear as colored sprites showing **node-and-vertex / graph markings** (i.e. look like generic graph/network diagrams, not subject-appropriate tech artwork). Suggests a wrong or generic source image was used for the icon generation rather than per-tech art. DESIRED: each icon should **abstractly represent the specific invention** it belongs to — model the icon↔invention linkage on how VANILLA tech icons map to vanilla inventions (study vanilla for style/subject convention, then match it per new invention). SCOPE: the icon-REPLACEMENT methodology/pipeline is PROVEN (see [[imp19c-invention-icons]] / [[imp19c-icon-generator-canonical]] / [[imp19c-dds-icon-pipeline]] — art via `icon_override` → `GFX_<key>` spriteType; generated by `tools/gen_table_icons.py`). This is NOT a pipeline bug — it's purely the **accuracy of the source art chosen per invention** (generic graph/node images were used instead of subject-appropriate ones). Fix = feed correct per-invention art into the existing generator; do NOT rebuild the pipeline. | Technology view. Reported during merge-overnight boot test. | OPEN | **2026-07-29** |
| 10 | Titles Unassigned notification | BROKEN | The "Titles Unassigned" alert/notification still does NOT register/fire for vacant Grand Council positions. **REGRESSION / fix-not-effective:** task #130 was previously marked completed for exactly this — either the fix didn't take, was on a different branch, or doesn't cover GC seats. | GC panel / top-bar notification when a GC office is empty. Reported during merge-overnight boot test. | FIXED+pushed merge-overnight `8d865ae44` — #130's token fix WAS present/correct. Real cause = timing: QING_council_init (game-start) never set qing_council_vacancy; only QING_council_recompute does, which at start runs via the DEFERRED day-32 autofill. So for the first month the var was unset → alert's `.IsSet` gate false. Fix: seed qing_council_vacancy=13 at construction (guarded). | **2026-07-29** |
| 11 | employment laws | FIXED+pushed merge-overnight `7285637a0` — added common/laws/00_employment_laws.txt (working_hours / child_and_bonded_labour / workplace_safety_and_tenure), hooked into JOBS_buildings_cap fill-ratio + strata/indentured economy tokens; GUI+loc+bias-var default-reset; design+post-impl reviews (upstream-grounded) passed. ORIG: There should be **laws governing employment** — no law category/options currently cover it. Feature request (not a defect). Would pair naturally with the manufactured_goods employment→output→wages loop (I5/I8). Scope TBD: which employment dimensions (child labour, working hours, min wage, unionisation, workplace safety, serfdom/indenture, hire/fire), which strata affected, effects on output/happiness/wealth. **INTEGRATION REQ: must tie into the EXISTING building + goods mechanics** (not a standalone modifier layer) — e.g. affect cottage/factory production, employment fill-ratio (I5), goods output, building eligibility/output. | Laws / government screen. Reported during merge-overnight boot test. | OPEN | **2026-07-29** |
| 12 | industrialization laws | FIXED+pushed merge-overnight `7285637a0` — added common/laws/00_industrialization_laws.txt (industrial_capacity_policy [moves the factory-build ceiling via global_import_commerce_modifier] / enterprise_ownership [NOT=CHI, excl. w/ Qing statute] / cottage_to_factory_transition [biases cottage production multiplier]); GUI+loc; reviews passed. ORIG: There should also be **laws governing industrialization** — no law category/options currently cover it. Feature request (not a defect); companion to #11. Would tie into the manufactured_goods factory/industry layer. Scope TBD: e.g. state-vs-private industry, factory subsidies/tariffs, pace-of-industrialisation policy, guild protection vs free enterprise, pollution/regulation, capital allocation — with effects on industrial output, construction, strata wealth/happiness. **INTEGRATION REQ: must tie into the EXISTING building + goods mechanics** (buildings, cottage/mechanised production chains, DEMAND/prices, employment) — not a standalone modifier layer. | Laws / government screen. Reported during merge-overnight boot test. | OPEN | **2026-07-29** |

<!-- Severity: CRASH / BROKEN / COSMETIC / NIT -->
<!-- Log each as you hit it; I'll triage root cause from the diff. -->

### From boot logs (`~/Downloads/logs.zip`, 2026-07-29 17:xx — merge-overnight test box; error.log 36k lines)

| # | Feature | Severity | Symptom | Repro / where | Status | Date logged |
|---|---------|----------|---------|---------------|--------|-------------|
| 13 | event/culture/tradition parse cascade | BROKEN | 437× `Unexpected token: is_triggered_only` + 136× `Unexpected token: secondary` — reported lines are VALID tokens, spread across nearly every `common/cultures/*`, `common/military_traditions/*` (incl. #129 `00_qing.txt` 40 hits) and many `events/imp19c_mod_events/*`. Signature of a CASCADE from an earlier brace/syntax fault, NOT per-file bugs (all tradition files checked are brace-balanced). Need to find the seed file that unbalances the reader. NOTE: logs are from merge-overnight box; local repo is on manufactured_goods so line numbers may differ. | `pdx_persistent_reader.cpp:229` across culture/tradition/event loads | NOT A BUG (investigated 2026-07-29) — NO cascade, NO brace fault: every file in the affected dirs is brace-balanced. `pdx_persistent_reader.cpp:229` is the engine's save-history reader, which uses a narrower grammar and chokes per-occurrence on valid main-grammar tokens: `secondary`/navy-keys in 136 culture files (=136×) and `is_triggered_only` in 83 event files (≈437×). Counts = literal token occurrences, not brace errors. Already diagnosed benign in `upstream_bugs.md` (U3) / `23_and_24.md` / `existing_economy_errors.md`: "report upstream, don't chase." Only "fix" = deleting required fields (not advisable). | **2026-07-29** |
| 14 | undefined triggers (building keys) | BROKEN | `Unknown trigger type: IND_industrial_estate` (2), `IND_resource_gathering_operation` (4), `URB_cultural_district` (2) — building/urban keys used in a TRIGGER context but not defined as scripted_triggers (none found in common/scripted_triggers/). Also `Unknown trigger type: id` (28×). Likely macro-list `is_in_list`/block-form misuse ([[imp19c-macro-list-trigger-rule]]) or a building-check written as a bare trigger. | scripted_triggers / wherever these keys are used as triggers | FIXED+pushed merge-overnight `475fea67d` — 8 bare building-key comparators in FlavorEvents.txt (flavor_eve.12/.14/.15) → `num_of_<key> >= 1`; 28 bare `id = N` in noble_titles.3 → `this = char:N`. Both idioms upstream-verified (Invictus + TI). | **2026-07-29** |
| 15 | missing GUI texture flood | COSMETIC | 8,565× `Could not find texture 'gfx/interface/base_parts/gradient_black_flip.dds'` — a GUI base part the mod references but the .dds is absent (not shipped, or wrong path). Single missing asset, huge log volume. | GUI base_parts; `gui_base.gui` and friends | FIXED+pushed merge-overnight `e7c8d11d2` — pre-existing (~2yr), not a MO regression, but largest flood (44 refs in 9 gui files). Regenerated self-contained: recovered gradient_black.dds from repo history (6bbc98f3f), reversed its 33 uncompressed 32bpp BGRA rows, kept the 128-byte DDS header identical. No external blob. | **2026-07-29** |
| 16 | economy script-error floods | KNOWN/PRE-EXISTING | 5,082× `Script system error!`; 2,719× `change_variable ... Variable not of the 'value' scope type. Type: empty`; CURRENCY none-value + `Div/0`; 273× `TRADE_national_expenditure ... not being set`; wealth-distribution + CONSUME_all_stockpiles chains. Matches the documented economy floods ([[imp19c-econ-log-scope-split-bug]], [[imp19c-1763-economy-log-floods]], [[imp19c-upstream-U4-flood-fix]]). Mostly pre-existing/upstream, NOT merge-overnight features — but volume is high; confirm none are NEW from the MG branch when that's tested. | `oa_wealth_changes.txt`, `CURRENCY_svalues.txt`, `WEALTH_svalues.txt`, `DEMAND_luxury_svalues.txt` | OPEN — triage new-vs-known | **2026-07-29** |
| 17 | GUI widget/color warnings | NIT | 319× `gui_base.gui - Failed setting property 'color'`; 65× `Widget has not been destroyed`; 343× map-object locator `too far outside province bounding box` (vfx/unit_stack/combat). Low-priority engine/GUI noise; record for completeness. | gui_base.gui, map locators | OPEN | **2026-07-29** |

<!-- (findings #13-17 mined from logs.zip, not live gameplay observation) -->

### Incidental findings surfaced during MO fixes (out of scope, recorded for follow-up)
- **Duplicate invention key `tech_electrochemistry`** — defined as a node in BOTH `common/inventions/00_civic_inventions.txt:646` AND `00_religious_inventions.txt:346`. `requires = { tech_electrochemistry }` (religious:360) is ambiguous; `domain_of_key()` lets the last-globbed file win the icon tint. Pre-existing, not from any MO fix. Latent data issue — needs a dedup pass.
- **Pre-existing svalue bug** — `TECH_cottage_industry_overall_bonus` (TECH_svalues.txt) gates BOTH its adds on `invention = tech_manufactories`; the second (`TECH_mechanical_tools_bonus_cottage_industry`) almost certainly should gate on `tech_mechanical_tools`. Surfaced during MO#12; outside its scope.

## Notes / open questions for user
- Findings #2–#4 are PRE-EXISTING mod/upstream issues (not from the 37-buildings work) surfaced during this boot test. Fixable but out of the buildings scope — confirm you want them fixed now vs. tracked separately.
- Finding #2 (35 identical strata icons): needs 35 distinct icons, OR at minimum per-stratum art shared across local/global/culture variants. Strata concepts (upper/middle/lower/proletariat/indentured) × metric (output=coins/gears, happiness=face, desired-ratio=scales/target). Confirm desired granularity before I generate.
- `local_nobles_desired_pop_ratio` / `global_nobles_desired_pop_ratio`: loc exists but icon file MISSING entirely (would placeholder too).
