########################################################################
# imp19c_effects_legion_setup.txt — historical starting-army setup
#
# create_unit / raise_legion follow the engine idiom verified against the
# feasibility oracles (Invictus, Terra-Indomita): every create_unit declares a
# base `sub_unit = <type>`, adds strength with `add_subunit = <type>`.
#
# TWO commander idioms are used in this file, deliberately:
#   1. The QING GARRISON helpers (SE_qing_raise_garrison[_cmd]) create the unit at
#      COUNTRY scope with an inline `commander = <char>` field — the proven
#      free-placement idiom (Invictus galatian_invasion.txt:715; 00_event_effects_
#      inv_1_0.txt:4281). This is REQUIRED because raise_legion musters at the
#      governorship seat and ignores `location`, which stacked the whole OOB at
#      Beijing (#241-fix, oracle-confirmed 2026-07-08).
#   2. The 1815 European coalition legions below (SE_occupation_of_france etc.) still
#      use raise_legion + `char:X = { add_to_legion = PREV }` + random_legion_unit =
#      { set_as_commander = char:X }, since they are deliberately raised from ONE
#      capital governorship and located across France (dispersal not required there).
# (An earlier version used add_loyal_subunit / set_personal_loyalty / a pre-add
#  every_sub_unit loop / an external add_commander on a saved scope — none of
#  those are valid here; that is why these effects were disabled "due to errors".)
#
# Prefix = SE
########################################################################

########################################################################
# QING DUAL STANDING ARMY (#66) — the two permanent, separately-administered,
# ethnically-defined institutions of the 1815 Qing military, meeting only at the
# throne (see memory imp19c-qing-army-1815-research):
#
#   八旗 Eight Banners — the hereditary conquest-elite caste + strategic reserve.
#     ~half its soldiers (禁旅八旗) garrison BEIJING; the rest hold ~20 walled
#     "Manchu cities" 滿城 (mancheng) — Xi'an (largest interior), the Manchuria
#     homeland, the Xinjiang/NW frontier (Ili = seat of the 伊犁將軍), and interior
#     choke-points. Prestigious, but by 1815 militarily soft (banner-poverty
#     crisis 旗人生計, lost 國語騎射). We express that softness through the existing
#     qing_banner_decay bands (qing_banner_decay_mild / _severe country modifiers),
#     NOT by shrinking the paper rolls — hence garrisons are raised at full paper
#     size and the decay modifier carries the combat penalty ("big on paper,
#     brittle in field").
#
#   綠營 Green Standard — the Han professional TERRITORIAL/CONSTABULARY army,
#     deliberately fragmented (anti-warlordism) and dispersed across every
#     province for policing, escort, coast/canal patrol and first-line
#     suppression. Modelled as several mediocre provincial stacks under the great
#     regional commands (Zhili, Liangjiang, Huguang, Sichuan, Liangguang,
#     Shaan-Gan, Fujian incl. Taiwan, Yunnan-Guizhou).
#
# IMPLEMENTATION NOTE: every legion is raised from c:CHI.capital_scope.governorship
# (proven to exist) but LOCATED at its garrison province — exactly the pattern
# SE_occupation_of_france uses (all raised from the GBR capital governorship,
# located across France). This sidesteps any per-province state.governorship
# existence risk. Each raise is individually guarded on province existence +
# Qing ownership via the SE_qing_raise_garrison[_cmd] helpers, so a partitioned
# or shrunken Qing simply gets fewer garrisons (each miss logs LOG_fail) instead
# of a scope error. count = $size$ in the while-loop is the number of ADDITIONAL
# regular_infantry companies on top of the 1 base sub_unit (so total = size + 1).
########################################################################

# ----------------------------------------------------------------------
# Helper: raise a garrison legion of ($size$ + 1) regular_infantry companies at
# province $prov$, named $name$, with NO scripted commander (the player assigns
# one). Guards province existence + Qing ownership; raises from the Qing capital
# governorship and locates the unit at $prov$. Silent LOG_fail if $prov$ is not
# held. Assumes c:CHI exists (the caller SE_qing_armies guards that once).
# ----------------------------------------------------------------------
SE_qing_raise_garrison = {
	if = {
		# [#190-fix task #190] Accept BOTH CHI-owned land AND land held by a CHI
		# SUBJECT. The 1815 frontier banner garrisons (Ili 伊犁, Ürümqi 烏魯木齊,
		# Mukden 盛京, Heilongjiang 黑龍江, Kashgar 喀什噶爾) sit in provinces owned by
		# CHI's autonomous-governorship subjects (ILI/MKD/HLJ, XNG under ILI), not by
		# CHI directly — historically these WERE Qing frontier commands. The old guard
		# (owner = c:CHI only) silently dropped exactly these 5 garrisons via LOG_fail.
		# is_subject_of is a proven trigger (common/military_traditions/00_indian.txt;
		# Invictus parthia_interactions.txt). The unit is created by $prov$'s OWNER, so
		# a subject-held frontier province raises under THAT subject (the intended
		# "dispersed across China AND its subjects" behaviour).
		limit = { exists = $prov$  $prov$ = { OR = { owner = c:CHI  owner = { is_subject_of = c:CHI } } } }
		# [#241 → B21-v3] ROOT CAUSE of the all-Beijing stack (oracle diagnosis 2026-07-11, HIGH
		# confidence): the load-bearing bug was the `$prov$ = { owner = { create_unit } }` PROVINCE->
		# OWNER wrapper itself — NOT the location token, the tick, or a missing commander. Entering the
		# country scope THROUGH a province scope makes the new unit's placement re-resolve against the
		# scope-entry context (collapsing to ROOT's capital = Beijing) instead of honouring the absolute
		# `location` token. Every proven reference spawn (Invictus invasions.2 two land units one tick;
		# galatian_invasion.10 `create_unit { navy=no location=p:384 }`; me_tasm.13 THREE navies at three
		# ports one tick; vanilla me_duuchuu.5 TWELVE navies one tick) issues create_unit DIRECTLY in a
		# bare country scope with an absolute `location = p:X` token and NEVER via `p:X = { owner = { } }`.
		# FIX: create in a bare COUNTRY scope. For a CHI-owned province that is c:CHI; for a frontier
		# province owned by a CHI SUBJECT (ILI/MKD/HLJ/XNG) save that owner and create in ITS scope —
		# without re-entering the province. $prov$ stays the absolute location token. No commander needed
		# (commanderless creates outnumber commanded 203:66 in Invictus and persist fine).
		$prov$ = { owner = { save_scope_as = garrison_owner } }
		scope:garrison_owner = {
			create_unit = {
				navy = no
				name = $name$
				location = $prov$
				sub_unit = regular_infantry
				while = { count = $size$  add_subunit = regular_infantry }
			}
		}
		LOG_line = { sys = QING  msg = "[#241/B21-v3] Qing garrison raised: $name$ at $prov$ (+$size$ coy) via bare country-scope create_unit" }
	}
	else = {
		LOG_fail = { sys = QING  fn = "SE_qing_raise_garrison"  reason = "$prov$ not held by Qing; $name$ not raised" }
	}
}

# ----------------------------------------------------------------------
# Helper: as above, but attaches historical commander $cmd$ (a char scope) to the
# legion. Guards that $cmd$ exists too (a dead/absent character just falls through
# to LOG_fail rather than erroring). Commander attach follows the verified idiom:
# $cmd$ = { add_to_legion = PREV } then random_legion_unit = { set_as_commander }.
# ----------------------------------------------------------------------
SE_qing_raise_garrison_cmd = {
	if = {
		# [#190-fix task #190] Same subject-land acceptance as SE_qing_raise_garrison
		# above (Ili banner garrison under commander Changling char:244 is precisely
		# one of the frontier cases this restores). See that helper for the rationale.
		limit = { exists = $prov$  $prov$ = { OR = { owner = c:CHI  owner = { is_subject_of = c:CHI } } }  exists = $cmd$ }
		# [#241 → B21-v3] Same root-cause fix as SE_qing_raise_garrison above: DROP the
		# `$prov$ = { owner = { create_unit } }` province->owner wrapper (which collapsed placement to
		# the capital) and create in a BARE country scope with the absolute `location = $prov$` token.
		# Save the owner once, then create in its scope. create_unit accepts an inline `commander = <char>`
		# field (Invictus galatian_invasion.txt `create_unit { navy=no location=pX commander=scope:... }`;
		# 00_event_effects_inv_1_0.txt:4281 `commander = char:13`). The commander is attached only when he
		# is a non-ruler employed by $prov$'s owner (a CHI commander cannot lead a subject-owned frontier
		# legion — e.g. Ili raises under the ILI subject); otherwise the garrison still musters commanderless.
		$prov$ = { owner = { save_scope_as = garrison_owner } }
		if = {
			limit = { $cmd$ = { is_ruler = no  employer = scope:garrison_owner } }
			scope:garrison_owner = {
				create_unit = {
					navy = no
					name = $name$
					location = $prov$
					commander = $cmd$
					sub_unit = regular_infantry
					while = { count = $size$  add_subunit = regular_infantry }
				}
			}
			# [#331 user] mark this char as a serving Qing garrison/army commander so the
			# lifecycle event (QING_council_on_commander_death → qing_office.32) can detect
			# that a member of the officer corps has died and prompt the War Minister for a
			# replacement recommendation. Stamped ONLY on the successfully-attached path.
			$cmd$ = { set_variable = { name = qing_officer_marker  value = 1 } }
			LOG_line = { sys = QING  msg = "[#241/B21-v3] Qing garrison raised: $name$ at $prov$ (+$size$ coy) under commander $cmd$" }
		}
		else = {
			scope:garrison_owner = {
				create_unit = {
					navy = no
					name = $name$
					location = $prov$
					sub_unit = regular_infantry
					while = { count = $size$  add_subunit = regular_infantry }
				}
			}
			LOG_line = { sys = QING  msg = "[#241/B21-v3] Qing garrison raised: $name$ at $prov$ (+$size$ coy); $cmd$ not eligible to command (player assigns)" }
		}
	}
	else = {
		LOG_fail = { sys = QING  fn = "SE_qing_raise_garrison_cmd"  reason = "$prov$ unheld or $cmd$ absent; $name$ not raised" }
	}
}

SE_qing_armies = {
	LOG_enter = { sys = QING  fn = "SE_qing_armies" }
	if = {
		# [#193-fix task #193] Idempotency sentinel: on_game_initialized re-fires on every
		# save-load, and raise_legion is ADDITIVE (not a no-op), so without a guard the whole
		# ~26-garrison OOB would be re-raised — doubling the Qing army each reload. Matches the
		# SE_qing_navy / SE_qing_starting_buildings sentinel idiom (adversarial-review finding).
		limit = {
			NOT = { has_global_variable = qing_armies_setup_done }
			exists = c:CHI
			c:CHI = { exists = capital_scope }
		}

		# [bookmark-1763 #304] HIGH QING (pre-1772) ORDER OF BATTLE — the post-Xinjiang peak
		# army, LARGER + fresher than the 1815 hollowed-out force (research digest §2: the
		# 1755-59 campaigns had just been won under Zhaohui/Fude/Agui; the banner-poverty
		# hollowing + Green-Standard desertion that gut the 1815 rolls are decades away). We
		# raise the SAME garrison network (all valid 1763 Qing territory — Xinjiang annexed
		# 1759, Ili General seated 1762) at ~30% larger paper sizes, and WITHOUT the 1815
		# commander attaches (Yinghe/Nayancheng/Yang Fang etc. are unborn or not-yet-in-post
		# in 1763 — the _cmd helper's exists=$cmd$ guard would silently DROP those garrisons,
		# shrinking the army). Commander-less raises (the player/AI assigns officers). The
		# 1815 start (else) is byte-for-byte the original OOB. Gate matches the High Qing era
		# threshold in se_QING_DECLINE.txt.
		if = {
			limit = { current_date < 1772.1.1 }
			# --- 八旗 Eight Banners (peak paper strength) ---
			SE_qing_raise_garrison_cmd = { prov = p:8363  name = "Metropolitan Eight Banners 禁旅八旗"  size = 30  cmd = char:581 }  # [#331] Fuheng 傅恆, Chief Grand Councillor
			SE_qing_raise_garrison_cmd = { prov = p:7129  name = "Xi'an Banner Garrison 西安駐防"      size = 9  cmd = char:586 }  # [#331] Songchun 嵩椿 (西安將軍)
			SE_qing_raise_garrison_cmd = { prov = p:3534  name = "Ili Banner Garrison 伊犁駐防"        size = 8  cmd = char:582 }   # [#331] Mingrui 明瑞, FIRST 伊犁將軍 (attaches only if CHI-employed under the province owner)
			SE_qing_raise_garrison_cmd = { prov = p:1087  name = "Shengjing Banner Garrison 盛京駐防"  size = 5  cmd = char:589 }  # [#331] Chaoquan 朝銓 (盛京將軍)
			SE_qing_raise_garrison_cmd = { prov = p:5005  name = "Jingzhou Banner Garrison 荊州駐防"   size = 4  cmd = char:585 }  # [#331] Aligun 阿里袞
			SE_qing_raise_garrison_cmd = { prov = p:8120  name = "Hangzhou Banner Garrison 杭州駐防"   size = 4  cmd = char:588 }  # [#331] Fulu 福祿 (杭州將軍)
			SE_qing_raise_garrison_cmd = { prov = p:6659  name = "Jiangning Banner Garrison 江寧駐防"  size = 4  cmd = char:590 }  # [#331] Rongbao 容保 (江寧將軍)
			SE_qing_raise_garrison_cmd = { prov = p:3537  name = "Chengdu Banner Garrison 成都駐防"    size = 4  cmd = char:564 }  # [#331] Agui 阿桂
			SE_qing_raise_garrison_cmd = { prov = p:9298  name = "Canton Banner Garrison 廣州駐防"     size = 4  cmd = char:587 }  # [#331] Langfu 朗福 (廣州將軍)
			SE_qing_raise_garrison = { prov = p:3651  name = "Fuzhou Banner Garrison 福州駐防"     size = 4 }
			SE_qing_raise_garrison = { prov = p:209   name = "Ningxia Banner Garrison 寧夏駐防"    size = 4 }
			SE_qing_raise_garrison = { prov = p:5616  name = "Liangzhou Banner Garrison 涼州駐防"  size = 3 }
			SE_qing_raise_garrison = { prov = p:4931  name = "Kaifeng Banner Garrison 開封駐防"    size = 3 }
			SE_qing_raise_garrison = { prov = p:8501  name = "Taiyuan Banner Garrison 太原駐防"    size = 3 }
			SE_qing_raise_garrison = { prov = p:3322  name = "Suiyuan Banner Garrison 綏遠駐防"    size = 3 }
			SE_qing_raise_garrison = { prov = p:43    name = "Heilongjiang Banner Garrison 黑龍江駐防"  size = 3 }
			SE_qing_raise_garrison_cmd = { prov = p:2930  name = "Ürümqi Banner Garrison 烏魯木齊駐防"  size = 4  cmd = char:583 }  # [#331] Šuhede 舒赫德
			SE_qing_raise_garrison_cmd = { prov = p:2700  name = "Kashgar Banner Garrison 喀什噶爾駐防"  size = 3  cmd = char:584 }  # [#331] Hailancha 海蘭察
			# --- 綠營 Green Standard (peak paper strength, realm at internal peace) ---
			SE_qing_raise_garrison_cmd = { prov = p:8363  name = "Jingshi Green Standard Army 京師綠營"  size = 14  cmd = char:591 }  # [#331] Fang Guancheng 方觀承 (直隸總督)
			SE_qing_raise_garrison_cmd = { prov = p:6659  name = "Liangjiang Green Standard 兩江綠營"    size = 9  cmd = char:566 }  # [#331] Yin Jishan 尹繼善 (兩江總督)
			SE_qing_raise_garrison_cmd = { prov = p:4991  name = "Huguang Green Standard 湖廣綠營"       size = 9  cmd = char:592 }  # [#331] Aibida 愛必達 (湖廣總督)
			SE_qing_raise_garrison_cmd = { prov = p:3537  name = "Sichuan Green Standard 四川綠營"       size = 9  cmd = char:565 }   # [#331] Zhaohui 兆惠, Xinjiang campaign spine
			SE_qing_raise_garrison_cmd = { prov = p:9298  name = "Liangguang Green Standard 兩廣綠營"    size = 9  cmd = char:595 }  # [#331] Su Chang 蘇昌 (兩廣總督)
			SE_qing_raise_garrison_cmd = { prov = p:1303  name = "Shaan-Gan Green Standard 陝甘綠營"     size = 7  cmd = char:594 }  # [#331] Yang Yingju 楊應琚 (陝甘總督)
			SE_qing_raise_garrison = { prov = p:3651  name = "Fujian Green Standard 福建綠營"        size = 6 }
			SE_qing_raise_garrison = { prov = p:6781  name = "Taiwan Green Standard 臺灣綠營"        size = 5 }
			SE_qing_raise_garrison_cmd = { prov = p:5786  name = "Yunnan-Guizhou Green Standard 雲貴綠營"  size = 6  cmd = char:593 }  # [#331] Wu Dashan 吳達善 (雲貴總督)
			set_global_variable = qing_armies_setup_done
			LOG_line = { sys = QING  msg = "Qing HIGH QING (pre-1772) dual standing army raised at peak strength (commander-less)" }
		}
		else = {

		# ==============================================================
		# 八旗 EIGHT BANNERS
		# ==============================================================
		# 禁旅八旗 jinlü baqi — the metropolitan/capital banners: roughly half of all
		# banner soldiers concentrated at Beijing, by far the largest single force.
		# Commanded by Yinghe (char:340), general of the Manchu Plain Blue Banner
		# since 1814.
		SE_qing_raise_garrison_cmd = { prov = p:8363  name = "Metropolitan Eight Banners 禁旅八旗"  size = 23  cmd = char:340 }

		# 駐防八旗 zhufang baqi — provincial garrison banners in the walled Manchu
		# cities 滿城. Xi'an is the largest interior garrison; Ili (Huiyuan, seat of
		# the 伊犁將軍) is the largest western one, under Changling (char:244, who held
		# the NW command). The rest are the mid-Yangzi / coastal / frontier mancheng.
		SE_qing_raise_garrison     = { prov = p:7129  name = "Xi'an Banner Garrison 西安駐防"      size = 7 }   # largest interior mancheng
		SE_qing_raise_garrison_cmd = { prov = p:3534  name = "Ili Banner Garrison 伊犁駐防"        size = 5  cmd = char:244 } # Huiyuan, seat of the Ili General
		SE_qing_raise_garrison     = { prov = p:1087  name = "Shengjing Banner Garrison 盛京駐防"  size = 4 }   # Mukden, Manchuria homeland seat
		SE_qing_raise_garrison     = { prov = p:5005  name = "Jingzhou Banner Garrison 荊州駐防"   size = 3 }   # mid-Yangzi choke
		SE_qing_raise_garrison     = { prov = p:8120  name = "Hangzhou Banner Garrison 杭州駐防"   size = 3 }
		SE_qing_raise_garrison     = { prov = p:6659  name = "Jiangning Banner Garrison 江寧駐防"  size = 3 }   # Nanjing
		SE_qing_raise_garrison     = { prov = p:3537  name = "Chengdu Banner Garrison 成都駐防"    size = 3 }
		SE_qing_raise_garrison     = { prov = p:9298  name = "Canton Banner Garrison 廣州駐防"     size = 3 }   # Guangzhou, coastal
		SE_qing_raise_garrison     = { prov = p:3651  name = "Fuzhou Banner Garrison 福州駐防"     size = 3 }
		SE_qing_raise_garrison     = { prov = p:209   name = "Ningxia Banner Garrison 寧夏駐防"    size = 3 }
		SE_qing_raise_garrison     = { prov = p:5616  name = "Liangzhou Banner Garrison 涼州駐防"  size = 2 }   # Wuwei
		SE_qing_raise_garrison     = { prov = p:4931  name = "Kaifeng Banner Garrison 開封駐防"    size = 2 }
		SE_qing_raise_garrison     = { prov = p:8501  name = "Taiyuan Banner Garrison 太原駐防"    size = 2 }
		SE_qing_raise_garrison     = { prov = p:3322  name = "Suiyuan Banner Garrison 綏遠駐防"    size = 2 }   # Guihua/Hohhot, Inner Mongolia
		SE_qing_raise_garrison     = { prov = p:43    name = "Heilongjiang Banner Garrison 黑龍江駐防"  size = 2 } # Qiqihar, thin frontier force facing Russia
		SE_qing_raise_garrison     = { prov = p:2930  name = "Ürümqi Banner Garrison 烏魯木齊駐防"  size = 3 }
		SE_qing_raise_garrison     = { prov = p:2700  name = "Kashgar Banner Garrison 喀什噶爾駐防"  size = 2 } # westernmost outpost

		# ==============================================================
		# 綠營 GREEN STANDARD — dispersed Han constabulary under the great regional
		# commands. Numerous but individually mediocre; the real fighting edge of
		# the late Qing lay in locally-raised militia 鄉勇, not this standing force.
		# ==============================================================
		# Zhili 直隸 — the metropolitan Green Standard guarding the Beijing approaches,
		# under Nayancheng (char:358), Viceroy of Zhili (the historical 1815 command,
		# preserved from the original single-army setup).
		SE_qing_raise_garrison_cmd = { prov = p:8363  name = "Jingshi Green Standard Army 京師綠營"  size = 11  cmd = char:358 }
		SE_qing_raise_garrison     = { prov = p:6659  name = "Liangjiang Green Standard 兩江綠營"    size = 7 }   # rich lower-Yangzi (Nanjing)
		SE_qing_raise_garrison     = { prov = p:4991  name = "Huguang Green Standard 湖廣綠營"       size = 7 }   # Wuchang
		SE_qing_raise_garrison_cmd = { prov = p:3537  name = "Sichuan Green Standard 四川綠營"       size = 7  cmd = char:339 } # logistical spine of the frontier; Wu Xiongguang
		SE_qing_raise_garrison     = { prov = p:9298  name = "Liangguang Green Standard 兩廣綠營"    size = 7 }   # Guangzhou, coast/anti-piracy + marine squadrons
		SE_qing_raise_garrison     = { prov = p:1303  name = "Shaan-Gan Green Standard 陝甘綠營"     size = 5 }   # Lanzhou, Muslim frontier
		SE_qing_raise_garrison     = { prov = p:3651  name = "Fujian Green Standard 福建綠營"        size = 5 }   # Fuzhou + Straits marine
		SE_qing_raise_garrison     = { prov = p:6781  name = "Taiwan Green Standard 臺灣綠營"        size = 4 }   # Tainan garrison
		SE_qing_raise_garrison_cmd = { prov = p:5786  name = "Yunnan-Guizhou Green Standard 雲貴綠營"  size = 5  cmd = char:241 } # SW tribal frontier; Yang Fang, White Lotus veteran

		set_global_variable = qing_armies_setup_done
		LOG_line = { sys = QING  msg = "Qing dual standing army raised: Eight Banners (metropolitan + provincial mancheng) + dispersed Green Standard" }
		}  # end 1815-OOB else [bookmark-1763 #304]
	}
	else = {
		LOG_fail = { sys = QING  fn = "SE_qing_armies"  reason = "c:CHI absent or has no capital_scope; no Qing army raised" }
	}
	LOG_exit = { sys = QING  fn = "SE_qing_armies"  result = OK }
}

########################################################################
# [#190-fix task #190] QING 1815 COASTAL WATER-FORCES (水師).
#
# THE BUG: the engine auto-generates every country's starting navy from
# STARTING_SHIPS_PER_PORT_POPULATION = 0.06 (common/defines/00_defines.txt:39).
# For China's huge coastal population that yields two ~85-ship modern fleets —
# grossly ahistorical: in 1815 the Qing had NO ocean-going battle-fleet, only
# provincial water-forces (水師) of shallow-draught war-junks for coastal defence,
# river/canal patrol and anti-piracy (the great pirate confederations had only
# just been suppressed, 1810). A blue-water steam/frigate navy did not arrive
# until the Self-Strengthening fleets (se_QING_SELFSTR.txt), which this mod
# already gates behind that mission tree.
#
# THE FIX: on game start, DISBAND every auto-generated CHI navy (destroy_unit,
# proven in Invictus 01_carthaginian_missions_08_naval.txt:152-157), then raise
# a modest historically-scaled junk squadron at the two principal maritime
# provincial commands — 廣東水師 (Guangdong, Canton estuary / anti-pirate) and
# 福建水師 (Fujian, Taiwan Strait). Modelled with `brig`, the only period-
# appropriate light warship key in this TC (screw_frigate/medium_steamer are the
# later modern hulls; the galley hulls are empty stubs — see qing_war_events.txt).
# Guarded on coastal ownership; each raise logs. Called once from
# oa_economy_setup.txt right after SE_qing_armies.
########################################################################
# ----------------------------------------------------------------------
# [#223-fix task #223] NAVY SPAWN — WHY ONLY ONE FLEET APPEARED, AND THE FIX.
#
# THE BUG (user boot-test): only the Fujian squadron ever spawned. Root cause
# (oracle-confirmed): multiple `create_unit navy=yes` calls issued SEQUENTIALLY in
# a single scope on the SAME on_game_initialized tick collide — only one navy
# survives. Every PROVEN-WORKING multi-fleet setup in this mod raises each fleet on
# a DISTINCT tick: the four Self-Strengthening fleets (se_QING_SELFSTR.txt) each
# spawn from a different mission's on_completion, never in one batch. is_coastal is
# NOT the culprit here — p:9298/p:3651/p:2893 all carry map_data/ports.csv entries,
# so they read coastal fine.
#
# THE FIX (final, #314 B22): the timing changes (#223 stagger, #242 one-tick) did NOT
# work — the user still saw only the Fujian fleet. The real culprit was the PLACEMENT
# idiom: the country-scope `create_unit { navy=yes location=p:X }` form silently spawned
# only one of the three fleets. Oracle consult (2026-07-10) found the reliable multi-navy
# idiom is the SAME province->owner->create_unit form that fixed the land garrisons, but
# with an ABSOLUTE PROVINCE / SAVED-SCOPE location token (NOT the relative `prev.prev`).
# `prev.prev` at the owner scope resolves to ROOT's capital (Beijing) for ALL three fleets;
# the engine cannot berth three navies at one inland capital, so only one survived (Fujian).
# Each squadron below now locates at its OWN coastal home port (p:9298 / p:3651 / p:2893, or
# its fallback scope) — the Invictus galatian_invasion.txt:718 absolute-token idiom, which
# applies identically to navies. All three squadron effects use it (main + fallback). They
# still raise together on one tick (.11) after the .10 disband; same-tick multi-navy is
# proven safe (Terra-Indomita me_jomon.txt:1779 spawns 12 fleets on one tick).
# ----------------------------------------------------------------------

# Disband the engine's ahistorical auto-generated blue-water fleets (seeded from
# STARTING_SHIPS_PER_PORT_POPULATION). destroy_unit is DESTRUCTIVE, so the global
# sentinel guarantees this runs exactly once. Called first, on its own tick, BEFORE
# the three squadron raises — so the water-forces below are the only 1815 Qing navy.
SE_qing_navy_disband = {
	LOG_enter = { sys = QING  fn = "SE_qing_navy_disband" }
	if = {
		limit = {
			exists = c:CHI
			NOT = { has_global_variable = qing_navy_setup_done }
		}
		set_global_variable = qing_navy_setup_done
		c:CHI = {
			if = {
				limit = { any_navy = { always = yes } }
				every_navy = { destroy_unit = yes }
				LOG_line = { sys = QING  msg = "auto-generated Qing fleets disbanded (ahistorical 1815 blue-water navy removed)" }
			}
		}
	}
	else = {
		LOG_fail = { sys = QING  fn = "SE_qing_navy_disband"  reason = "c:CHI absent or navy setup already done (sentinel set)" }
	}
	LOG_exit = { sys = QING  fn = "SE_qing_navy_disband"  result = OK }
}

# 廣東水師 Guangdong Water-Force — Canton estuary, the empire's busiest maritime
# frontier (Canton System trade + Pearl River anti-piracy). p:9298 is Guangzhou.
# Raised on its OWN tick (see #223-fix note above). Raise/fallback logic verbatim
# from the pre-fix code. Called in country scope (ROOT = c:CHI).
SE_qing_navy_guangdong = {
	LOG_enter = { sys = QING  fn = "SE_qing_navy_guangdong" }
	if = {
		limit = { exists = p:9298  p:9298 = { owner = c:CHI  is_coastal = yes } }
		# [#314 B22-fix v2] The reliable multi-navy idiom is province->owner->create_unit with
		# an ABSOLUTE location token (`location = p:9298`), NOT the relative `prev.prev`. At the
		# owner scope prev.prev = ROOT's capital (Beijing), so all three fleets berthed at the
		# same inland capital and the engine kept only one (Fujian) — the boot-test symptom.
		# The absolute-token land idiom (galatian_invasion.txt:718) applies identically here.
		c:CHI = {
			create_unit = {
				navy = yes
				name = "Guangdong Coastal Patrol 廣東水師"
				location = p:9298
				commander = char:597
				sub_unit = brig
				while = { count = 5  add_subunit = brig }
			}
		}
			char:597 = { set_variable = { name = qing_officer_marker  value = 1 } }  # [#331 user] serving-officer mark
		LOG_line = { sys = QING  msg = "Guangdong Coastal Patrol raised at Canton (p:9298), 6x war-junk" }
	}
	else = {
		# [#212-fix] fallback: raise at the largest owned coastal province, excluding
		# the other two water-forces' home ports (3651/2893).
		if = {
			limit = { any_owned_province = { is_coastal = yes  NOT = { province_id = 3651 }  NOT = { province_id = 2893 } } }
			ordered_owned_province = {
				limit = { is_coastal = yes  NOT = { province_id = 3651 }  NOT = { province_id = 2893 } }
				order_by = total_population
				max = 1
				save_scope_as = qing_gd_fallback_port
			}
			# [#314 B22-fix] province->owner->create_unit { location = <scope> } form (see main branch).
			c:CHI = {
				create_unit = {
					navy = yes
					name = "Guangdong Coastal Patrol 廣東水師"
					location = scope:qing_gd_fallback_port
					sub_unit = brig
					while = { count = 5  add_subunit = brig }
				}
			}
			LOG_line = { sys = QING  msg = "Guangdong Coastal Patrol raised at fallback coastal port (Canton p:9298 not coastal), 6x war-junk" }
		}
		else = {
			LOG_fail = { sys = QING  fn = "SE_qing_navy_guangdong"  reason = "p:9298 Canton not held/coastal and no fallback port; Guangdong Coastal Patrol not raised" }
		}
	}
	LOG_exit = { sys = QING  fn = "SE_qing_navy_guangdong"  result = OK }
}

# 福建水師 Fujian Water-Force — Taiwan Strait command (Fuzhou + Straits marine).
# p:3651 is Fuzhou. Raised on its OWN tick. Logic verbatim from the pre-fix code.
SE_qing_navy_fujian = {
	LOG_enter = { sys = QING  fn = "SE_qing_navy_fujian" }
	if = {
		limit = { exists = p:3651  p:3651 = { owner = c:CHI  is_coastal = yes } }
		# [#314 B22-fix] province->owner->create_unit { location = <p:X> } form (see Guangdong).
		c:CHI = {
			create_unit = {
				navy = yes
				name = "Fujian Coastal Patrol 福建水師"
				location = p:3651
				commander = char:596
				sub_unit = brig
				while = { count = 3  add_subunit = brig }
			}
		}
			char:596 = { set_variable = { name = qing_officer_marker  value = 1 } }  # [#331 user] serving-officer mark
		LOG_line = { sys = QING  msg = "Fujian Coastal Patrol raised at Fuzhou (p:3651), 4x war-junk" }
	}
	else = {
		if = {
			limit = { any_owned_province = { is_coastal = yes } }
			ordered_owned_province = {
				# [#193-fix task #193] exclude Canton (9298) and Ningbo (2893) so the Fujian
				# fallback never stacks a second squadron on the Guangdong/Zhejiang port.
				limit = { is_coastal = yes  NOT = { province_id = 9298 }  NOT = { province_id = 2893 } }
				order_by = total_population
				max = 1
				save_scope_as = qing_navy_fallback_port
			}
			# [#314 B22-fix] province->owner->create_unit { location = <p:X> } form (see Guangdong).
			c:CHI = {
				create_unit = {
					navy = yes
					name = "Fujian Coastal Patrol 福建水師"
					location = scope:qing_navy_fallback_port
					sub_unit = brig
					while = { count = 3  add_subunit = brig }
				}
			}
			LOG_line = { sys = QING  msg = "Fujian Coastal Patrol raised at fallback coastal port (Fuzhou not coastal), 4x war-junk" }
		}
		else = {
			LOG_fail = { sys = QING  fn = "SE_qing_navy_fujian"  reason = "no coastal province owned; Fujian Coastal Patrol not raised" }
		}
	}
	LOG_exit = { sys = QING  fn = "SE_qing_navy_fujian"  result = OK }
}

# [#191 task #191] 浙江水師 Zhejiang Water-Force — the Ningbo / Zhenhai (鎮海) command
# guarding the Zhejiang coast and the Yangzi approaches, the third principal
# provincial water-force (清史稿·兵志). p:2893 is Ningbo. Raised on its OWN tick.
# [#223-fix] added a coastal fallback (previously NONE — a second silent-drop path).
SE_qing_navy_zhejiang = {
	LOG_enter = { sys = QING  fn = "SE_qing_navy_zhejiang" }
	if = {
		limit = { exists = p:2893  p:2893 = { owner = c:CHI  is_coastal = yes } }
		# [#314 B22-fix] province->owner->create_unit { location = <p:X> } form (see Guangdong).
		c:CHI = {
			create_unit = {
				navy = yes
				name = "Zhejiang Coastal Patrol 浙江水師"
				location = p:2893
				sub_unit = brig
				while = { count = 2  add_subunit = brig }
			}
		}
		LOG_line = { sys = QING  msg = "Zhejiang Coastal Patrol raised at Ningbo (p:2893), 3x war-junk" }
	}
	else = {
		# [#223-fix] fallback: largest owned coastal province excluding the other two
		# home ports (9298/3651), so Zhejiang still gets a squadron if Ningbo is not
		# held/coastal — matching the Guangdong/Fujian fallback idiom.
		if = {
			limit = { any_owned_province = { is_coastal = yes  NOT = { province_id = 9298 }  NOT = { province_id = 3651 } } }
			ordered_owned_province = {
				limit = { is_coastal = yes  NOT = { province_id = 9298 }  NOT = { province_id = 3651 } }
				order_by = total_population
				max = 1
				save_scope_as = qing_zj_fallback_port
			}
			# [#314 B22-fix] province->owner->create_unit { location = <p:X> } form (see Guangdong).
			c:CHI = {
				create_unit = {
					navy = yes
					name = "Zhejiang Coastal Patrol 浙江水師"
					location = scope:qing_zj_fallback_port
					sub_unit = brig
					while = { count = 2  add_subunit = brig }
				}
			}
			LOG_line = { sys = QING  msg = "Zhejiang Coastal Patrol raised at fallback coastal port (Ningbo p:2893 not coastal), 3x war-junk" }
		}
		else = {
			LOG_fail = { sys = QING  fn = "SE_qing_navy_zhejiang"  reason = "p:2893 Ningbo not held/coastal and no fallback port; Zhejiang Coastal Patrol not raised" }
		}
	}
	LOG_exit = { sys = QING  fn = "SE_qing_navy_zhejiang"  result = OK }
}

SE_occupation_of_france = {
	LOG_enter = { sys = DIPLO  fn = "SE_occupation_of_france" }
	if = {
		limit = { exists = c:FRA }
		if = {
			limit = { exists = c:GBR }
			c:GBR = {
				add_military_access = c:FRA
				add_truce = {
					target = c:FRA
					duration = 3650 #10 years
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Div. I, British Occupation Force"
							location = p:3768 #Cambrai
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:88 = { add_to_legion = PREV } #Wellington
						random_legion_unit = { set_as_commander = char:88 }
					}
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Div. II, British Occupation Force"
							location = p:8782 #Calais
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:494 = { add_to_legion = PREV } #Henry Clinton
						random_legion_unit = { set_as_commander = char:494 }
					}
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Div. III, British Occupation Force"
							location = p:3451 #Valenciennes
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:496 = { add_to_legion = PREV } #Charles Colville
						random_legion_unit = { set_as_commander = char:496 }
					}
				}
			}
			LOG_line = { sys = DIPLO  msg = "GBR army of occupation deployed in France" }
		}
		if = {
			limit = { exists = c:AUS }
			c:AUS = {
				add_military_access = c:FRA
				add_truce = {
					target = c:FRA
					duration = 3650 #10 years
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Austrian Occupation Force"
							location = p:3887 #Colmar
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:493 = { add_to_legion = PREV } #Johann Frimont
						random_legion_unit = { set_as_commander = char:493 }
					}
				}
			}
			LOG_line = { sys = DIPLO  msg = "AUS army of occupation deployed in France" }
		}
		if = {
			limit = { exists = c:PRU }
			c:PRU = {
				add_military_access = c:FRA
				add_truce = {
					target = c:FRA
					duration = 3650 #10 years
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Prussian Occupation Force"
							location = p:5025 #Sedan
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:293 = { add_to_legion = PREV } #Hans von Zieten
						random_legion_unit = { set_as_commander = char:293 }
					}
				}
			}
			LOG_line = { sys = DIPLO  msg = "PRU army of occupation deployed in France" }
		}
		if = {
			limit = { exists = c:RUS }
			c:RUS = {
				add_military_access = c:FRA
				add_truce = {
					target = c:FRA
					duration = 3650 #10 years
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Russian Occupation Force"
							location = p:1714 #Vervins
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:289 = { add_to_legion = PREV } #Mikhail Vorontsov
						random_legion_unit = { set_as_commander = char:289 }
					}
				}
			}
			LOG_line = { sys = DIPLO  msg = "RUS army of occupation deployed in France" }
		}
		if = {
			limit = { exists = c:BAV }
			c:BAV = {
				add_military_access = c:FRA
				add_truce = {
					target = c:FRA
					duration = 3650 #10 years
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Bavarian Occupation Force"
							location = p:8839 #Nogent
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:118 = { add_to_legion = PREV } #Gourlez de Lamotte
						random_legion_unit = { set_as_commander = char:118 }
					}
				}
			}
			LOG_line = { sys = DIPLO  msg = "BAV army of occupation deployed in France" }
		}
		if = {
			limit = { exists = c:SWI }
			c:SWI = {
				add_military_access = c:FRA
				add_truce = {
					target = c:FRA
					duration = 3650 #10 years
				}
				capital_scope.governorship = {
					raise_legion = {
						create_unit = {
							name = "Swiss Occupation Force"
							location = p:3495 #Lons-le-Saunier
							sub_unit = regular_infantry
							save_scope_as = legion_scope
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
							add_subunit = regular_infantry
						}
						char:119 = { add_to_legion = PREV } #Niklaus von Bachmann
						random_legion_unit = { set_as_commander = char:119 }
					}
				}
			}
			LOG_line = { sys = DIPLO  msg = "SWI army of occupation deployed in France" }
		}
	}
	else = {
		LOG_fail = { sys = DIPLO  fn = "SE_occupation_of_france"  reason = "c:FRA absent; no army of occupation deployed" }
	}
	LOG_exit = { sys = DIPLO  fn = "SE_occupation_of_france"  result = OK }
}
