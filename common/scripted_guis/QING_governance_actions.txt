########################################################################
# QING_governance_actions.txt — player-facing scripted GUIs for the Qing
# GOVERNANCE suite: the Grand Council (軍機處) hub and the character-held great
# offices. Companion to QING_mechanics_actions.txt.
#
# Two shapes:
#   * COUNTRY-scope verbs (convene / dissolve the council) — same pattern as
#     QING_mechanics_actions.txt: scope = country, gated CHI + ai_is_valid no.
#   * CHARACTER-scope verbs (appoint / vacate a great office — a Grand Council
#     seat IS a great office under the council-is-offices model, #103) — invoked
#     on a courtier from the character view; scope = character,
#     with saved_scopes = { player } so is_shown/is_valid can gate on the country.
#     The underlying effects in se_QING_COUNCIL.txt self-scope to the country via
#     `employer`, so these GUIs call them directly in the character scope (this =
#     the courtier) — no re-scoping needed.
#
# All gated CHI-only + ai_is_valid = { always = no }.
# Prefix = qing_gov_
########################################################################

# [BT-officeholder 2026-07-22] REMOVED qing_gov_council_convene (the on-demand "refill the whole
# Grand Council" button). Blanket re-autofill re-grabbed/double-booked characters. Autofill runs
# ONCE at game start; thereafter offices are filled individually (single-seat refill on vacate,
# or the player's per-office picker).

# ======================================================================
# CHARACTER-SCOPE: appoint this courtier as GRAND CHANCELLOR (領班軍機大臣).
# Under the council-is-offices model (#103) the Grand Chancellor is simply
# another great office; there is no separate "seat" or "name chief" step. This
# mirrors the ten domain-office appoint verbs below (shared QING_office_appoint).
# this = the appointee; the effect reaches CHI via `employer`. Appointing over a
# sitting chancellor auto-vacates them (handled inside QING_office_appoint).
# ======================================================================
qing_gov_office_appoint_chancellor = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:chancellor } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 20 } }
	}
	effect = {
		scope:player = { add_political_influence = -20 }
		QING_office_appoint = { office = chancellor }
	}
}

# ======================================================================
# CHARACTER-SCOPE: appoint this courtier to a great office.
# The engine has no runtime GUI parameters, so there is one appoint verb per
# office; each calls the shared QING_office_appoint with its office literal.
# this = the appointee; the effect reaches CHI via `employer`. Appointing over a
# sitting holder auto-vacates them (handled inside QING_office_appoint).
# Offices: personnel 吏部 / revenue 戶部 / rites 禮部 / war 兵部 / justice 刑部
#          works 工部 / censor 都察院 / lifanyuan 理藩院 / chamberlain 內務府 / zongli 總理衙門
# ======================================================================
qing_gov_office_appoint_personnel = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:personnel } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = personnel }
	}
}

qing_gov_office_appoint_revenue = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:revenue } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = revenue }
	}
}

qing_gov_office_appoint_rites = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:rites } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = rites }
	}
}

qing_gov_office_appoint_war = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:war } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = war }
	}
}

qing_gov_office_appoint_justice = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:justice } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = justice }
	}
}

qing_gov_office_appoint_works = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:works } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = works }
	}
}

qing_gov_office_appoint_censor = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:censor } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = censor }
	}
}

qing_gov_office_appoint_lifanyuan = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:lifanyuan } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = lifanyuan }
	}
}

qing_gov_office_appoint_chamberlain = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:chamberlain } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = chamberlain }
	}
}

qing_gov_office_appoint_zongli = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:zongli } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = zongli }
	}
}

# [#273 develop] NEW office: Central Secretariat 內閣大學士 (skill = charisma).
qing_gov_office_appoint_grand_secretariat = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:grand_secretariat } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = grand_secretariat }
	}
}

# [#273 develop] NEW office: Imperial Guard 領侍衛內大臣 (skill = martial).
qing_gov_office_appoint_guard_commandant = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = { scope:player = { tag = CHI }  employer = scope:player }
	is_valid = {
		custom_tooltip = {
			text = qing_gov_office_valid_tt
			is_adult = yes
			is_alive = yes
			NOT = { AND = { has_variable = qing_office_held  var:qing_office_held = flag:guard_commandant } }
		}
		custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 15 } }
	}
	effect = {
		scope:player = { add_political_influence = -15 }
		QING_office_appoint = { office = guard_commandant }
	}
}

# ======================================================================
# CHARACTER-SCOPE: relieve this courtier of whatever great office they hold.
# One verb for all ten offices via the QING_office_vacate_dispatch effect.
# ======================================================================
qing_gov_office_vacate = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = {
		scope:player = { tag = CHI }
		has_variable = qing_office_held
	}
	is_valid = { always = yes }
	effect = {
		# [BT-appointment-redesign 2026-07-22] MANUAL Dismiss → NO auto-backfill (the no-backfill
		# dispatcher twin). Per the user's rule, only DEATH/DEPARTURE of an office-holder auto-creates
		# a replacement; a player-clicked dismissal leaves the seat EMPTY for the player to re-staff by
		# hand (matching the "clicking Appoint/Dismiss creates no characters" principle). The dispatcher
		# self-scopes to the country via `employer`; this = the holder.
		QING_office_vacate_dispatch_nobackfill = yes
	}
}

# ======================================================================
# CHARACTER-SCOPE: dismiss a serving foreign advisor (洋員). Frees the post and
# cools the home power's stake. One verb for all fields via the dispatcher.
# this = the advisor; the effect self-scopes to CHI via ROOT.
# ======================================================================
qing_gov_advisor_dismiss = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = {
		scope:player = { tag = CHI }
		has_variable = qing_is_foreign_advisor
	}
	is_valid = { always = yes }
	effect = {
		QING_advisor_dismiss = yes
	}
}

# ======================================================================
# [grand-council-tab] COUNTRY-SCOPE: rebuild the Grand Council view's courtier
# candidate cache on demand. The tab's category button calls this onclick so the
# appoint/seat pickers show a current top-N of eligible courtiers the moment the
# tab is opened (the quarterly QING_GOV_pulse also refreshes it). No cost / no
# side effects beyond the cache; always valid so the click never no-ops.
# ======================================================================
qing_gov_council_refresh_candidates = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = {
		QING_council_refresh_candidates = yes
	}
}

# ======================================================================
# [#221] PER-OFFICE candidate-cache refreshers. One country-scope verb per
# ranking key: the four single governing skills (used by the ten domain-office
# pickers) plus the sum-of-four svalue (used by the summit Grand Chancellor
# picker). Each office's Appoint/Replace button in the government view fires the
# refresher matching THAT office's governing skill, so the candidate list is
# sorted for the post being filled before the picker window opens. No cost / no
# side effects beyond the cache; always valid so the click never no-ops.
# Office→skill map (se_QING_COUNCIL.txt QING_council_recompute):
#   personnel/revenue/works/censor = finesse ; war = martial ;
#   rites/justice = zeal ; lifanyuan/chamberlain/zongli = charisma ;
#   chancellor = sum-of-four (combined_stats_council_svalue).
# ======================================================================
qing_gov_refresh_candidates_martial = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { QING_council_refresh_candidates_by = { sortval = council_sort_martial } }
}
qing_gov_refresh_candidates_finesse = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { QING_council_refresh_candidates_by = { sortval = council_sort_finesse } }
}
qing_gov_refresh_candidates_charisma = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { QING_council_refresh_candidates_by = { sortval = council_sort_charisma } }
}
qing_gov_refresh_candidates_zeal = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { QING_council_refresh_candidates_by = { sortval = council_sort_zeal } }
}
qing_gov_refresh_candidates_summit = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { QING_council_refresh_candidates_by = { sortval = combined_stats_council_svalue } }
}

# ======================================================================
# [#314 B25] EMPEROR SECLUSION — voluntarily withdraw into 退居休養 and open a
# Grand Regency. Shown only to a CAPABLE, ADULT, non-aged emperor who is not
# already secluded and for whom a regency is not otherwise warranted (a child /
# incapable / aged emperor is handled by the automatic regency, so seclusion adds
# nothing there). Costs legitimacy (handled in the effect).
# ======================================================================
qing_action_emperor_seclusion = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = {
		tag = CHI
		NOT = { has_variable = qing_emperor_secluded }
		exists = current_ruler
		# only a capable ruler chooses this — otherwise the auto-regency already covers it
		current_ruler = { is_adult = yes  NOT = { has_trait = incapable }  age < 70 }
	}
	is_valid = { always = yes }
	effect = { QING_emperor_enter_seclusion = yes }
}

# ======================================================================
# [#314 B25] RESUME PERSONAL RULE — end a voluntary seclusion. Shown only while
# the emperor is secluded. The regency then lapses (unless another warrant holds).
# ======================================================================
qing_action_emperor_resume_rule = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = {
		tag = CHI
		has_variable = qing_emperor_secluded
	}
	is_valid = { always = yes }
	effect = { QING_emperor_resume_rule = yes }
}

# ======================================================================
# [BT-4 fix] COUNTRY-SCOPE: trivial helper GUIs to set the picker office as a
# COUNTRY variable (not just a GUI var) so script can read it. One per office;
# each is invoked at the picker open-site in government_view.gui to stamp the
# flag before the refresh runs + before the window opens. No cost / no side effects.
# ======================================================================
qing_gc_set_picker_office_chancellor = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:chancellor } }
}
qing_gc_set_picker_office_personnel = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:personnel } }
}
qing_gc_set_picker_office_revenue = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:revenue } }
}
qing_gc_set_picker_office_rites = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:rites } }
}
qing_gc_set_picker_office_war = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:war } }
}
qing_gc_set_picker_office_justice = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:justice } }
}
qing_gc_set_picker_office_works = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:works } }
}
qing_gc_set_picker_office_censor = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:censor } }
}
qing_gc_set_picker_office_lifanyuan = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:lifanyuan } }
}
qing_gc_set_picker_office_chamberlain = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:chamberlain } }
}
qing_gc_set_picker_office_zongli = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:zongli } }
}
qing_gc_set_picker_office_grand_secretariat = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:grand_secretariat } }
}
qing_gc_set_picker_office_guard_commandant = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:guard_commandant } }
}

# [T1-A review-fix] The THREE MINISTRY CORPS (Censorate inspectors 御史, Imperial Guardsmen 侍衛,
# Zongli diplomats 駐外使臣) share the SAME candidate-picker window as the Grand Council offices, but
# they are NOT great offices — they are capped rank-and-file corps with their own stamp flags. Their
# panel buttons formerly set ONLY the GUI var qing_gc_picker_office (never the SCRIPT var the row-
# click handler dispatches off), so the shared handler qing_gov_office_appoint_selected saw no
# selected office and the click was dead (or, worse, dispatched off a STALE council flag left from an
# earlier appoint — mis-seating the candidate on a real office + spending PI). These bridges stamp
# the corps token into the same qing_gc_picker_office_var the handler reads; the handler now branches
# on them to run the corps enrolment (no PI, cap <6) instead of an office appointment.
qing_gc_set_picker_office_censor_inspector = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:censor_inspector } }
}
qing_gc_set_picker_office_imperial_guardsman = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:imperial_guardsman } }
}
qing_gc_set_picker_office_zongli_diplomat = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = { tag = CHI }
	is_valid = { always = yes }
	effect = { set_variable = { name = qing_gc_picker_office_var  value = flag:zongli_diplomat } }
}

# ======================================================================
# [BT-4 fix] CHARACTER-SCOPE: generic appoint verb that reads the selected office
# from qing_gc_picker_office_var and dispatches to the correct QING_office_appoint
# call. This replaces the 13 individual appoint buttons with ONE row-click handler.
# this = the appointee; scope:player = CHI.
# ======================================================================
qing_gov_office_appoint_selected = {
	scope = character
	saved_scopes = { player }
	ai_is_valid = { always = no }
	is_shown = {
		scope:player = { tag = CHI  has_variable = qing_gc_picker_office_var }
		employer = scope:player
	}
	is_valid = {
		is_adult = yes
		is_alive = yes
		# [#8 fix 2026-07-18] backstop: never appoint a harem consort (後宮妃嬪) to any office or corps.
		# The candidate builders (QING_council_refresh_candidates[_by]) already exclude consorts so one
		# should never reach a picker row, but the row-click handler is authoritative — gate it here too.
		NOT = { has_variable = qing_is_harem_consort }
		# [T1-A review-fix] The picker window is SHARED between the 13 Grand Council great offices and
		# the three capped ministry CORPS (censor inspectors 御史 / imperial guardsmen 侍衛 / Zongli
		# diplomats 駐外使臣). A corps enrolment is NOT an office appointment: it costs no PI, admits a
		# man who already sits elsewhere is barred only by the corps' own rules, and is capped at 6.
		# Branch the validity on which the picker selected.
		trigger_if = {
			limit = {
			OR = {
				scope:player.var:qing_gc_picker_office_var = flag:censor_inspector
				scope:player.var:qing_gc_picker_office_var = flag:imperial_guardsman
				scope:player.var:qing_gc_picker_office_var = flag:zongli_diplomat
			}
			}
			custom_tooltip = {
				text = qing_gov_corps_valid_tt
				# a great-office holder cannot also serve rank-and-file (mirrors the corps effects).
				NOT = { has_variable = qing_office_held }
				# [#89 2026-08-08] STRICT 1:1 — this shared picker enrols a man into a corps by STAMPING its
				# marker with no relief of a prior post, so it must refuse anyone already holding ANY court
				# post (amban / Hoppo / the OTHER corps / either study or its chief / palace eunuch / march
				# Protector-General), not just a great-office holder. The canonical trigger enumerates every
				# post marker in one place; the per-corps "not already in THIS corps" check below still
				# applies (redundant but harmless with the broad gate). Matches the dedicated panel verbs.
				NOT = { QING_char_holds_court_position = yes }
				is_general = no
				is_admiral = no
				is_governor = no
				NOT = { has_variable = qing_officer_marker }
				# not already in THIS corps, and the corps is not full (<6) — matches the dedicated
				# qing_censorate_appoint_inspector / qing_guard_enrol_guardsman / qing_zongli_appoint_diplomat.
				trigger_if = {
					limit = { scope:player.var:qing_gc_picker_office_var = flag:censor_inspector }
					NOT = { has_variable = qing_is_censor_inspector }
					scope:player = { OR = { NOT = { has_variable = qing_censor_inspector_count }  var:qing_censor_inspector_count < 6 } }
				}
				trigger_else_if = {
					limit = { scope:player.var:qing_gc_picker_office_var = flag:imperial_guardsman }
					NOT = { has_variable = qing_is_imperial_guardsman }
					scope:player = { OR = { NOT = { has_variable = qing_guard_corps_count }  var:qing_guard_corps_count < 6 } }
				}
				trigger_else = {
					NOT = { has_variable = qing_zongli_diplomat }
					scope:player = { OR = { NOT = { has_variable = qing_zongli_diplomat_count }  var:qing_zongli_diplomat_count < 6 } }
				}
			}
			custom_tooltip = {
				text = qing_gc_appoint_requires_sex_eligibility_tt
				# [task #10 fix 2, 2026-08-27] SEX-ELIGIBILITY click-time backstop for the 3 CORPS
				# sub-posts (censor_inspector / imperial_guardsman / zongli_diplomat). The great-office
				# copy below (trigger_else) never runs for these three flags -- they resolve inside THIS
				# trigger_if branch and returned unconditionally true before this fix. All three are
				# LOWER tier per QING_char_gc_office_sex_eligible (qing_dynasty_triggers.txt) -- requires
				# at least Limited Legal Rights. Imperial Clan exemption inlined the SAME way as the
				# great-office copy: is_close_relative's own ROOT/employer usage does not resolve
				# correctly in this GUI's character scope (scope:player is the saved CHI scope).
				OR = {
					is_female = no
					is_close_relative = scope:player.current_ruler
					scope:player = {
						OR = {
							has_law = women_limited_legal_rights
							has_law = women_equal_legal_rights
							has_law = womens_suffrage_law
						}
					}
				}
			}
		}
		# ---- OFFICE tokens (the 13 Grand Council great offices): the existing council rules.
		trigger_else = {
			custom_tooltip = {
				text = qing_gov_office_valid_tt
				# [BT-4] Don't re-appoint to the same seat the candidate holds.
				NOT = {
					AND = {
						has_variable = qing_office_held
						var:qing_office_held = scope:player.var:qing_gc_picker_office_var
					}
				}
			}
			# [BT-50 / #396] the candidate must hold no conflicting role. Mirrors the shared
			# QING_office_eligible_candidate trigger, but inlined because this is CHARACTER scope
			# (scope:player = CHI, not ROOT): a serving commander / governor / vanilla office-holder,
			# the ruler, or the heir cannot be seated on the Grand Council. This is the picker face of
			# the reported symptom (ministers who were also commanders).
			custom_tooltip = {
				text = qing_gov_office_not_conflicting_tt
				is_ruler = no
				is_general = no
				is_admiral = no
				is_governor = no
				NOT = { AND = { exists = scope:player.current_ruler  this = scope:player.current_ruler } }
				NOT = { AND = { exists = scope:player.primary_heir   this = scope:player.primary_heir } }
				NOT = { has_office = office_foreign_minister }
				NOT = { has_office = office_royal_tutor }
				NOT = { has_office = office_marshal }
				NOT = { has_office = office_master_of_the_guard }
				NOT = { has_office = office_high_priest_monarchy }
				NOT = { has_office = office_philosopher }
				NOT = { has_office = office_steward }
				NOT = { has_office = office_physician }
			}
			# Chancellor costs 20 PI, all others 15 — guard against the higher bound.
			custom_tooltip = { text = qing_gov_council_needs_pi_tt  scope:player = { political_influence >= 20 } }
			# [#117 2026-08-12] EXAM-DEGREE ELIGIBILITY for civil offices. Same OR-combined gate as
			# QING_council_refresh_candidates_by (se_QING_COUNCIL.txt) — the martial offices are
			# exempted via the OR, not a bare AND-sibling NOT-clause (which would AND-collapse to
			# unconditional-false for a martial target and block every candidate). Wrapped in
			# custom_tooltip like every sibling clause in this block, so a blocked civil-office click
			# in the acknowledged stale-refresh window (copy #1's list can transiently show a
			# degreeless man) still shows the player WHY, instead of a silent dead click.
			custom_tooltip = {
				text = qing_gc_appoint_requires_degree_tt
				OR = {
					scope:player.var:qing_gc_picker_office_var = flag:war
					scope:player.var:qing_gc_picker_office_var = flag:guard_commandant
					QING_office_required_degree_civil = yes
				}
			}
			# [task #10 fix, 2026-08-26] SEX-ELIGIBILITY click-time backstop, mirroring the degree
			# backstop immediately above. QING_council_refresh_candidates_by's live picker-list filter
			# (QING_char_gc_office_sex_eligible, se_QING_COUNCIL.txt:1552) already excludes ineligible
			# women from the normal candidate list, but this row-click handler is authoritative and can
			# see a candidate from the OLDER, unfiltered QING_council_refresh_candidates pulse list
			# during the same acknowledged stale-refresh window the degree backstop guards against — so
			# it needs its own copy, inlined in this block's scope:player idiom (that trigger's ROOT/
			# employer links resolve differently here and cannot be called directly). Tier mapping and
			# Imperial Clan exemption match QING_char_gc_office_sex_eligible exactly
			# (qing_dynasty_triggers.txt) — keep the two in sync if the office split or law names change.
			custom_tooltip = {
				text = qing_gc_appoint_requires_sex_eligibility_tt
				OR = {
					is_female = no
					is_close_relative = scope:player.current_ruler
					NOT = { scope:player = { has_variable = qing_gc_picker_office_var } }
					AND = {
						OR = {
							scope:player.var:qing_gc_picker_office_var = flag:censor
							scope:player.var:qing_gc_picker_office_var = flag:lifanyuan
							scope:player.var:qing_gc_picker_office_var = flag:chamberlain
							scope:player.var:qing_gc_picker_office_var = flag:zongli
							scope:player.var:qing_gc_picker_office_var = flag:grand_secretariat
							scope:player.var:qing_gc_picker_office_var = flag:guard_commandant
							scope:player.var:qing_gc_picker_office_var = flag:censor_inspector
							scope:player.var:qing_gc_picker_office_var = flag:imperial_guardsman
							scope:player.var:qing_gc_picker_office_var = flag:zongli_diplomat
						}
						scope:player = {
							OR = {
								has_law = women_limited_legal_rights
								has_law = women_equal_legal_rights
								has_law = womens_suffrage_law
							}
						}
					}
					AND = {
						OR = {
							scope:player.var:qing_gc_picker_office_var = flag:personnel
							scope:player.var:qing_gc_picker_office_var = flag:revenue
							scope:player.var:qing_gc_picker_office_var = flag:rites
							scope:player.var:qing_gc_picker_office_var = flag:war
							scope:player.var:qing_gc_picker_office_var = flag:justice
							scope:player.var:qing_gc_picker_office_var = flag:works
						}
						scope:player = {
							OR = {
								has_law = women_equal_legal_rights
								has_law = womens_suffrage_law
							}
						}
					}
					AND = {
						scope:player.var:qing_gc_picker_office_var = flag:chancellor
						scope:player = { has_law = womens_suffrage_law }
					}
				}
			}
		}
	}
	effect = {
		scope:player = {
			# [T1-A review-fix] CORPS enrolment (no PI): stamp the corps flag on the appointee (prev)
			# and recompute the ministry so the panel roster + count gate refresh immediately — exactly
			# what the dedicated qing_*_appoint_* GUIs do, routed here through the shared picker.
			if = {
				limit = { var:qing_gc_picker_office_var = flag:censor_inspector }
				prev = {
					set_variable = { name = qing_is_censor_inspector  value = 1 }
					QING_post_stamp = { post = censor_inspector }   # [#118]
					# [fix, task #101-follow-up 2026-08-16] player-appoint path -- must pay the same
					# wage the minted path gets.
					add_character_modifier = { name = qing_censor_inspector_office  duration = -1 }
				}
				QING_ministry_recompute_perf_censor = yes
				LOG_line = { sys = QING  msg = "corps: censor inspector commissioned via row-click picker" }
			}
			else_if = {
				limit = { var:qing_gc_picker_office_var = flag:imperial_guardsman }
				prev = {
					set_variable = { name = qing_is_imperial_guardsman  value = 1 }
					QING_post_stamp = { post = imperial_guardsman }   # [#118]
					# [fix, task #101-follow-up 2026-08-16] player-appoint path -- must pay the same
					# wage the minted path gets.
					add_character_modifier = { name = qing_imperial_guardsman_office  duration = -1 }
				}
				QING_ministry_recompute_perf_guard_commandant = yes
				LOG_line = { sys = QING  msg = "corps: imperial guardsman enrolled via row-click picker" }
			}
			else_if = {
				limit = { var:qing_gc_picker_office_var = flag:zongli_diplomat }
				prev = {
					set_variable = { name = qing_zongli_diplomat  value = 1 }
					QING_post_stamp = { post = zongli_diplomat }   # [#118]
					# [fix, task #101-follow-up 2026-08-16] player-appoint path -- must pay the same
					# wage the minted path gets.
					add_character_modifier = { name = qing_zongli_diplomat_office  duration = -1 }
				}
				QING_ministry_recompute_perf_zongli = yes
				LOG_line = { sys = QING  msg = "corps: zongli diplomat appointed via row-click picker" }
			}
			# [fix task #6 2026-08-29] Appoint (filling a currently VACANT seat) is now free; Replace
			# (the target office already has a living holder, so this click evicts him) still costs PI
			# (chancellor 20, others 15). Previously this charged the flat cost unconditionally regardless
			# of vacancy, so filling an empty seat cost the same PI as reshuffling a sitting minister.
			# Folded the vacancy check (has_variable = qing_office_$office$_holder, set/cleared by
			# QING_office_appoint / QING_office_vacate -- se_QING_COUNCIL.txt) into the existing 13-way
			# dispatch rather than adding a second parallel 13-way chain -- the check MUST run before
			# prev = { QING_office_appoint } below, since that call is what sets the holder var to the
			# new appointee.
			else = {
				if = {
					limit = { var:qing_gc_picker_office_var = flag:chancellor }
					if = { limit = { has_variable = qing_office_chancellor_holder }  add_political_influence = -20 }
					prev = { QING_office_appoint = { office = chancellor } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:personnel }
					if = { limit = { has_variable = qing_office_personnel_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = personnel } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:revenue }
					if = { limit = { has_variable = qing_office_revenue_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = revenue } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:rites }
					if = { limit = { has_variable = qing_office_rites_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = rites } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:war }
					if = { limit = { has_variable = qing_office_war_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = war } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:justice }
					if = { limit = { has_variable = qing_office_justice_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = justice } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:works }
					if = { limit = { has_variable = qing_office_works_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = works } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:censor }
					if = { limit = { has_variable = qing_office_censor_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = censor } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:lifanyuan }
					if = { limit = { has_variable = qing_office_lifanyuan_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = lifanyuan } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:chamberlain }
					if = { limit = { has_variable = qing_office_chamberlain_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = chamberlain } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:zongli }
					if = { limit = { has_variable = qing_office_zongli_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = zongli } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:grand_secretariat }
					if = { limit = { has_variable = qing_office_grand_secretariat_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = grand_secretariat } }
				}
				else_if = {
					limit = { var:qing_gc_picker_office_var = flag:guard_commandant }
					if = { limit = { has_variable = qing_office_guard_commandant_holder }  add_political_influence = -15 }
					prev = { QING_office_appoint = { office = guard_commandant } }
				}
				LOG_line = { sys = QING  msg = "office appointed via row-click picker" }
			}
		}
	}
}

# ======================================================================
# [BT-49] RULER-IS-SINOSPHERE gate — true when the player's ruler is of a Han
# (chinese_group) or Manchu culture, i.e. the courts that title their heir 皇太子
# (Taizi). Mirrors the Huangdi ruler-title logic (imperial_monarchy_ruler_manchu /
# _chinese_group in mod_governments): the heir label follows the ruling culture,
# so only these courts see "Taizi" — every other monarchy keeps vanilla "Primary
# Heir". Fed to SelectLocalization on the NEXT_RULER_TEXT sub-header in
# government_view.gui. Country scope (ROOT = the player). Read-only; AI never evaluates.
# ======================================================================
qing_ruler_is_sinosphere = {
	scope = country
	ai_is_valid = { always = no }
	is_shown = {
		exists = current_ruler
		current_ruler = {
			OR = {
				# [BT-49b] a CHARACTER's culture is tested with has_culture / has_culture_group, NOT the
				# bare `culture = X` (which silently never matched — the Qing emperor is has_culture=manchu,
				# jurchen group, so the label never flipped to Taizi). Manchu is in the jurchen group; Han
				# rulers are chinese_group. Proven char triggers (00_culture_subgroups.txt; Invictus).
				has_culture_group = chinese_group
				has_culture = manchu
			}
		}
	}
}
