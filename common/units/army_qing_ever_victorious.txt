qing_ever_victorious = {
    army = yes
	assault = yes
	support = yes

	# [#84/EVA] 常勝軍 EVER-VICTORIOUS ARMY — the Western-drilled, Western-officered modern
	# corps raised at Shanghai (Ward, then Gordon) against the Taiping: the seed of a modern
	# centrally-commanded Qing army. Rifle-armed, disciplined and artillery-supported — the
	# strongest Qing foot once the self-strengthening era opens, but a late, expensive,
	# small professional force (not a line-army replacement).
	#
	# Gated inside the unit `allow` (no enable_unit_type verb — army_artillery.txt:9).
	# [logfix] unit allow evaluates in STATE scope, so country triggers hop via owner={}
	# (the proven riflemen/production-building idiom). Requires the modern rifle arm — the EVA
	# is meaningless before rifling — matching the riflemen tech gate, plus the Chinese-culture
	# gate. The Self-Strengthening mission (QING_selfstr_found_evarmy) unlock_invention =
	# tech_rifles-adjacent context makes this available in the movement's era.
	allow = {
		owner = {
			OR = {
				country_culture_group = jurchen        # the Qing: Manchu primary culture is in the jurchen group, NOT chinese_group
				country_culture_group = chinese_group   # any Han-Chinese successor state
			}
			invention = tech_rifles
		}
	}

	maneuver = 4
	movement_speed = 2.7
	build_time = 65

	# Combat profile: the strongest Qing foot of the modern era — rifle-armed line that
	# outfights banner/green-standard and beats cavalry with disciplined volley fire, but
	# few and costly (an elite professional corps).
	regular_infantry = 1.55
	conscripts = 1.7
	light_infantry = 1.35
	heavy_infantry = 1.3
	heavy_cavalry = 1.15
	light_cavalry = 1.25
	archers = 1.4

	attrition_weight = 0.7
	attrition_loss = 0.013

	food_consumption = 0.22
	food_storage = 2.9

	morale_damage_taken = 0.85
	build_cost = {
		gold = 22
		manpower = 1
	}

	# Elite professional corps: hard-cap the AI fraction (a modern spearhead, not the army).
	ai_max_percentage = 10
}
