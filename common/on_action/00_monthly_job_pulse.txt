monthly_job_pulse =
{
	# Engine-invoked monthly (From Code: yes), "none" scope — no root country/
	# province, so any real logic must enter scopes explicitly via every_country
	# etc. NOTE: its sibling monthly_administration_pulse was disabled in
	# oa_wealth_changes.txt for "unacceptable monthly slowdowns", so heavy
	# per-governorship employment work does NOT belong here — that stays on the
	# quarterly cadence (see ECON_BUILD.md §2). For now this only emits a trace
	# marker so a -debug_mode session confirms the hook fires and how often,
	# before any load is added. Zero cost outside -debug_mode.
	effect =
	{
		ECON_LOG_phase_enter = { stage = monthly_job_pulse }
	}
	random_events =
	{
		500 = 0
		#1 = office_gain.3
	}
}
