########################################################################
# qing_test_prims_on_actions.txt — TEMPORARY wiring for the create_country /
# change_country_tag viability probe (se_QING_TEST_PRIMS.txt), task #39 follow-up.
#
# *** TEMPORARY — REMOVE (this file + se_QING_TEST_PRIMS.txt) BEFORE MERGE TO master. ***
#
# Registers via the on_actions LIST form of on_game_initialized (the merge form — bare
# inline-effect on_game_initialized blocks do NOT merge across files; only ONE file's
# inline effect runs, so a named action chained from the list is required — see #254).
# The effect runs per-country at game init; QING_TEST_country_primitives self-gates
# CHI-only + a once-ever global flag, so it fires exactly once.
########################################################################

on_game_initialized = {
	on_actions = {
		qing_test_prims_on_game_initialized
	}
}

qing_test_prims_on_game_initialized = {
	effect = {
		if = {
			limit = { tag = CHI }
			QING_TEST_country_primitives = yes
		}
	}
}
