local_wealth_pool_pulse = {
    # Defunct for now: never registered to any on_action pulse list, and local_wealth_pool_gain /
    # local_wealth_pool_demanded were never defined anywhere ("Badly read script value" at load).
    # Mirrors the same defunct-stub convention as CONSUME_setup_all_variables (se_CONSUME.txt).
    #effect = {
    #    if = {
    #        limit = {
    #            NOT = {
    #                has_variable = local_wealth_pool_update
    #            }
    #        }
    #        set_variable = {
    #            name = local_wealth_pool_update
    #            value = 0
    #        }
    #    }
    #    change_variable = {
    #        name = "local_wealth_pool_update"
    #        add = local_wealth_pool_gain
    #    }
    #    change_variable = {
    #        name = "local_wealth_pool_update"
    #        subtract = local_wealth_pool_demanded
    #    }
    #}
}
