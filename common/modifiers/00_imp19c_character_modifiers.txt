#cannot_be_foreign_minister = {
	#disallow_office = { office = secretary_m_foreign_affairs }
#}

IneligibleForAllRoles = {
	disallow_command = yes
	disallow_office = yes
	disallow_job = yes
	cancellation_trigger = {
		OR = {
			# has_character_modifier = EligibleForCommand # Removed for now due to issue with raising capital governorship levy
			# TODO: Find a workaround for this
			has_character_modifier = EligibleForJobs
			has_character_modifier = EligibleForOffice
		}
	}
}

EligibleForCommandOnly = {
	disallow_job = yes
	disallow_office = yes
	cancellation_trigger = {
		OR = {
			has_character_modifier = EligibleForJobs
			has_character_modifier = EligibleForOffice
		}
	}
}

EligibleForJobs = {
	disallow_office = yes
	cancellation_trigger = {
		has_character_modifier = EligibleForOffice
	}
}

EligibleForOffice = {
	prominence = 10
	cancellation_trigger = {
		always = no
	}
}

LoyalAristocrat = {
	character_loyalty = 65 #doesn't seem to work
	loyalty_gain_chance = 0.6
	prominence = 10
}

# task #90 — the Han regional magnate (勇營/湘軍/淮軍 founder). A capable general
# who has raised a personal provincial army: a real martial asset to the dynasty in
# its hour of need, but a man whose loyal cohorts answer to HIM. He is harder for
# the throne to command back into line (loyalty_gain_chance_modifier down) and grows
# in standing as his private base does. The concrete face of qing_han_provincial_power.
# Granted by QING_regional_army_bind_commander; stripped by QING_reassert_strip_magnate.
qing_regional_magnate = {
	martial = 1
	land_morale_modifier = 0.1
	monthly_character_prominence = 0.03
	loyalty_gain_chance_modifier = -0.2
}