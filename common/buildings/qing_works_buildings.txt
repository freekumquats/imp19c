qing_dike_building = {
	# [#121] 河堤 — river dike/levee: the Yellow River (黃河) dike system, maintained
	# by the Board of Works (工部) at enormous annual expense (河工). Reduces flood
	# risk (unrest, population loss), supports local food production. Historical
	# anchor: Yellow River hydraulic works consumed 10-15% of the Qing central
	# budget in ordinary years; failures brought catastrophic floods (1851, 1855).
	local_population_capacity = 500
	local_monthly_state_loyalty = 0.03
	local_monthly_food_modifier = 0.05
	local_population_happiness = 0.03

	cost = 70
	time = 240

	# [BT 2026-07-27 #4] River dikes only make sense on a river. has_minor_river is a
	# proven province-scope trigger (river_port_building / common/buildings/00_default.txt:60).
	# Event/pulse spawns via add_building_level bypass BOTH potential and allow, so the
	# Works-minister raise is unaffected — this only tightens the player build menu.
	potential = {
		has_minor_river = yes
	}
	allow = {
		owner = { invention = tech_central_administration }
		has_minor_river = yes
		sufficient_job_slots = yes
	}

	modification_display = {
		0 = local_population_capacity
		1 = local_monthly_state_loyalty
		2 = local_monthly_food_modifier
		3 = local_population_happiness
	}
}

qing_canal_depot_building = {
	# [#121] 漕運倉 — grain-tribute canal depot: a granary/warehouse hub on the
	# Grand Canal (大運河) system that moved rice from the Yangtze delta north to
	# the capital (漕運, the grain-tribute lifeline). Supports trade, food supply,
	# local tax efficiency. Historical anchor: the Grand Canal carried ~400,000
	# tons of tribute grain annually at its Qing height; silting/decay forced a
	# shift to sea transport by the 1820s-30s (Hoshi Ayao, The Ming Tribute
	# Grain System).
	# [#433] the Grand Canal was above all a MOVEMENT artery — grain, troops and couriers moved
	# on it. army_movement_speed gives the depot its concrete transport teeth (proven province-scope
	# key: INF_railway_upgrade / 00_infrastructure_buildings.txt:66, INF... 00_from_events_province.txt:412).
	local_state_trade_routes = 1
	army_movement_speed = 0.10
	local_monthly_food_modifier = 0.08
	local_tax_modifier = 0.06
	local_monthly_civilization = 0.003

	cost = 65
	time = 200

	# [BT 2026-07-27 #3] The Grand Canal ran Hangzhou→Beijing: only its corridor regions
	# (Zhejiang → Jiangsu → Shandong → Zhili) may raise a depot. is_in_region is the proven
	# province-scope trigger (se_QING_DECLINE.txt / common map regions). Event spawns bypass this.
	potential = {
		OR = {
			is_in_region = Zhili
			is_in_region = Shandong
			is_in_region = Jiangsu
			is_in_region = Zhejiang
		}
	}
	allow = {
		owner = { invention = tech_central_administration }
		OR = {
			is_in_region = Zhili
			is_in_region = Shandong
			is_in_region = Jiangsu
			is_in_region = Zhejiang
		}
		sufficient_job_slots = yes
	}

	modification_display = {
		0 = local_state_trade_routes
		1 = army_movement_speed
		2 = local_monthly_food_modifier
		3 = local_tax_modifier
		4 = local_monthly_civilization
	}
}

qing_wall_section_building = {
	# [#121] 長城段 — restored Great Wall section: a symbolic prestige work of
	# the Board of Works. By the Qing the Wall was militarily obsolete (the
	# dynasty's OWN conquest came from Manchuria, not stopped by the Wall), but
	# restoring a monumental section broadcasts imperial majesty and defensive
	# resolve. Defensive, prestige, and a modest civilization marker.
	local_defensive = 0.08
	local_monthly_civilization = 0.005
	local_population_happiness = 0.02

	cost = 90
	time = 300

	# [BT 2026-07-27 #3] The Wall runs the northern frontier — only its corridor regions
	# (Gansu 嘉峪關 → Shaanxi → Shanxi → Zhili 山海關, plus the Manchurian gate at Liaoning)
	# may raise a section. is_in_region is proven province-scope. Event spawns bypass this.
	# Highest cost/time of the three — a grand project.
	potential = {
		OR = {
			is_in_region = Zhili
			is_in_region = Shanxi
			is_in_region = Shaanxi
			is_in_region = Gansu
			is_in_region = Liaoning
		}
	}
	allow = {
		owner = { invention = tech_central_administration }
		OR = {
			is_in_region = Zhili
			is_in_region = Shanxi
			is_in_region = Shaanxi
			is_in_region = Gansu
			is_in_region = Liaoning
		}
		sufficient_job_slots = yes
	}

	modification_display = {
		0 = local_defensive
		1 = local_monthly_civilization
		2 = local_population_happiness
	}
}

# ======================================================================
# [#434] THE TWO GREAT MONUMENTS — the Great Wall (長城) and the Grand Canal (大運河)
# PROPER, distinct from the component pieces above (the wall SECTION 長城段 and the canal
# DEPOT 漕運倉). These are the capstone works: bigger modifiers, higher cost, raised by a
# dedicated event that levies both TREASURY and MANPOWER (the corvée labour that built them).
# The section/depot remain the ordinary lower-tier pieces the Works minister raises routinely.
# ======================================================================

qing_great_wall_building = {
	# 長城 — the Great Wall proper: the monumental defensive wonder, far grander than a restored
	# section. Strong defence + a real prestige/legitimacy + civilization mark. Built once, at the
	# northern frontier, by the great-project event (manpower + gold).
	# defence + a prestige/civilization mark. Prestige/legitimacy reward lives in the BUILD EVENT
	# (country-scope keys are not valid on a province building — the selfstr wonder proves buildings
	# carry only local_* keys); here the monument radiates civilization from the frontier.
	local_defensive = 0.25
	local_country_civilization_value = 0.03
	local_monthly_civilization = 0.02
	local_population_happiness = 0.05

	cost = 240
	time = 600

	# [#24 sweep 2026-08-01] CORRECTION: potential = { always = no } was WRONG — add_building_level
	# RESPECTS potential (proven at boot: the qing_mission_cathedral_building BT#6 fix note, "a
	# permanently-false gate HIDES the type"; and memory imp19c-add-building-level-respects-potential).
	# So the old always=no meant the seed (Shanhaiguan/Juyongguan) AND the Works-verb build BOTH
	# silently dropped — leaving the QING_works_build_great_wall verb repeatable for free legitimacy/
	# stability (the once-only NOT-has_building guard could never latch). FIX: a real, satisfiable POTENTIAL
	# gate — the historical Great-Wall belt (owner Chinese/Manchu) — so the monument lands. allow is KEPT
	# always=no: add_building_level (both the game-start seed AND the QING_works_build_great_wall verb)
	# BYPASSES allow, so the wonder still plants, but it stays OUT of the routine build menu — no region-wide
	# menu-build snowball. The Works-verb's own once-only NOT-has_building guard now latches (the building lands).
	potential = {
		owner = { OR = { country_culture_group = jurchen  country_culture_group = chinese_group } }
		OR = {
			is_in_region = Zhili
			is_in_region = Shanxi
			is_in_region = Shaanxi
			is_in_region = Gansu
			is_in_region = Liaoning
		}
	}
	allow = { always = no }

	modification_display = {
		0 = local_defensive
		1 = local_country_civilization_value
		2 = local_monthly_civilization
		3 = local_population_happiness
	}
}

qing_grand_canal_building = {
	# 大運河 — the Grand Canal proper: the empire's great movement-and-trade artery, far beyond a
	# single depot. Strong movement + trade + food. Built by the great-project event (manpower + gold).
	army_movement_speed = 0.25
	local_state_trade_routes = 2
	local_monthly_food_modifier = 0.12
	local_tax_modifier = 0.10
	local_monthly_civilization = 0.01

	cost = 220
	time = 600

	# [#24 sweep 2026-08-01] CORRECTION: potential = { always = no } was WRONG — see the Great Wall
	# above (add_building_level RESPECTS potential; the old gate dropped both the Huai'an seed and the
	# Works-verb build, leaving QING_works_build_grand_canal repeatable for free legitimacy/stability).
	# FIX: a real, satisfiable POTENTIAL gate — the Grand Canal corridor (owner Chinese/Manchu), matching the
	# qing_canal_depot_building region list — so the monument lands. allow KEPT always=no: add_building_level
	# (seed + the QING_works_build_grand_canal verb) bypasses allow, so it plants but stays off the routine
	# menu — no snowball; the verb's once-only guard now latches.
	potential = {
		owner = { OR = { country_culture_group = jurchen  country_culture_group = chinese_group } }
		OR = {
			is_in_region = Zhili
			is_in_region = Shandong
			is_in_region = Jiangsu
			is_in_region = Zhejiang
		}
	}
	allow = { always = no }

	modification_display = {
		0 = army_movement_speed
		1 = local_state_trade_routes
		2 = local_monthly_food_modifier
		3 = local_tax_modifier
		4 = local_monthly_civilization
	}
}
