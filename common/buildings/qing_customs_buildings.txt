# [#91 item E] Maritime Customs house — a real on-map building for the
# Imperial Maritime Customs Service (海關總稅務司署). The efficiency/foreign-control
# meters + Robert Hart (qing_customs_ig_holder) remain the summary/AI layer in
# se_QING_CUSTOMS.txt; this hangs a CONCRETE structure off them, built at a treaty
# port when the service is established/upgraded (see QING_customs_build_house).
# Schema follows arsenal_building (00_military_buildings.txt:20): local modifiers,
# sane cost/time, allow with sufficient_job_slots + a light tech gate.
qing_customs_house_building = {

	# commerce + tax flavour (an honest, efficient revenue house) plus a modern-
	# administration civilization drip (the foreign-run statistical/postal service).
	local_tax_modifier = 0.15
	local_state_trade_routes = 0.2
	local_monthly_civilization = 0.01

	cost = 60
	time = 240

	allow = {
		owner = { invention = tech_monetary_theory }
		sufficient_job_slots = yes
	}

	modification_display = {
	}
}
