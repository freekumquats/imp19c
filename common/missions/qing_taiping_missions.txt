########################################################################
# qing_taiping_missions.txt — the CHRISTIAN / HEAVENLY-KINGDOM mission tree
# (task #14). CHI-only, player-driven — and the HARDEST, most heterodox road in
# the whole Qing suite.
#
# This is the alternate history in which the throne does not break the Taiping
# but JOINS their faith — or in which a throne remade in the Taiping fire founds
# the Heavenly Kingdom of Great Peace (太平天國) in earnest. It is the deliberate
# path to the Christian-theocracy end-state (militant_theocracy + syncretic
# Chinese Christianity), the fourth and rarest of the four golden-age fates.
#
# It is gated behind the Taiping OVERTURE — the "come to terms with the movement"
# choice seeded by qing_roster.13 (Hong Xiuquan) and echoed by qing_rebellion.3.
# Without qing_taiping_overture the tree cannot even begin. Each task calls
# QING_taiping_advance (se_QING_REFORM.txt), pushing qing_taiping_progress toward
# the 70 the end-state requires, and leans on WESTERN BACKING (missionaries, arms,
# recognition) — the historical Taiping's foreign entanglement inverted into
# sponsorship.
#
# The capstone calls QING_reform_endstate_heavenly_kingdom, which changes the
# government to militant_theocracy, turns the state religion to syncretic_christian,
# subsumes any live Taiping rising, and seals the fate.
#
# Gating: CHI-only; potential also requires the overture, so the tree is invisible
# until that door has been opened. Tasks gate on stability / treasury / the
# advancing meter.
#
# NOTE (cosmetic): icons reuse stock test1/2/3 + mission_image_test, as the other
# Qing trees do.
#
# Sources: Spence, God's Chinese Son; Cambridge History of China v.10 on the
#   Taiping; the God-Worshipping Society 拜上帝會, the Heavenly Capital 天京, Hong
#   Rengan's 洪仁玕 reformist "New Treatise on Aids to Administration" 資政新篇.
########################################################################

qing_heavenly_kingdom_mission = {
	# Art source: "Qing ambush of the Taiping Army at Wangjiakou" (1854), from "Ten scenes
	# recording the retreat and defeat of the Taiping Northern Expeditionary Forces,
	# February 1854 - March 1855". Via Wikimedia Commons; public domain.
	icon = qing_heavenly_kingdom_mission
	header = mission_image_qing_taiping

	repeatable = no

	chance = { }
	ai_chance = { factor = 0 }   # player-only

	potential = {
		tag = CHI
		is_ai = no
		# [bookmark-1763 #304] the Taiping Rebellion is 1850-1864; the tree must not be
		# reachable before then (blocks anachronistic early opening at the 1763 start — the
		# qing_taiping_overture can be recorded early via a pressure event, but the tree stays
		# hidden until 1850; the 1815 start sits before 1850 so its tuned behaviour is unchanged).
		current_date >= 1850.1.1
		# invisible until the throne has opened the door to the movement
		has_variable = qing_taiping_overture
		NOT = { has_variable = qing_reform_endstate_reached }
	}

	abort = {
		OR = {
			NOT = { tag = CHI }
			# [task #12 review-fix, mutual-exclusion 2026-08-30] once the suppression branch's
			# capstone (qing_hk_restore_order) has crushed the rebellion, the whole point of this
			# tree -- joining it -- is gone; close it out rather than leave it open with an
			# unreachable capstone.
			has_variable = qing_taiping_suppressed
		}
	}

	on_start = {
		LOG_line = { sys = QING  msg = "MISSION Heavenly Kingdom (Christian path) started" }
		QING_taiping_init = yes
	}

	on_completion = {
		LOG_line = { sys = QING  msg = "MISSION Heavenly Kingdom COMPLETED" }
	}

	# ==================================================================
	# ROOT TASK — treat with the God-Worshipping Society (拜上帝會).
	# Open a real channel to the movement rather than fighting it to the death.
	# ==================================================================
	qing_hk_channel = {
		icon = qing_hk_channel
		allow = {
			political_influence >= 15
		}
		on_start = {
			add_political_influence = -15
		}
		on_completion = {
			custom_tooltip = qing_hk_channel_tt
			QING_taiping_advance = { amount = 12 }
			QING_DECLINE_nudge = { var = qing_sect_pressure  amount = -6 }
			LOG_line = { sys = QING  msg = "MISSION task hk_channel done" }
		}
	}

	# ==================================================================
	# WESTERN BACKING — missionaries and arms. The powers that officered the
	# Ever-Victorious Army are courted the other way: as sponsors of the new faith.
	# ==================================================================
	qing_hk_western_backing = {
		icon = qing_hk_western_backing
		requires = { qing_hk_channel }
		allow = {
			treasury >= 90
		}
		on_start = {
			add_treasury = -90
			TREASURY_LOG_it = { amount = -90 }
		}
		on_completion = {
			custom_tooltip = qing_hk_western_backing_tt
			QING_taiping_advance = { amount = 15 }
			# foreign engagement drags the realm toward the wider reform crisis too
			QING_DECLINE_nudge = { var = qing_reform_pressure  amount = 5 }
			LOG_line = { sys = QING  msg = "MISSION task hk_western_backing done" }
		}
	}

	# ==================================================================
	# THE HEAVENLY DOCTRINE — propagate the syncretic Christian teaching and
	# Hong Rengan's reform programme (資政新篇). The ideological conversion of the
	# realm's heart.
	# ==================================================================
	qing_hk_doctrine = {
		icon = qing_hk_doctrine
		requires = { qing_hk_channel }
		allow = {
			political_influence >= 25
			custom_tooltip = {
				text = qing_hk_needs_progress_tt
				var:qing_taiping_progress >= 25
			}
		}
		on_start = {
			add_political_influence = -25
		}
		on_completion = {
			custom_tooltip = qing_hk_doctrine_tt
			QING_taiping_advance = { amount = 18 }
			# heterodoxy is destabilising before it is triumphant
			add_stability = -1
			LOG_line = { sys = QING  msg = "MISSION task hk_doctrine done" }
		}
	}

	# ==================================================================
	# THE HEAVENLY CAPITAL — 天京: consecrate a capital of the new order and put
	# the God Worshippers' austere discipline (banned opium, banned foot-binding,
	# shared granaries) into the machinery of state.
	# ==================================================================
	# [task #12 review-fix, mutual-exclusion 2026-08-30] this capstone used to require the
	# SUPPRESSION branch (qing_hk_gordon/grain_south/militia/restore_order) -- the tasks that
	# raise the historical anti-Taiping armies and, in restore_order's case, literally STRIP
	# the qing_rebel_taiping modifier. That meant a player could not reach "join the Taiping"
	# without first crushing them, contradicting the whole tree's own stated premise (this is
	# the alternate history where the throne JOINS the faith, not suppresses it). Re-gated onto
	# the JOIN branch's own culmination instead (congregation->uprising, doctrine->land_system,
	# channel->western_backing all now feed here), and onto NOT having already committed to the
	# rival suppression branch (qing_taiping_suppressed, set by qing_hk_restore_order below).
	qing_hk_heavenly_capital = {
		icon = qing_hk_heavenly_capital
		requires = { qing_hk_uprising qing_hk_land_system qing_hk_western_backing }
		allow = {
			treasury >= 80
			custom_tooltip = {
				text = qing_hk_needs_progress_tt
				var:qing_taiping_progress >= 50
			}
			custom_tooltip = {
				text = qing_hk_needs_not_suppressed_tt
				NOT = { has_variable = qing_taiping_suppressed }
			}
		}
		on_start = {
			add_treasury = -80
			TREASURY_LOG_it = { amount = -80 }
		}
		on_completion = {
			custom_tooltip = qing_hk_heavenly_capital_tt
			QING_taiping_advance = { amount = 15 }
			add_country_modifier = { name = qing_rebel_taiping  duration = 1825 }
			add_stability = -1
			set_variable = { name = qing_taiping_joined  value = 1 }
			LOG_line = { sys = QING  msg = "MISSION task hk_heavenly_capital done -- the throne has JOINED the Taiping" }
		}
	}

	# ==================================================================
	# THE GOD WORSHIPPERS (拜上帝會) — organise the secret congregation in the Guangxi hills,
	# the seed-body of the rising. The first cadre of the new faith.
	# ==================================================================
	qing_hk_congregation = {
		icon = qing_hk_congregation
		requires = { qing_hk_channel }
		allow = { political_influence >= 15 }
		on_start = { add_political_influence = -15 }
		on_completion = {
			custom_tooltip = qing_hk_congregation_tt
			QING_taiping_advance = { amount = 10 }
			QING_DECLINE_nudge = { var = qing_sect_pressure  amount = -4 }
			LOG_line = { sys = QING  msg = "MISSION task hk_congregation done" }
		}
	}

	# ==================================================================
	# THE JINTIAN UPRISING (金田起義) — raise the banner of revolt and break out of Guangxi,
	# the march north that turns a sect into an army.
	# ==================================================================
	qing_hk_uprising = {
		icon = qing_hk_uprising
		requires = { qing_hk_congregation }
		allow = {
			treasury >= 60
			custom_tooltip = { text = qing_hk_needs_progress_tt  var:qing_taiping_progress >= 20 }
		}
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_hk_uprising_tt
			QING_taiping_advance = { amount = 14 }
			add_stability = -1
			LOG_line = { sys = QING  msg = "MISSION task hk_uprising done" }
		}
	}

	# ==================================================================
	# THE LAND SYSTEM (天朝田畝制度) — proclaim the radical land programme: the common granary,
	# the shared field, the levelling of the old gentry order.
	# ==================================================================
	qing_hk_land_system = {
		icon = qing_hk_land_system
		requires = { qing_hk_doctrine }
		allow = {
			political_influence >= 25
			custom_tooltip = { text = qing_hk_needs_progress_tt  var:qing_taiping_progress >= 40 }
		}
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_hk_land_system_tt
			QING_taiping_advance = { amount = 12 }
			QING_DECLINE_nudge = { var = qing_reform_pressure  amount = 6 }
			# [#234] the 天朝田畝制度's common granary made concrete: the land system's defining
			# institution was the shared granary (聖庫) into which the harvest was pooled. Raise one
			# on the most-populous held Yangzi-heartland province (where the Taiping actually ruled).
			if = {
				limit = { any_owned_province = { OR = { is_in_region = Jiangsu  is_in_region = Jiangxi  is_in_region = Hunan  is_in_region = Hubei  is_in_region = Anhui  is_in_region = Zhejiang }  NOT = { has_building = qing_granary_building } } }
				ordered_owned_province = {
					order_by = total_population
					max = 1
					limit = { OR = { is_in_region = Jiangsu  is_in_region = Jiangxi  is_in_region = Hunan  is_in_region = Hubei  is_in_region = Anhui  is_in_region = Zhejiang }  NOT = { has_building = qing_granary_building } }
					add_building_level = qing_granary_building
				}
			}
			LOG_line = { sys = QING  msg = "MISSION task hk_land_system done" }
		}
	}

	# ==================================================================
	# THE NORTHERN EXPEDITION (北伐) — send the army against the old capital itself, the
	# lunge for the throne that decides whether the Heavenly Kingdom rules or merely rebels.
	# ==================================================================
	qing_hk_northern_expedition = {
		icon = qing_hk_northern_expedition
		requires = { qing_hk_heavenly_capital }
		allow = {
			treasury >= 100
			custom_tooltip = { text = qing_hk_needs_progress_tt  var:qing_taiping_progress >= 60 }
		}
		on_start = { add_treasury = -100  TREASURY_LOG_it = { amount = -100 } }
		on_completion = {
			custom_tooltip = qing_hk_northern_expedition_tt
			QING_taiping_advance = { amount = 15 }
			add_stability = -1
			LOG_line = { sys = QING  msg = "MISSION task hk_northern_expedition done" }
		}
	}

	# ==================================================================
	# THE NEW ADMINISTRATION (資政新篇) — put Hong Rengan's reform programme into the machinery
	# of the new state: railways, banks, a free press — a modernising theocracy.
	# ==================================================================
	qing_hk_new_admin = {
		icon = qing_hk_new_admin
		requires = { qing_hk_heavenly_capital }
		allow = { treasury >= 70 }
		on_start = { add_treasury = -70  TREASURY_LOG_it = { amount = -70 } }
		on_completion = {
			custom_tooltip = qing_hk_new_admin_tt
			QING_taiping_advance = { amount = 10 }
			QING_selfstr_advance = { amount = 5 }
			LOG_line = { sys = QING  msg = "MISSION task hk_new_admin done" }
		}
	}

	# ==================================================================
	# CAPSTONE — PROCLAIM THE HEAVENLY KINGDOM (天國) → END-STATE (4).
	# Government becomes a militant theocracy; the state religion turns to the
	# syncretic Chinese Christianity of the God Worshippers. The rarest fate.
	# ==================================================================
	qing_hk_proclaim = {
		icon = qing_hk_proclaim
		requires = { qing_hk_heavenly_capital }
		allow = {
			custom_tooltip = {
				text = qing_hk_needs_apex_tt
				var:qing_taiping_progress >= 70
			}
			custom_tooltip = {
				text = qing_hk_needs_not_sealed_tt
				NOT = { has_variable = qing_reform_endstate_reached }
			}
		}
		on_completion = {
			custom_tooltip = qing_hk_proclaim_tt
			QING_reform_endstate_heavenly_kingdom = yes
			LOG_line = { sys = QING  msg = "MISSION Heavenly Kingdom CAPSTONE (theocracy proclaimed)" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL Xiang Army column at Changsha
	# (p:2793, Hunan's capital and Zeng Guofan's real 1853 muster site — confirmed CHI
	# own_control_core, setup/main/00_default.txt), the same bare-create_unit idiom the
	# Burma tree uses for the identical "the army marches" claim. Gated on the Qing (or a
	# Qing subject) actually holding Changsha — a real, non-trivial condition.
	qing_hk_xiang_army = {
		icon = qing_hk_xiang_army
		requires = { qing_hk_channel }
		allow = {
			treasury >= 60
			custom_tooltip = {
				text = qing_hk_needs_changsha_tt
				owns_or_subject_owns = 2793
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_hk_xiang_army_tt
			SE_qing_raise_garrison = { prov = p:2793  name = "Xiang Army"  size = 5  unit = qing_green_standard }
			current_ruler = { add_popularity = 12 }
			LOG_line = { sys = QING  msg = "MISSION task Xiang Army raised at Changsha" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL Huai Army column at Hefei
	# (p:6688, Li Hongzhang's real home base and 1862 muster site), same idiom.
	qing_hk_huai_army = {
		icon = qing_hk_huai_army
		requires = { qing_hk_western_backing }
		allow = {
			treasury >= 60
			custom_tooltip = {
				text = qing_hk_needs_hefei_tt
				owns_or_subject_owns = 6688
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_hk_huai_army_tt
			SE_qing_raise_garrison = { prov = p:6688  name = "Huai Army"  size = 5  unit = qing_green_standard }
			current_ruler = { add_popularity = 12 }
			LOG_line = { sys = QING  msg = "MISSION task Huai Army raised at Hefei" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL relief column at Anqing
	# (p:9168, the real river-gate city named in the task's own title), same idiom.
	# [task #12 review-fix, mutual-exclusion 2026-08-30] repointed from qing_hk_doctrine
	# (a JOIN-branch task) to qing_hk_huai_army (a SUPPRESS-branch task) -- the old edge
	# forced a suppression playthrough to first complete a joining act, crossing branches.
	qing_hk_anqing = {
		icon = qing_hk_anqing
		requires = { qing_hk_huai_army }
		allow = {
			treasury >= 55
			custom_tooltip = {
				text = qing_hk_needs_anqing_tt
				owns_or_subject_owns = 9168
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -55  TREASURY_LOG_it = { amount = -55 } }
		on_completion = {
			custom_tooltip = qing_hk_anqing_tt
			SE_qing_raise_garrison = { prov = p:9168  name = "Anqing Relief Force"  size = 4  unit = qing_green_standard }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task Anqing garrisoned" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL relief column at Suzhou
	# (p:2588, the real Jiangsu Suzhou — area:Suzhou, map_data/areas.txt — the city
	# the task's own title names), same idiom.
	# [task #12 review-fix, mutual-exclusion 2026-08-30] repointed from qing_hk_congregation
	# (a JOIN-branch task) to qing_hk_xiang_army (a SUPPRESS-branch task), same reasoning as
	# qing_hk_anqing above.
	qing_hk_suzhou = {
		icon = qing_hk_suzhou
		requires = { qing_hk_xiang_army }
		allow = {
			treasury >= 55
			custom_tooltip = {
				text = qing_hk_needs_suzhou_tt
				owns_or_subject_owns = 2588
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -55  TREASURY_LOG_it = { amount = -55 } }
		on_completion = {
			custom_tooltip = qing_hk_suzhou_tt
			SE_qing_raise_garrison = { prov = p:2588  name = "Suzhou Relief Force"  size = 4  unit = qing_green_standard }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task Suzhou garrisoned" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL relief column at Hangzhou
	# (p:8120, the real Zhejiang capital the task's own title names), same idiom.
	qing_hk_hangzhou = {
		icon = qing_hk_hangzhou
		requires = { qing_hk_xiang_army }
		allow = {
			treasury >= 55
			custom_tooltip = {
				text = qing_hk_needs_hangzhou_tt
				owns_or_subject_owns = 8120
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -55  TREASURY_LOG_it = { amount = -55 } }
		on_completion = {
			custom_tooltip = qing_hk_hangzhou_tt
			SE_qing_raise_garrison = { prov = p:8120  name = "Hangzhou Relief Force"  size = 4  unit = qing_green_standard }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task Hangzhou garrisoned" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL suppression column at Bozhou
	# (p:4344, the real Huaibei-plain town at the historical heart of the Nian
	# Rebellion — confirmed CHI own_control_core), same idiom.
	qing_hk_nian = {
		icon = qing_hk_nian
		requires = { qing_hk_huai_army }
		allow = {
			treasury >= 55
			custom_tooltip = {
				text = qing_hk_needs_bozhou_tt
				owns_or_subject_owns = 4344
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -55  TREASURY_LOG_it = { amount = -55 } }
		on_completion = {
			custom_tooltip = qing_hk_nian_tt
			SE_qing_raise_garrison = { prov = p:4344  name = "Nian Suppression Force"  size = 4  unit = qing_green_standard }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task Nian rebels ridden down at Bozhou" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL qing_ever_victorious detachment at
	# Shanghai (p:5429, the historical EVA's real base under Ward then Gordon) — the
	# bare create_unit idiom does not check the unit's own construction-menu `allow`
	# (imp19c_effects_legion_setup.txt:106-108), so this raises the real Western-drilled
	# corps even before the modernization-doctrine law is adopted, matching the task's
	# own "Gordon takes command" claim.
	qing_hk_gordon = {
		icon = qing_hk_gordon
		requires = { qing_hk_anqing }
		allow = {
			political_influence >= 25
			custom_tooltip = {
				text = qing_hk_needs_shanghai_tt
				owns_or_subject_owns = 5429
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_hk_gordon_tt
			SE_qing_raise_garrison = { prov = p:5429  name = "Ever Victorious Army"  size = 3  unit = qing_ever_victorious }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task Gordon's Ever Victorious Army raised at Shanghai" }
		}
	}

	# [task-D stub-fix] made concrete: plants a REAL banner garrison
	# (qing_banner_garrison_building) on the most-populous held Yangzi-heartland
	# province lacking one — the same regional idiom qing_hk_land_system above already
	# uses (ordered_owned_province across the Jiangsu/Jiangxi/Hunan/Hubei/Anhui/Zhejiang
	# regions) for an identical "raise a real force across the loyal countryside" claim.
	qing_hk_militia = {
		icon = qing_hk_militia
		requires = { qing_hk_suzhou }
		allow = {
			treasury >= 55
			custom_tooltip = {
				text = qing_hk_needs_militia_ground_tt
				any_owned_province = { OR = { is_in_region = Jiangsu  is_in_region = Jiangxi  is_in_region = Hunan  is_in_region = Hubei  is_in_region = Anhui  is_in_region = Zhejiang }  NOT = { has_building = qing_banner_garrison_building } }
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -55  TREASURY_LOG_it = { amount = -55 } }
		on_completion = {
			custom_tooltip = qing_hk_militia_tt
			if = {
				limit = { any_owned_province = { OR = { is_in_region = Jiangsu  is_in_region = Jiangxi  is_in_region = Hunan  is_in_region = Hubei  is_in_region = Anhui  is_in_region = Zhejiang }  NOT = { has_building = qing_banner_garrison_building } } }
				ordered_owned_province = {
					order_by = total_population
					max = 1
					limit = { OR = { is_in_region = Jiangsu  is_in_region = Jiangxi  is_in_region = Hunan  is_in_region = Hubei  is_in_region = Anhui  is_in_region = Zhejiang }  NOT = { has_building = qing_banner_garrison_building } }
					add_building_level = qing_banner_garrison_building
				}
			}
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task tuanlian militia garrison planted in the loyal countryside" }
		}
	}

	# [task-D stub-fix] made concrete: retools a held Yangzi-heartland province to silk —
	# the real premier southeastern cash-crop revenue good (common/trade_goods/
	# 00_imp19c.txt) — the same set_trade_goods idiom the Burma teak/jade tasks use for
	# an identical "the revenues are restored" claim.
	qing_hk_grain_south = {
		icon = qing_hk_grain_south
		requires = { qing_hk_hangzhou }
		allow = {
			treasury >= 55
			custom_tooltip = {
				text = qing_hk_needs_grain_south_tt
				any_owned_province = { OR = { is_in_region = Jiangsu  is_in_region = Zhejiang  is_in_region = Jiangxi }  NOT = { trade_goods = silk } }
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -55  TREASURY_LOG_it = { amount = -55 } }
		on_completion = {
			custom_tooltip = qing_hk_grain_south_tt
			if = {
				limit = { any_owned_province = { OR = { is_in_region = Jiangsu  is_in_region = Zhejiang  is_in_region = Jiangxi }  NOT = { trade_goods = silk } } }
				ordered_owned_province = {
					order_by = total_population
					max = 1
					limit = { OR = { is_in_region = Jiangsu  is_in_region = Zhejiang  is_in_region = Jiangxi }  NOT = { trade_goods = silk } }
					set_trade_goods = silk
				}
			}
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task southeastern revenues restored (heartland ground turned to silk)" }
		}
	}

	# [task-D stub-fix] made concrete: raises a REAL garrison at Nanjing (p:6659, the
	# real Heavenly Capital the task's own title claims to retake) and, since "restoring
	# the order of Heaven" should genuinely diminish the ongoing rebellion, strips the
	# qing_rebel_taiping country modifier if it is still active (guarded — it may never
	# have been granted, e.g. if this suppression branch is reached without the
	# Christian-path qing_hk_heavenly_capital task ever firing).
	qing_hk_restore_order = {
		icon = qing_hk_restore_order
		requires = { qing_hk_nian }
		allow = {
			treasury >= 70
			custom_tooltip = {
				text = qing_hk_needs_nanjing_tt
				owns_or_subject_owns = 6659
			}
			custom_tooltip = {
				text = qing_hk_needs_not_joined_tt
				NOT = { has_variable = qing_taiping_joined }
			}
		}
		on_start = { add_treasury = -70  TREASURY_LOG_it = { amount = -70 } }
		on_completion = {
			custom_tooltip = qing_hk_restore_order_tt
			SE_qing_raise_garrison = { prov = p:6659  name = "Nanjing Retaken"  size = 6  unit = qing_green_standard }
			if = { limit = { has_country_modifier = qing_rebel_taiping }  remove_country_modifier = qing_rebel_taiping }
			current_ruler = { add_popularity = 15 }
			add_stability = 1
			# [task #12 review-fix, mutual-exclusion 2026-08-30] this is the suppression branch's
			# capstone -- Nanjing retaken means the rebellion is crushed, so the Christian
			# "join the Taiping" ending is no longer reachable this playthrough.
			set_variable = { name = qing_taiping_suppressed  value = 1 }
			LOG_line = { sys = QING  msg = "MISSION task Nanjing retaken, rebellion modifier lifted if active -- the Taiping are SUPPRESSED" }
		}
	}
}
