########################################################################
# qing_india_missions.txt — CONTEST THE COMPANY (逐夷復印), #449.
#
# The Qing bid to exploit the chaos of eighteenth-century India and STRANGLE the
# British East India Company in its cradle — before the upstart trading company grows
# into the paramount power of Hindustan — bringing the independent Indian thrones under
# the wing of the Son of Heaven and driving the western sea-merchants back to the coast.
#
# The 1763 board (setup/main/00_default.txt): this is PRE-BUXAR India. The EIC is not yet
# an empire — it is a megacorporation (client_colony of Britain, GBR) clinging to a
# handful of coastal footholds, seated at Calcutta (6219) with factories at Madras,
# Bombay and Bengkulu. Post-Plassey it dominates Bengal's REVENUE, but the Nawab of
# Bengal (BNG, Mir Qasim, at Murshidabad) is still the SOVEREIGN and is arming for the
# war that will become Buxar (1764). The great Indian powers are INDEPENDENT, not Company
# clients: the Timurid emperor (MUG, nominal paramount at Delhi), Awadh (AWA), the Nizam
# of Hyderabad (HYD), Mysore under Hyder Ali (MYS), the Marathas (MRT/NAG). The Qing come
# as an eastern paramount offering the wing to these thrones — and as the power that can
# break the Company at Calcutta before it swallows the subcontinent. The tree has a
# natural anti-Company ally (the Nawab) and an independent throne to court (the Mughal).
#
# As with the Burma (#421) and open-Japan (#447) trees, the PLAYER does the real work:
# the task goals gate on the Qing genuinely CONTROLLING the ground (the Company seat at
# Calcutta) or bringing the Indian powers under the wing (is_subject_of). What is scripted
# (se_QING_INDIA.txt) is only the texture — the pressure the Qing descent lays on the
# Company's Bengal factories, the claim that lets control convert to cession, and the
# tributary embrace of the Nawab and the Mughal (FUNC_make_subject).
#
# It FORKS after the shared opening (the descent on Hindustan):
#   ARC W — 撫孟加拉 THE NAWAB'S CAUSE: make common cause with the Nawab of Bengal (BNG)
#     against the Company, take him under the wing, then storm the Company seat at
#     Calcutta (6219) itself — the Company broken in its Bengal stronghold.
#   ARC E — 撫蒙兀兒 THE MUGHAL COURTED: bring the independent Timurid emperor (MUG) — the
#     nominal paramount of Hindustan, courted by every rising power — under the Son of
#     Heaven, then hold ground in the Gangetic heartland: the paramountcy of Hindustan
#     turned east before the Company can claim it.
#
# CAPSTONE — 逐夷復印 THE COMPANY EXPELLED: the Company broken at Calcutta and the two
# thrones of Hindustan (Bengal and the Mughal) under the wing; the lasting
# qing_india_paramountcy dominion modifier sealed.
#
# Gating: CHI-only, player-only (ai_chance factor 0). Requires the Company (EIC) to exist.
# Tasks cost treasury / political influence and gate on the advancing control + the
# tributary ties, so the tree cannot resolve in the Company's absence. Icons reuse stock
# test1/2/3 (the whole Qing suite's placeholder convention).
#
# Sources: C.A. Bayly, Indian Society and the Making of the British Empire (1988); P.J.
#   Marshall, Bengal: The British Bridgehead (1987); Peter Marshall, The Making and Unmaking
#   of Empires: Britain, India, and America c.1750-1783 (2005); William Dalrymple, The
#   Anarchy: The East India Company, Corporate Violence, and the Pillage of an Empire (2019).
########################################################################

qing_india_mission = {
	# Art source: map of the Chinese Empire and Indo-China by Marie-Joseph-Alphonse
	# Letourneur, published by Librairie Albert Belin, 1902 (public domain).
	icon = qing_india_mission
	header = mission_image_qing_india

	repeatable = no

	chance = { }
	ai_chance = { factor = 0 }   # player-only

	potential = {
		tag = CHI
		is_ai = no
		# there must be a Company to contest.
		exists = c:EIC
	}

	abort = {
		NOT = { tag = CHI }
	}

	on_start = {
		LOG_line = { sys = QING  msg = "MISSION contest the Company (逐夷復印) started" }
	}

	on_completion = {
		LOG_line = { sys = QING  msg = "MISSION contest the Company COMPLETED" }
	}

	# ==================================================================
	# ROOT — THE DESCENT ON HINDUSTAN (南征印度). Commit the frontier armies and the treasury
	# to the long march south through the passes into the plains of Hindustan — the point from
	# which the empire contests the western sea-merchants for the subcontinent. The descent
	# lays its pressure on the Company's Bengal factories.
	# ==================================================================
	qing_india_descent = {
		icon = qing_india_descent
		allow = {
			treasury >= 150
			political_influence >= 30
		}
		on_start = {
			add_treasury = -150
			TREASURY_LOG_it = { amount = -150 }
			add_political_influence = -30
		}
		on_completion = {
			custom_tooltip = qing_india_descent_tt
			# the descent bears down on the Company's Bengal factories (the pressure texture).
			QING_india_open_demand = yes
			current_ruler = { add_popularity = 10 }
			# [scripted-revolutions review-fix] if the descent is mounted while the Company is
			# overstretched by the Napoleonic emergency (asia_napoleonic.3, ~1798-1815: Bonaparte in
			# Egypt + Tipu's Mysore in arms), the moment is riper — a stability + popularity bonus.
			# Wires the asia_india_opening flag the ripple event sets (previously set but never read —
			# the adversarial review caught the dead flag). The asia_company_overstretched modifier on
			# EIC also mechanically weakens it during the window.
			if = {
				limit = { has_global_variable = asia_india_opening }
				custom_tooltip = qing_india_descent_napoleonic_tt
				add_stability = 1
				current_ruler = { add_popularity = 10 }
			}
			LOG_line = { sys = QING  msg = "MISSION task India descent on Hindustan" }
		}
	}

	# ==================================================================
	# ARC W — 撫孟加拉 THE NAWAB'S CAUSE. Make common cause with the Nawab of Bengal (BNG),
	# sovereign and at war with the Company in 1763. Take him under the Qing wing as a
	# tributary — the anti-Company ally in the field. A claim on the Company seat is granted
	# so the drive on Calcutta has a war-goal.
	# ==================================================================
	qing_india_nawab = {
		icon = qing_india_nawab
		requires = { qing_india_descent }
		allow = {
			political_influence >= 30
			custom_tooltip = {
				text = qing_india_needs_nawab_tt
				# the Nawab must be on the map to be reached.
				exists = c:BNG
			}
		}
		on_start = {
			add_political_influence = -30
		}
		on_completion = {
			custom_tooltip = qing_india_nawab_tt
			# take the Nawab under the wing (the make-subject-by-fiat idiom; guarded), and
			# grant a claim on the Company seat at Calcutta so the drive has a war-goal.
			QING_india_embrace_nawab = yes
			QING_india_claim_bengal = yes
			current_ruler = { add_popularity = 15 }
			LOG_line = { sys = QING  msg = "MISSION task India Nawab of Bengal embraced" }
		}
	}

	# ==================================================================
	# ARC W (cont.) — THE FALL OF CALCUTTA (克加爾各答). Storm the Company's Bengal stronghold:
	# take and hold Fort William at Calcutta (6219) itself. With its seat lost, the Company's
	# grip on Bengal is broken.
	# ==================================================================
	qing_india_calcutta = {
		icon = qing_india_calcutta
		requires = { qing_india_nawab }
		allow = {
			treasury >= 100
			custom_tooltip = {
				text = qing_india_needs_calcutta_tt
				# the Company seat under Qing control.
				exists = p:6219
				p:6219 = { controller = ROOT }
			}
		}
		on_start = {
			add_treasury = -100
			TREASURY_LOG_it = { amount = -100 }
		}
		on_completion = {
			custom_tooltip = qing_india_calcutta_tt
			if = { limit = { exists = p:6219  NOT = { owns = 6219 } }  p:6219 = { add_claim = ROOT } }
			current_ruler = { add_popularity = 20 }
			add_stability = 1
			LOG_line = { sys = QING  msg = "MISSION task India Calcutta taken" }
		}
	}

	# ==================================================================
	# ARC E — 撫蒙兀兒 THE MUGHAL COURTED. Reach the independent Timurid emperor (MUG) — the
	# nominal paramount of Hindustan in 1763, courted by every rising power — and set him
	# under the Son of Heaven as a tributary: the legitimist crown of Hindustan turned east,
	# denied to the Company before it can make the emperor its own puppet (as it would by 1803).
	# ==================================================================
	qing_india_mughal = {
		icon = qing_india_mughal
		requires = { qing_india_descent }
		allow = {
			political_influence >= 30
			custom_tooltip = {
				text = qing_india_needs_mughal_tt
				# the Mughal must be on the map to be reached.
				exists = c:MUG
			}
		}
		on_start = {
			add_political_influence = -30
		}
		on_completion = {
			custom_tooltip = qing_india_mughal_tt
			# court the independent Mughal and take him under the wing (guarded).
			QING_india_free_mughal = yes
			current_ruler = { add_popularity = 15 }
			LOG_line = { sys = QING  msg = "MISSION task India Mughal courted and embraced" }
		}
	}

	# ==================================================================
	# ARC E (cont.) — THE GANGETIC HEARTLAND (據恆河). Secure the plains: take and hold ground
	# in the Gangetic heartland (the East_India region — Bihar and the doab), the granary of
	# Hindustan, so the Qing paramountcy rests on real soil and not on tribute alone.
	# ==================================================================
	qing_india_heartland = {
		icon = qing_india_heartland
		requires = { qing_india_mughal }
		allow = {
			custom_tooltip = {
				text = qing_india_needs_heartland_tt
				# ground held in the Gangetic heartland — by the Qing directly, or by the freed
				# Mughal tributary (either roots the paramountcy on real soil).
				OR = {
					any_owned_province = { count >= 2  is_in_region = East_India }
					AND = {
						exists = c:MUG
						c:MUG = { is_subject_of = ROOT }
						c:MUG = { any_owned_province = { count >= 1  is_in_region = East_India } }
					}
				}
			}
		}
		on_completion = {
			custom_tooltip = qing_india_heartland_tt
			current_ruler = { add_popularity = 15 }
			add_stability = 1
			LOG_line = { sys = QING  msg = "MISSION task India Gangetic heartland held" }
		}
	}

	# ==================================================================
	# CAPSTONE — 逐夷復印 THE COMPANY EXPELLED. The Company broken at Calcutta (its seat held)
	# and the two thrones of Hindustan under the wing — the Nawab of Bengal and the Timurid
	# emperor both Qing tributaries. The lasting qing_india_paramountcy dominion modifier is
	# sealed and the pressure on the factories lifts: the western sea-merchants driven from
	# the subcontinent, the paramountcy of Hindustan won for the Son of Heaven.
	# ==================================================================
	qing_india_capstone = {
		icon = qing_india_capstone
		requires = { qing_india_afghan qing_india_calcutta qing_india_ceylon qing_india_ganges_fleet qing_india_heartland qing_india_malabar }
		allow = {
			custom_tooltip = {
				text = qing_india_needs_paramountcy_tt
				# the Company seat held AND both Indian thrones mastered — each either held under
				# the wing as a tributary OR absorbed into the empire outright (annexed out of
				# existence), so a player who integrates a throne rather than keeping it a subject
				# does not strand the completed tree.
				exists = p:6219
				p:6219 = { controller = ROOT }
				OR = { NOT = { exists = c:BNG }  c:BNG = { is_subject_of = ROOT } }
				OR = { NOT = { exists = c:MUG }  c:MUG = { is_subject_of = ROOT } }
			}
		}
		on_completion = {
			custom_tooltip = qing_india_capstone_tt
			add_country_modifier = { name = qing_india_paramountcy  duration = -1 }
			# the Company is broken — the pressure on the Bengal factories lifts.
			QING_india_close_campaign = yes
			current_ruler = { add_popularity = 40 }
			add_stability = 1
			LOG_line = { sys = QING  msg = "MISSION contest the Company CAPSTONE (逐夷復印)" }
		}
	}

	# ==================================================================
	# THE MARATHA COURTS (結馬拉塔) — court the Maratha confederacy against the Company.
	# ==================================================================
	# [india-audit] made concrete: real bidirectional accommodation-opinion diplomacy with the
	# actual Maratha powers (MRT the Peshwa's Maratha Empire, NAG Nagpur — both real INDEPENDENT
	# 1763 tags, setup/countries/countries.txt), the proven qing_gp_accommodation_opinion idiom
	# (qing_new_world_missions/qing_mexico_missions), guarded on existence so an absorbed/absent
	# Maratha power is a no-op.
	qing_india_maratha = {
		icon = qing_india_maratha
		requires = { qing_india_descent }
		allow = { political_influence >= 25 }
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_india_maratha_tt
			if = { limit = { exists = c:MRT }  c:MRT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:MRT } } }
			if = { limit = { exists = c:NAG }  c:NAG = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:NAG } } }
			current_ruler = { add_popularity = 12 }
			LOG_line = { sys = QING  msg = "MISSION task India Maratha confederacy courted (MRT/NAG opinion)" }
		}
	}

	# ==================================================================
	# THE PUNJAB (通錫克) — open relations with the Sikh misls of the Punjab.
	# ==================================================================
	# [india-audit] made concrete: real bidirectional accommodation-opinion diplomacy with the
	# actual Sikh misls of the Punjab (AHL Ahluwalia, BHM Bhangi — the largest 1763 Dal Khalsa
	# fragment tags, setup/countries/countries.txt), guarded on existence.
	qing_india_sikh = {
		icon = qing_india_sikh
		requires = { qing_india_descent }
		allow = { political_influence >= 20 }
		on_start = { add_political_influence = -20 }
		on_completion = {
			custom_tooltip = qing_india_sikh_tt
			if = { limit = { exists = c:AHL }  c:AHL = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:AHL } } }
			if = { limit = { exists = c:BHM }  c:BHM = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:BHM } } }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task India Sikh misls opened relations (AHL/BHM opinion)" }
		}
	}

	# ==================================================================
	# SEIZE THE COMPANY'S COMMERCE (奪夷商) — the trade takeover. The Company came for
	# Bengal's wealth: its raw-export trade in cotton, saltpetre and pepper, shipped home
	# through Calcutta. Tear it out and stand Qing commerce up in its place — customs
	# houses on the seized ports, silk filatures spinning Indian fibre on the spot, a Qing
	# tea plantation at the old pepper post. CONCRETE (per concrete-over-abstract): real
	# buildings raised + the trade good turned from Company raw-export to Qing manufacture
	# on every India-trade province the Qing hold. Gated on actually controlling ground.
	# ==================================================================
	qing_india_trade = {
		icon = qing_india_trade
		requires = { qing_india_nawab }
		allow = {
			treasury >= 60
			custom_tooltip = {
				text = qing_india_needs_trade_ground_tt
				# must hold real India-trade ground to seize the Company's commerce on it:
				# the Company seat, or any controlled port across its Bengal/Bihar footprint.
				OR = {
					AND = { exists = p:6219  p:6219 = { controller = ROOT } }
					any_owned_province = {
						is_port = yes
						OR = { is_in_region = Bengal_region  is_in_region = Bahar  is_in_region = South_India }
					}
				}
			}
		}
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_india_trade_tt
			# CONCRETE trade takeover: customs houses + filatures raised, goods swapped to Qing.
			QING_india_seize_trade = yes
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task qing_india_trade (Company commerce seized)" }
		}
	}

	# ==================================================================
	# THE HIMALAYA ROAD (越嶺入印) — force the passes from Tibet into the Gangetic plain.
	# ==================================================================
	# [india-audit] made concrete: a real claim on Kathmandu (7739, NEP's capital — the actual
	# terminus of the Tibet-to-Gangetic-plain route; verified provincenames_l_english.yml), so
	# forcing the passes lays a genuine war-goal on the corridor. Distinct from (and complementary
	# to, not duplicating) the Gorkha task in qing_himalaya_seasia_missions.txt, which handles NEP
	# subjecthood — this task only claims the corridor province, guarded NOT-owns.
	qing_india_himalaya_road = {
		icon = qing_india_himalaya_road
		requires = { qing_india_mughal }
		allow = { treasury >= 80 }
		on_start = { add_treasury = -80  TREASURY_LOG_it = { amount = -80 } }
		on_completion = {
			custom_tooltip = qing_india_himalaya_road_tt
			if = { limit = { exists = p:7739  NOT = { owns = 7739 } }  p:7739 = { add_claim = ROOT } }
			current_ruler = { add_popularity = 12 }  add_stability = 1
			LOG_line = { sys = QING  msg = "MISSION task India Himalaya road forced (Kathmandu claimed)" }
		}
	}

	# [india-audit] made concrete: real bidirectional accommodation-opinion diplomacy with the
	# actual Nizam of Hyderabad (HYD, real INDEPENDENT 1763 tag), guarded on existence.
	qing_india_hyderabad = {
		icon = qing_india_hyderabad
		requires = { qing_india_maratha }
		allow = { political_influence >= 25 }
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_india_hyderabad_tt
			if = { limit = { exists = c:HYD }  c:HYD = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:HYD } } }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task India Hyderabad envoys received (HYD opinion)" }
		}
	}

	# [india-audit] made concrete: Mysore (MYS, real INDEPENDENT tag, Hyder Ali) genuinely "joins
	# the cause against the Company" — real accommodation opinion with Qing PLUS a real hostile
	# effect on the Company itself (c:EIC manpower/stability debuff), the same siam_flank idiom the
	# Burma war tree uses for a real anti-Company belligerent (qing_burma_war_missions.txt).
	qing_india_mysore = {
		icon = qing_india_mysore
		requires = { qing_india_sikh }
		allow = { political_influence >= 25 }
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_india_mysore_tt
			if = { limit = { exists = c:MYS }  c:MYS = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:MYS } } }
			if = { limit = { exists = c:EIC }  c:EIC = { add_manpower = -2  add_stability = -1 } }
			current_ruler = { add_popularity = 12 }
			LOG_line = { sys = QING  msg = "MISSION task India Mysore joins the cause (MYS opinion, EIC pressed)" }
		}
	}

	# [india-audit] made concrete: the Rohilla Afghans have NO separate country tag on this board
	# (setup/main/00_default.txt:40119 — Rohilkhand is abstracted into the nominal Mughal, MUG,
	# sphere; "good-enough ROW"). So the real, verified geography is claimed directly: the actual
	# Rohilkhand parganas (province IDs cross-checked against provincenames_l_english.yml and
	# MUG's own_control_core list) — Rampur, Shahjahanpur, Najibabad, Saharanpur, Badaun,
	# Moradabad, Muzaffarnagar, Pilibhit, Bareilly.
	qing_india_rohilla = {
		icon = qing_india_rohilla
		requires = { qing_india_trade }
		allow = { political_influence >= 20 }
		on_start = { add_political_influence = -20 }
		on_completion = {
			custom_tooltip = qing_india_rohilla_tt
			if = { limit = { exists = p:344   NOT = { owns = 344 } }    p:344 = { add_claim = ROOT } }
			if = { limit = { exists = p:1157  NOT = { owns = 1157 } }   p:1157 = { add_claim = ROOT } }
			if = { limit = { exists = p:1373  NOT = { owns = 1373 } }   p:1373 = { add_claim = ROOT } }
			if = { limit = { exists = p:2766  NOT = { owns = 2766 } }   p:2766 = { add_claim = ROOT } }
			if = { limit = { exists = p:6932  NOT = { owns = 6932 } }   p:6932 = { add_claim = ROOT } }
			if = { limit = { exists = p:7684  NOT = { owns = 7684 } }   p:7684 = { add_claim = ROOT } }
			if = { limit = { exists = p:7827  NOT = { owns = 7827 } }   p:7827 = { add_claim = ROOT } }
			if = { limit = { exists = p:8065  NOT = { owns = 8065 } }   p:8065 = { add_claim = ROOT } }
			if = { limit = { exists = p:9613  NOT = { owns = 9613 } }   p:9613 = { add_claim = ROOT } }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task India Rohilkhand claimed (9 parganas)" }
		}
	}

	# [india-audit] made concrete: real bidirectional accommodation-opinion diplomacy with the
	# actual Nawab of Awadh (AWA, real INDEPENDENT 1763 tag, Lucknow), guarded on existence.
	qing_india_awadh = {
		icon = qing_india_awadh
		requires = { qing_india_himalaya_road }
		allow = { political_influence >= 25 }
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_india_awadh_tt
			if = { limit = { exists = c:AWA }  c:AWA = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:AWA } } }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task India Awadh inclines to the throne (AWA opinion)" }
		}
	}

	# [india-audit] made concrete: a real claim on Bijapur (9026 — the historical Deccan
	# sultanate seat, held in 1763 by the minor Maratha jagir RMD/Ramdurg; verified
	# provincenames_l_english.yml + 00_default.txt), the overland gateway into the Deccan
	# plateau — distinct from the Hyderabad envoy-opinion task above.
	qing_india_deccan = {
		icon = qing_india_deccan
		requires = { qing_india_hyderabad }
		allow = { treasury >= 60 }
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_india_deccan_tt
			if = { limit = { exists = p:9026  NOT = { owns = 9026 } }  p:9026 = { add_claim = ROOT } }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task India Deccan trade route claimed (Bijapur)" }
		}
	}

	# [india-audit] made concrete: a real claim on Pondichéry (2963), the actual French India
	# (FRI) Coromandel Coast foothold — a genuine western company post contested, matching the
	# tooltip's "western companies" claim exactly (verified provincenames_l_english.yml +
	# 00_default.txt FRI = French India, capital 2963).
	qing_india_coromandel = {
		icon = qing_india_coromandel
		requires = { qing_india_mysore }
		allow = { treasury >= 60 }
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_india_coromandel_tt
			if = { limit = { exists = p:2963  NOT = { owns = 2963 } }  p:2963 = { add_claim = ROOT } }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task India Coromandel ports contested (Pondichery claimed)" }
		}
	}

	# [india-audit] made concrete: required for the capstone, so given real weight — the actual
	# independent Kingdom of Travancore (TRV, the real Malabar pepper coast power holding Cochin/
	# Kollam/Thiruvananthapuram; verified 00_default.txt) is taken under the wing as a sinosphere
	# tributary, the same proven FUNC_make_subject idiom as the Nawab/Mughal above — "a share of
	# the Malabar pepper trade" genuinely secured via tributary embrace, not a flat popularity award.
	qing_india_malabar = {
		icon = qing_india_malabar
		requires = { qing_india_rohilla }
		allow = { treasury >= 60 }
		on_start = { add_treasury = -60  TREASURY_LOG_it = { amount = -60 } }
		on_completion = {
			custom_tooltip = qing_india_malabar_tt
			if = { limit = { exists = c:TRV  NOT = { c:TRV = { is_subject_of = ROOT } } }  FUNC_make_subject = { overlord = ROOT  target = c:TRV  type = sinosphere_tributary } }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task India Malabar (Travancore) secured as tributary" }
		}
	}

	# [india-audit] made concrete: required for the capstone. The real independent Kingdom of
	# Kandy (KND — the interior Sinhalese kingdom, independent of the Dutch VOC's coastal Ceylon/
	# CYL governorate; verified 00_default.txt) is drawn under the wing as a sinosphere tributary,
	# same FUNC_make_subject idiom. CYL itself is a Dutch (NED) dependency, not a valid subject
	# target without a reparenting op outside this task's scope, so KND is the real "sphere" catch.
	qing_india_ceylon = {
		icon = qing_india_ceylon
		requires = { qing_india_awadh }
		allow = { treasury >= 65 }
		on_start = { add_treasury = -65  TREASURY_LOG_it = { amount = -65 } }
		on_completion = {
			custom_tooltip = qing_india_ceylon_tt
			if = { limit = { exists = c:KND  NOT = { c:KND = { is_subject_of = ROOT } } }  FUNC_make_subject = { overlord = ROOT  target = c:KND  type = sinosphere_tributary } }
			current_ruler = { add_popularity = 10 }
			LOG_line = { sys = QING  msg = "MISSION task India Ceylon (Kandy) drawn into the sphere" }
		}
	}

	# [india-audit] made concrete: required for the capstone. Real bidirectional accommodation-
	# opinion diplomacy with the actual Durrani Afghans (AFG, real 1763 tag, setup/countries/
	# countries.txt:602), guarded on existence — the power that actually holds the northwest passes.
	qing_india_afghan = {
		icon = qing_india_afghan
		requires = { qing_india_deccan }
		allow = { political_influence >= 25 }
		on_start = { add_political_influence = -25 }
		on_completion = {
			custom_tooltip = qing_india_afghan_tt
			if = { limit = { exists = c:AFG }  c:AFG = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = ROOT } }  ROOT = { add_opinion = { modifier = qing_gp_accommodation_opinion  target = c:AFG } } }
			current_ruler = { add_popularity = 11 }
			LOG_line = { sys = QING  msg = "MISSION task India Durrani Afghans open the passes (AFG opinion)" }
		}
	}

	# [india-audit] made concrete: required for the capstone. A REAL river-fleet navy is launched
	# (create_unit navy=yes, the proven idiom — se_QING_TREASURE_FLEET.txt/se_QING_SELFSTR.txt —
	# brig is the sole period-appropriate warship key in this TC) at the ablest held port in the
	# Ganges basin (East_India / Bengal_region / Bahar), guarded: no held port there is a silent
	# LOG_fail no-op rather than a crash, mirroring QING_treasure_launch_fleet's own-port fallback.
	qing_india_ganges_fleet = {
		icon = qing_india_ganges_fleet
		requires = { qing_india_coromandel }
		allow = { treasury >= 80 }
		on_start = { add_treasury = -80  TREASURY_LOG_it = { amount = -80 } }
		on_completion = {
			custom_tooltip = qing_india_ganges_fleet_tt
			if = {
				limit = {
					any_owned_province = { is_port = yes  OR = { is_in_region = East_India  is_in_region = Bengal_region  is_in_region = Bahar } }
				}
				ordered_owned_province = {
					order_by = total_population
					max = 1
					limit = { is_port = yes  OR = { is_in_region = East_India  is_in_region = Bengal_region  is_in_region = Bahar } }
					save_scope_as = qing_india_ganges_home_port
				}
				create_unit = {
					navy = yes
					name = QING_UNIT_GANGES_FLEET
					location = scope:qing_india_ganges_home_port
					sub_unit = brig
					while = { count = 2  add_subunit = brig }
				}
				LOG_line = { sys = QING  msg = "MISSION task India Ganges river fleet launched" }
			}
			else = {
				LOG_fail = { sys = QING  fn = "qing_india_ganges_fleet"  reason = "no held Ganges-basin port; fleet not raised" }
			}
			current_ruler = { add_popularity = 14 }
			add_stability = 1
		}
	}
}
