gave_consent = {
	value = 50
	yearly_decay = 10
}

not_gave_consent = {
	value = -50
	yearly_decay = 10
}

diplomatic_play_support = {
	value = 10
	yearly_decay = 5
}

diplomatic_play_condemn = {
	value = -10
	yearly_decay = 5
}

helped_in_play = {
	value = 10
	yearly_decay = 1
}

saved_in_play = {
	value = 100
	yearly_decay = 10
}

hindered_in_play = {
	value = -10
	yearly_decay = 1
}

betrayed_in_play = {
	value = -100
	yearly_decay = 10
}


bitter_over_occupation = {
	value = -100
	yearly_decay = 1
}

diplomatic_play_escalated = {
	value = -50
	yearly_decay = 10
}

executed_agitator = {
	value = -20
	yearly_decay = 5
}

sponsored_agitator_discovered = {
	value = -10
	yearly_decay = 1
}

# --- Qing tributary / investiture opinions (qing_migration events) ---------
# The Son of Heaven's investiture affirmed a tributary's legitimacy (冊封); a
# gracious naming (e.g. 越南, 1804) won goodwill, an overbearing one, resentment.
qing_investiture_opinion = {
	value = 40
	yearly_decay = 4
}

qing_overbearing_opinion = {
	value = -30
	yearly_decay = 3
}

# --- Great-power rivalry opinions (Qing expansion antagonises a specific power)
# SE Asia (incl. Vietnam) → France; Burma → Britain; Central Asia/Siberia/Alaska
# → Russia. Applied by QING_greatpower_react (se_QING_DIPLO.txt).
qing_gp_rivalry_opinion = {          # the affronted power resents Qing encroachment
	value = -40
	yearly_decay = 3
}

qing_gp_accommodation_opinion = {    # ... or is placated by deference / a deal
	value = 25
	yearly_decay = 4
}

# The steady goodwill of sustained envoy-work — a stacking, slowly-decaying
# improved-relations bonus laid down when the Qing accommodates or partitions
# amicably with a power, and stripped once that power's tension crosses the
# hostility threshold (past which no diplomacy smooths it over). Mod-owned
# replacement for the (undefined) vanilla opinion_improve_relation.
qing_gp_relation_opinion = {
	value = 10
	yearly_decay = 2
}

# --- balance-of-power / 以夷制夷 (play the powers off one another) -----------
# A power quietly welcomes the Qing checking one of its Great-Game RIVALS (the
# reward side); or resents the Qing cosying up to one (the cost side). Weaker and
# faster-decaying than the direct rivalry/accommodation opinions above — playing
# both ends is a lever, not a substitute for a real settlement.
qing_gp_playoff_opinion = {          # a common rival has been checked — mild goodwill
	value = 15
	yearly_decay = 5
}

qing_gp_partisan_opinion = {         # the Qing has sided with our rival — mild resentment
	value = -15
	yearly_decay = 5
}

# --- Japan / Meiji sub-layer (東亞近代化; task #22) --------------------------
# Japan is the Qing's PEER, not a predatory great power. Its relationship runs on
# its own accord/rivalry poles (se_QING_DIPLO.txt), surfaced in the two courts'
# mutual opinion. Applied by the Japan mission-tree tasks and player verbs.
qing_japan_accord_opinion = {        # the pan-Asian entente (同文同種) warms the courts
	value = 35
	yearly_decay = 3
}

qing_japan_rivalry_opinion = {       # the collision over Korea/Ryukyu/Taiwan chills them
	value = -45
	yearly_decay = 3
}

# --- pre-Perry Japan arc (幕末前夜; #81) -------------------------------------
# The patron tie a backed daimyo (or a shored-up shogunate) feels toward the
# Qing court that funds it — strong, slowly-decaying goodwill on the real
# on-map Japanese tag (CSU/SHZ/TKG), applied by QING_jppre_back_daimyo /
# QING_jppre_shore_shogunate (se_QING_JAPAN_PREPERRY.txt).
qing_jppre_patron_opinion = {        # a daimyo/shogunate warms to its Qing patron
	value = 40
	yearly_decay = 3
}

# --- foreign advisors / technical experts (洋員; task New#2) ----------------
# A Great Power warms to the Qing when one of its nationals serves as a hired
# expert (a stake in China's modernization). Moderate, slow-decaying goodwill:
# the advisor's presence is a standing tie, not a one-off gesture. Applied by
# QING_advisor_apply_home_hook (se_QING_ADVISORS.txt).
qing_advisor_home_opinion = {
	value = 20
	yearly_decay = 3
}

# --- Generic colonial-claims hostility (se_CLAIM_HOSTILITY.txt) --------------
# A country resents whoever takes land it holds a CLAIM on but does not control.
# Because the actors are dynamic (any claimant vs. any taker) these opinions ARE
# the per-pair hostility store — one is laid down on the claimant, targeted at
# the taker, and decays on its own. Seizure (permanent) bites harder and lingers
# longer than a wartime occupation (which may be reversed at the peace).
claim_violated_opinion = {           # someone permanently seized land we claim
	value = -35
	yearly_decay = 3
}

claim_incursion_opinion = {          # someone is merely occupying land we claim
	value = -15
	yearly_decay = 6
}

# --- Generic migrant friction (se_MIGRATION.txt) ----------------------------
# Cross-border migration strains relations between the two governments, even
# when neither ordered it (the movement is bottom-up). These opinions ARE the
# per-pair friction store — laid down on the aggrieved state, targeted at the
# other, decaying on their own. Both are mild and fast-decaying: a steady
# trickle barely registers, but a sustained flood restacks them faster than
# they fade, driving relations down toward the breaking point where a play
# fires. Weaker than the claim opinions above — friction, not a seizure.
migrant_influx_opinion = {           # the RECEIVING state resents the source of an unwanted influx
	value = -8
	yearly_decay = 4
}

migrant_emigration_opinion = {       # the SENDING state resents the receiving state's harsh handling of its emigrants
	value = -8
	yearly_decay = 4
}

# --- Amban / resident commissioner friction (#113) --------------------------
# The 駐紮大臣 (amban, resident) posted to Inner Asian dependencies. When the
# resident and the Lifan Yuan holder clash, the subject's opinion of the overlord
# worsens; when they cooperate, it improves. When a capable resident maintains
# stability, the overlord-subject bond strengthens. These opinions ARE the per-pair
# friction store — applied by QING_amban_evaluate and amban events, decaying on
# their own. Moderate strength, moderate decay: the resident's performance is an
# ongoing factor in the relationship.
qing_amban_clash_opinion = {         # resident-holder friction degrades overlord-subject relations
	value = -12
	yearly_decay = 3
}

qing_amban_cooperation_opinion = {   # resident-holder cooperation strengthens the bond
	value = 8
	yearly_decay = 3
}

qing_frontier_occupation_opinion = { # [#346] a Beijing garrison on the dependency's soil chafes (occupation, not bond)
	value = -10
	yearly_decay = 4
}

qing_amban_capable_opinion = {       # a loyal, capable resident is a pillar of the relationship
	value = 10
	yearly_decay = 2
}

qing_amban_ineffective_opinion = {   # a weak or disloyal resident is a liability
	value = -8
	yearly_decay = 3
}

# ---- [#276] dynastic-marriage diplomacy: a standing goodwill between wedded houses.
# Two ruling houses joined by royal marriage regard one another warmly; the bond
# decays slowly, so it persists across a reign but fades if not renewed. See
# se_MARRIAGE.txt / DESIGN_MARRIAGE_DIPLOMACY.md.
royal_marriage_opmod = {
	value = 40
	yearly_decay = 3
}

# [#313] a LESSER marriage tie to a NON-ruling noble house — real goodwill, but weaker
# than a dynastic match and with no union/inheritance road behind it.
noble_marriage_opmod = {
	value = 20
	yearly_decay = 3
}

# [#311] a BETROTHAL — a lighter, pledged goodwill than a consummated marriage; decays a
# touch faster (it is a promise, not yet a bond). See MARRIAGE_betroth (se_MARRIAGE.txt).
royal_betrothal_opmod = {
	value = 20
	yearly_decay = 4
}

# [#311] the grievance of a JILTED house whose betrothal was repudiated (the 1725 Louis XV /
# Spanish infanta affront). A deep, slow-healing resentment. See MARRIAGE_repudiate.
royal_jilted_opmod = {
	value = -60
	yearly_decay = 2
}

# [#312] applied to a realm whose succession-law change was not recognised by rival
# great-power neighbours (the Pragmatic-Sanction contest). Decays as the dispute cools.
opinion_disputed_succession = {
	value = -20
	yearly_decay = 4
}
