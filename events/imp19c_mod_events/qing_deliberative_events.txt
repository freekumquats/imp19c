########################################################################
# qing_deliberative_events.txt — the DELIBERATIVE COUNCIL (議政王大臣會議) hidden trampolines — #35.
#
# The Deliberative Council panel (QING_deliberative_panel.txt) open button + the three levers
# (convene / reward / curb) each reach a construct the loader cannot survive compile-inlined into
# a scripted_gui button: QING_delib_recompute_roster contains an ordered_character SORTING
# ITERATOR **and** create_character, and the levers reach ordered_in_list / every_in_list. A
# scripted_gui compile-inlines its button's entire effect chain at PARSE time; such a construct
# inlined into a compiled button is the same crash class that access-violated the loader for the
# censorate/harem/justice/princes iterators (VERIFIED #443/#90 crash classes). FIX: each button
# trigger_events its hidden event here (trigger_event is a RUNTIME ref, never compile-inlined), so
# the iterator/create lives in an event unit the parser does not expand. Behaviour is identical.
#
# All hidden, is_triggered_only, CHI country_events. Logged sys = QING. Mirrors qing_princes.3-.6.
########################################################################

namespace = qing_delib

# .1  ROSTER REFRESH — rebuild the eight banner seats (fill vacancies, self-heal the dead/
#     reassigned) + rebuild the qing_delib_council list the panel iterates. Fired by the
#     open-panel button and by QING_delib_pulse's own direct call is NOT here (the pulse is a
#     plain effect and calls recompute inline — only the BUTTON needs this trampoline).
qing_delib.1 = {
	type = country_event
	hidden = yes
	is_triggered_only = yes
	immediate = {
		LOG_line = { sys = QING  msg = "EVENT qing_delib.1 (open-panel roster refresh) [ROOT.GetTag]" }
		QING_delib_recompute_roster = yes
	}
}

# .2  CONVENE THE GRAND DELIBERATION (大集議)
qing_delib.2 = {
	type = country_event
	hidden = yes
	is_triggered_only = yes
	immediate = {
		LOG_line = { sys = QING  msg = "EVENT qing_delib.2 (convene 大集議) [ROOT.GetTag]" }
		QING_delib_convene = yes
	}
}

# .3  REWARD A BANNER LORD (優賞旗主)
qing_delib.3 = {
	type = country_event
	hidden = yes
	is_triggered_only = yes
	immediate = {
		LOG_line = { sys = QING  msg = "EVENT qing_delib.3 (reward a banner lord) [ROOT.GetTag]" }
		QING_delib_reward = yes
	}
}

# .4  CURB AN OVER-MIGHTY BANNER LORD (裁抑跋扈)
qing_delib.4 = {
	type = country_event
	hidden = yes
	is_triggered_only = yes
	immediate = {
		LOG_line = { sys = QING  msg = "EVENT qing_delib.4 (curb an over-mighty lord) [ROOT.GetTag]" }
		QING_delib_curb = yes
	}
}
