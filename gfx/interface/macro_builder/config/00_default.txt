all_buildings = {
    base = build_in_province
    layout = build_in_province_with_list
	
	map_mode = macro_builder_build_in_province

	includes = {
		port_building
		river_port_building
		EDU_school
		EDU_university
		IND_industrial_estate
		IND_resource_gathering_operation
		# [BT 2026-07-27 #89] The six Qing state production works. THIS includes-list is the
		# macro builder's allowlist: MacroBuilderView.GetBuildInProvinceModel is populated ONLY
		# from the buildings named here (this is how sobisonator's non-vanilla INF_railway_upgrade
		# appears). The GUI item types + name-match visibility were already correct; they were
		# rendering against a model that had never been told to include these buildings.
		qing_silk_filature_building
		qing_porcelain_kiln_building
		qing_tea_workshop_building
		qing_cotton_workshop_building
		qing_salt_yard_building
		qing_opium_poppy_farm_building
		INF_sewer_infrastructure
		INF_hospital
		INF_canal
		INF_railway_upgrade
		INF_depot
		# [BT 2026-07-27] Board-of-Works PUBLIC WORKS — routine player-buildable works only.
		# The two capstone monuments (qing_great_wall_building / qing_grand_canal_building) are
		# event-raised and deliberately EXCLUDED here so they never enter the build menu.
		qing_dike_building
		qing_canal_depot_building
		qing_wall_section_building
		qing_granary_building
		fortress_building
		arsenal_building
		URB_commerce_district
		URB_administration_district
		URB_residential_district
		URB_cultural_district
		# [BT 2026-07-27] FOREIGN — Christian missions + foreign concession.
		qing_mission_underground_building
		qing_mission_public_building
		qing_foreign_concession_building
	}
	
	sound = "event:/SFX/UI/Province/sfx_ui_province_build_building"
}

recruit_naval_units = {
    base = build_in_province
    layout = build_in_province_naval_units
	
	map_mode = macro_builder_build_in_province

    includes = {
        ships
    }
	
	sound = "event:/SFX/UI/Province/sfx_ui_province_build_unit"
}

#	sound = "event:/SFX/UI/Province/sfx_ui_province_pop_move"

#import_tradegood = {
#    base = trade_goods
#    layout = trade_goods
#    map_mode = trade
#	
#	includes = all
#
#	sound = "event:/SFX/UI/Trade/sfx_ui_trade_trade"
#}
