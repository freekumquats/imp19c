# Overnight Decisions 3 — 2026-07-10/11

Branch: **overnight-312-315-audits** (off `1763_bookmark`). Autonomous run.

## Mandate (from user)
1. New branch (done: `overnight-312-315-audits`).
2. Implement tasks **#312, #313, #315** (#314 boot-test fixes are already code-complete on 1763_bookmark; only in-game verification remains — NOT re-done here).
3. Then implement **the overnight-build-candidate findings** = the Top-3 in `research/1763_OVERNIGHT_TOP3_DECOMP.md` (user confirmed "one of them was tributaries"):
   - **#1 Full Imperial Examination System (科舉)** — ~20h decomposition.
   - **#2 Full Tributary System (朝貢體系)** — ~18h decomposition.
   - **#3 Grand Canal Grain-Tribute Logistics (漕運)** — ~16h decomposition.
4. **Deep adversarial review (Workflow) after EACH task completes**; fix confirmed findings before moving on, THEN commit.
5. Log all major decisions here; **explicitly call out deferred pieces**.
6. Work continuously without stopping to ask, until all tasks complete.

## Corrected interpretation (2026-07-10)
- Initial reading mis-identified the "findings" as the AUDIT_* docs. User corrected: the target is the **~20h overnight build candidates** (Exam / Tributary / Grand Canal), decomposed in `research/1763_OVERNIGHT_TOP3_DECOMP.md` + `research/1763_OVERNIGHT_BUILD_CANDIDATES.md`.
- **DEFERRED (explicitly, not built this run):**
  - The AUDIT_* dashboard-suite (invisible-mechanics dashboards) and the B27 dead-code fix (`TRADE_update_all_TZ_local_prices`) — these are the OTHER audit stream, not the build candidates. Left for a later pass.
  - Build candidates 4-8 in `1763_OVERNIGHT_BUILD_CANDIDATES.md` (Silver/Opium monetary model, Xinjiang consolidation arc, Court-Intrigue sim, Population/Famine model, Silk-Road caravans) — only the Top-3 are in scope.
- **Execution reality note:** the Top-3 decompositions describe ~54h of parallel-agent work each with a research→design→engine→events→GUI→review pipeline. I am executing them SEQUENTIALLY (312/313/315 first, then Exam, then Tributary, then Grand Canal), scoping each to a correct, boot-safe, reviewed vertical slice rather than the full multi-agent fan-out. Where a decomposition sub-piece is dropped or reduced to fit, it is logged as DEFERRED in that task's entry below.

## Standing rules honoured
- Commit/push as **freekumquats** (repo-local identity already set).
- Byte conventions: script_values BOM/LF; scripted_effects no-BOM/LF; loc .yml BOM single-space-indent; modifiers no-BOM.
- Every feature wired to se_LOG; every fix gets a task-tagged comment.
- Consult Invictus + Terra-Indomita oracles for any UNPROVEN engine idiom before building on it.
- Concrete over abstract; additive conversion of existing counters.

## Interpretation decisions (made autonomously; flagged for review)
- **"overnight decision agent findings"** was ambiguous (candidate docs: `overnight_decisions2.md`, `research/1763_OVERNIGHT_BUILD_CANDIDATES.md`, `research/AUDIT_*.md`). CHOSE the **AUDIT_* findings** because (a) they are the most recent (2026-07-10) audit outputs, (b) they contain concrete, scoped, actionable items (the dashboard suite + the B27 dead-code defect) rather than open-ended new-feature candidates, and (c) the 1763_OVERNIGHT_BUILD_CANDIDATES are large NEW deep-sim features that read as a menu to pick from, not "findings to implement". If this is the wrong doc, the build candidates remain untouched and available.
  - **DEFERRED (not built this run):** all of `research/1763_OVERNIGHT_BUILD_CANDIDATES.md` (Examination System, Tributary System, Grand Canal logistics, Silver/Opium model, Xinjiang arc, Court Intrigue sim, Population/Famine model, Silk Road caravans). These are separate overnight-scale features, not "findings".

---

## Task log
(entries appended as each task + its review complete)

### #315 — Qing colonization arc: Australia + New Guinea (CODE-COMPLETE, review running)
- Added two tasks to the EXISTING `qing_colonization_mission` tree (common/missions/qing_colonization_missions.txt): `qing_col_new_holland` (requires pacific_isles; claims Sydney 633 / Melbourne 2107 / Adelaide 6001 / Perth 2441; GBR frontier-play on Sydney; frontier-pull; provoke_britain; grants qing_col_new_holland_settled) → `qing_col_new_guinea` (claims Wau 50 / Sarawaget 318 / Barapasi 1076 / Hobart 5734; grants qing_col_south_seas_dominion). Added new_guinea as 4th capstone prereq + south_seas_dominion to the capstone OR-gate.
- 2 new modifiers (qing_colonization_modifiers.txt), loc (qing_colonization_l_english.yml). All 8 province IDs confirmed in definition.csv. Reused the proven idiom exactly (add_claim guarded, QING_COLON_frontier_pull, QING_gp_provoke_britain, CURRENCY_grant_country_wealth). Gated on qing_selfstr OR qing_high_qing_era like the rest of the tree.
- **DECISION:** hung the branch off the maritime `pacific_isles` node (not a new root) so it inherits the fleet/era gate and feeds the existing Pacific-Dominion capstone — one coherent tree, not a parallel one.
- **DEFERRED:** no new units/colonists are spawned (concrete-over-abstract: add_claim + migration-pull only, matching sibling branches); a dedicated southern-fleet unit spawn was NOT added.
- **ADVERSARIAL REVIEW (15-agent workflow, 9 confirmed / 12 raised) — ALL FIXED:**
  - **CRITICAL:** `p:633` was Sydney, **Nova Scotia (Canada)** — NOT Sydney, Australia (`p:895`). The flagship claim + GBR frontier-play + migration-pull all targeted the wrong continent (and collided with the tree's own Canada branch). Fixed all uses 633→895 + header note.
  - **MAJOR:** the southern branch was UNREACHABLE at a 1763 start — its prerequisite `qing_col_pacific_isles` gated only on the 19th-c. fleet modifiers, no High-Qing escape. Added `has_variable = qing_high_qing_era` to pacific_isles' allow (matching the ROOT bureau gate), so the whole maritime chain is reachable at the bookmark start it was written for. Develop/1815 unchanged (era flag never set).
  - NITS fixed: header comment listed 2 provinces never claimed (Brisbane 4528 / Banabufi 1409); "Cook charted New Holland 1770" overstated (east coast only; Dutch mapped the rest); New Holland/新南威爾士 term mismatch → 新荷蘭; 南洋 → 大洋洲領地 for the modifier.
  - KEPT (nit, intentional): capstone lists both pacific_isles + new_guinea (redundant logically, but draws the UI prerequisite arrow). Refuted (not-a-bug): the 4th capstone prereq does not regress develop; GBR frontier-play resolves against GBR-subject-owned provinces via #58.

### #312 — Female-line succession laws (CODE-COMPLETE, review pending)
- **KEY ENGINE FINDING (oracle):** the ONLY runtime succession change is `change_law = <succession_law>`; there is NO set_succession / designate_heir effect. The engine has 3 hardcoded female-penalty tiers (agnatic / cognatic / egyptian) selected by the law's `succession = <type>` field. TRUE absolute-cognatic (zero female penalty) is NOT per-country scriptable — only a global defines change (COGNATIC_FEMALE_PENALTY) would do it, which affects all cognatic realms.
- **KEY MOD FINDING:** the mod's law rework REMOVED the vanilla `succession_law` group entirely — it did not exist. So I re-added it (common/laws/00_succession_laws.txt) with agnatic + cognatic tiers, potential is_monarchy=yes.
- Built: `SUCCESSION_set_agnatic / _agnatic_cognatic / _absolute_cognatic` effects (se_SUCCESSION.txt) wrapping change_law + legitimacy cost + the Pragmatic-Sanction contest (SUCCESSION_pragmatic_contest: rival great-power NEIGHBOURS with opinion<25 contest a female-permitting change → pragmatic_sanction_contested modifier + opinion_disputed_succession). 3 reform buttons + 3 read-only indicators (SUCCESSION_actions.txt), wired into the Grand Council Edicts strip. Modifiers (qing_succession_modifiers.txt), opinion (imp19c_opinions.txt), loc.
- **DECISION — Absolute-Cognatic is FAKED:** per the oracle limitation, "Absolute Cognatic" = the cognatic law PLUS the `absolute_cognatic_succession` modifier (small legitimacy/reputation nod) + tooltip presenting it as gender-equal. Mechanically it is still cognatic (males slightly preferred). This is explicitly a compromise, not true equal succession.
- **DEFERRED / FLAGGED FOR REVIEW:**
  - **Boot-safety risk:** re-adding the succession_law group makes `agnatic_succession_law` (first member) the DEFAULT for every monarchy at game start — a mod-wide behaviour change. Historically fine for 1763 (male-line was the norm) but must be confirmed not to regress/crash. The adversarial review must check this specifically.
  - Egyptian / agnatic-seniority / elective vanilla succession tiers were intentionally NOT reintroduced (out of scope — task is female-line laws only).
  - The reform buttons live on the QING GC surface only (the laws are monarchy-general, but the only player GUI is the Qing view). AI never uses these (ai_is_valid=no).
  - No mission-tree / marriage-claim interaction was wired (the task mentioned female-line laws changing which marriages create pressable claims — that heiress-claim coupling is DEFERRED; it needs the #313 marriage-claim layer).
- **ADVERSARIAL REVIEW (two workflows: 12-agent w7q2qw06z + shared 6-agent wkyjqiv7e) — findings applied:**
  - **MAJOR (FIXED):** `is_neighbor = ROOT` in `SUCCESSION_pragmatic_contest` (any_country + every_country limits) is INVALID at country scope — `is_neighbor` is province/state/sea-zone only (oracle-verified; matches project memory `imp19c-grand-council-office-redesign.md`). The whole Pragmatic-Sanction contest was dead (always fell to the "recognised" else-branch). One verifier dissented (claimed it works country-scope) but the majority + memory + zero country-scope precedent in either oracle settle it. **FIX:** replaced with the proven country-adjacency province-walk `any_owned_province = { any_neighbor_province = { owner = ROOT } }` (proven at 00_ambitions.txt:2078, governor_policies/00_default.txt:326) in BOTH limits; corrected the bogus Invictus citation in the comment.
  - **MINOR (FIXED):** the 3 read-only `succession_is_*` indicator scripted_guis were dead code (defined, wired nowhere) — the player had no positive display of the active law. **FIX:** added an active-law `textbox` indicator above the reform buttons in the Grand Council Edicts strip (government_view.gui), driven by SelectLocalization over `succession_is_absolute_cognatic`/`succession_is_agnatic_cognatic`, plus loc keys SUCCESSION_ACTIVE_* / _TT.
  - **MINOR/nit (KEPT as designed):** CHI-only reform GUI vs monarchy-general laws — INTENTIONAL (the laws must exist engine-wide so change_law is valid, but only the Qing have a player surface). Refuted the "agnatic default regresses every monarchy" concern (verifier: agnatic IS the engine FALLBACK scoring path, so no behaviour change vs the prior no-group state). Doctrinal-toggle-cost nit (2↔3 both cognatic) left as-is (the modifier grants real legitimacy/reputation, so not fully inert).
  - **REFUTED (no change):** dangling `has_law = elective_succession_law` in the untouched anoint_heir_button.txt is a pre-existing dangling ref, not introduced by #312 — out of scope (logged for a later dead-ref sweep).

### #313 — Expand marriage to non-ruling families + candidate-list filters/sorts (DONE, reviewed)
- Built: family filter (ruling / cadet&noble / any) + gender filter (our son weds / our daughter weds / either) + sort (power / age / skill), each a CYCLE button in a new filter/sort toolbar in gui/marriage_window.gui. Two-pass candidate build (pass1 caches per-country display vars via MARRIAGE_cache_best_candidate + MARRIAGE_candidate_matches_filter; pass2 `ordered_country order_by = MARRIAGE_sort_svalue` adds to the datamodel list). Non-ruling matches route MARRIAGE_form_pact through `from_ruler = no` (a lesser tie: goodwill opinion `noble_marriage_opmod` only, NOT added to marriage_partners → no alliance/union/inheritance).
- All idioms verified proven against Invictus/TI: ordered_country order_by=svalue, `var:NAME` value reads, scope:X.martial/.age cross-scope reads, save_temporary_scope_as, from_ruler_family, any_character/ordered_character.
- **ADVERSARIAL REVIEW (shared 6-agent workflow wkyjqiv7e) — 3 confirmed, ALL FIXED:**
  - **MAJOR (FIXED):** `MARRIAGE_form_pact` hardcoded OUR-male + THEIR-female pairing with NO gender parameter, so the "our daughter weds" filter silently no-opped (candidate list filtered for a partner SON, but form_pact then hunted a partner DAUGHTER) and a ruler with only daughters could NEVER wed. **FIX:** form_pact now reads `marriage_filter_gender` on the senior country into a `marriage_our_child_female` var and picks OUR child of the filtered sex + the partner's child of the opposite sex, via `trigger_if scope:pact_senior = { var:marriage_our_child_female = 1 }` in every child/character selection limit (avoided fragile `prev` chains — used the already-saved scope:pact_senior). Applies to both the ruling (current_ruler.ordered_child) and non-ruling (ordered_character) paths. Pulse/betrothal callers set no var → default our-male (unchanged behaviour).
  - **MAJOR (FIXED):** `MARRIAGE_sort_svalue` read the cached vars with bare tokens (`add = marriage_display_best_age`) instead of `add = var:...`, so age/skill sort silently evaluated 0 for every candidate (only power sorted). **FIX:** prefixed both with `var:`.
  - **MINOR (FIXED):** the Propose `is_valid` gate checked only ruler's-children on both sides, disagreeing with the from_ruler=no / gender-filter effect paths (false-negative disabled valid noble matches; false-positive silent no-ops). **FIX:** added filter-aware gate triggers — `MARRIAGE_own_house_has_match_trigger` (our side: filtered family pool + our sex via new `MARRIAGE_own_child_matches_gender_trigger`) and `scope:target = { MARRIAGE_candidate_matches_filter = yes }` (their side) — as custom_tooltip gates, replacing the ruling-only both-sides check. New loc keys marriage_action_needs_our_match_tt / _their_match_tt.
- **DEFERRED:** heiress-claim coupling (female-line law × marriage → pressable claim) still not wired — shared #312 deferral; needs a claim-generation layer. No AI use of the non-ruling/filter paths (player GUI only).

---

## Overnight build candidates

### #321 — Imperial Examination System (科舉): the SCHOLAR HIRE-POOL (DONE, reviewed)
- **SCOPE DECISION:** the mod ALREADY had the abstract `qing_exam_ladder` (0..100) + a rich 5-event keju flavour cycle (qing_keju_events.txt) + shuyuan bands. The full 20h decomposition (triennial scheduler, pass-rate formula, crisis suite, GUI panel) largely DUPLICATES what exists. The genuinely NET-NEW, concrete-over-abstract slice is a **persistent scholar hire-pool**: exam graduates become REAL characters in a variable_list that office-appointment draws on first. Built exactly that; the rest is DEFERRED as redundant with the existing keju layer.
- Built `se_QING_EXAM.txt` (NEW): `QING_exam_init` (seed pool bookkeeping, wired at game-start init), `QING_exam_mint_scholar` (create_character graduate + degree trait + bind + add to `qing_scholar_pool` list), `QING_exam_graduate_cohort` (mint 1-3 graduates scaled by ladder health; called from qing_keju.2 palace-exam), `QING_exam_fill_first_vacant_from_pool` (draw ablest pool scholar into first vacant office; used by NEW qing_keju.6 "掄才" event), `QING_exam_pool_drop_member` (pool-exit-on-seating, hooked into QING_office_appoint tail), `QING_exam_pool_tick` (quarterly retire aged/dead un-placed, into mechanics pulse), `QING_exam_pool_remove_on_death` (into on_character_death). New qing_keju.6 event + flavour-roll wiring (gated on live vacancy + non-empty pool) + loc.
- All idioms proven: add_to_variable_list / remove_list_variable / ordered_in_list order_by=<svalue> / any_in_list / every_in_list / variable_list_size; create_character + set_home_country (roster idiom); mid-iteration remove_list_variable target=prev PROVEN in Terra-Indomita COL_sguis.txt:37.
- **ADVERSARIAL REVIEW (6-agent workflow wytzy1dto) — 2 confirmed MAJOR, BOTH FIXED:**
  - **MAJOR (FIXED):** pool scholars LEAKED — the generic council autofill / manual appoint / keju.2 laureate all seat a pool scholar via QING_office_appoint, which did ZERO pool cleanup, so the man held office yet stayed listed/marked/counted → phantom count kept keju.6 firing + fill-picker could re-select a seated man (seat churn). **FIX:** moved pool-exit INTO QING_office_appoint's tail (`QING_exam_pool_drop_member`, self-guarded on the marker) so EVERY appointment route drops a seated scholar exactly once; also excluded office-holders from the fill-picker.
  - **MAJOR (FIXED):** QING_exam_fill_first_vacant_from_pool removed the scholar + decremented UNCONDITIONALLY before seating, but QING_office_appoint_first_vacant can no-op (stale holder-var for a holder captured in war → recompute reports a vacancy the appoint picker doesn't see) → scholar silently consumed, never seated, +1 stability granted anyway. **FIX:** removal now happens only inside QING_office_appoint (i.e. only on actual seating); the fill effect no longer consumes on a no-op.
  - **REFUTED/removed:** vestigial `qing_scholar_intake_date` (set, never read) — removed cleanly (retirement keys on age, created 28 → retire ~55).
- **DEFERRED (explicitly, redundant-with-existing or GUI):** the full triennial scheduler / pass-rate formula / 捐納-abolition-crisis suite (the keju events already cover this flavour); a dedicated exam GUI panel (pool surfaced via the keju.6 event text + count var only — NO standalone L4 window built); the `$office$`-parameterised pool-first auto-fill verb (dropped as dead code — the first-vacant variant covers the wired use). AI never runs any of this (CHI is_ai=no).

### #322 — Tributary System (朝貢體系): the TRIBUTE-EMBASSY RHYTHM (DONE, reviewed)
- **SCOPE DECISION:** the mod ALREADY had the abstract suzerain-prestige meter (qing_suzerain_prestige) + DEFENSIVE vassal crises (defend/abandon, se_QING_VASSAL.txt) + a concrete quarterly tribute-income collector (B17, se_SUBJECT_QING.txt). The genuinely NET-NEW slice is the POSITIVE ONGOING RHYTHM: periodic tribute EMBASSIES embodied by real envoy characters, off the ACTUAL tributary subject graph (KOR/VIE/NEP/RYU/TNN — the dependency=tributary rows at the 1763/1815 start). Built that; the full per-vassal investiture/breakaway/GUI-table decomposition is DEFERRED.
- Built `se_QING_TRIBUTE.txt` (NEW): `QING_tribute_init`, `QING_tribute_schedule_missions` (single shared quarterly scheduler — one due tributary/pulse, no event-queue bloat), `QING_tribute_mint_envoy` (create_character envoy, culture follows the sender), `QING_tribute_receive` (tribute-trade profit + tempo counter), `QING_tribute_dismiss_envoy`, `QING_tribute_prestige_from_missions` (tempo→prestige drift). NEW qing_tribute.1 arrival event (audience-in-full vs curt-dismiss) + `QING_tribute_is_tributary_trigger` (00_marriage… no — qing_dynasty_triggers.txt) + init/pulse wiring + BOM loc file.
- Idioms proven: random_subject + is_subject_type=tributary; save_scope_as persistence into a delayed event (se_QING_AMBAN idiom); set_home_country=scope:X dismiss (Invictus); has_monthly_income read + cross-scope var read; QING_DECLINE_nudge auto-clamps 0..100.
- **ADVERSARIAL REVIEW (7-agent workflow wcw5bii2v) — 1 CRITICAL + 2 MINOR, ALL FIXED:**
  - **CRITICAL (FIXED):** the scheduler saved the subject scope INSIDE random_subject but ran mint/fire OUTSIDE it, guarded only on `exists=scope:trib_sender`. Saved scopes PERSIST across pulses, so once all 5 tributaries were on cooldown (random_subject matched nothing) the STALE scope re-fired a bogus embassy + minted a leaked envoy + re-stamped the cooldown EVERY quarter for the rest of the game. **FIX:** moved ALL mission effects INSIDE random_subject (the proven AMBAN pattern) with a `ROOT = { ... }` hop for the country-scope mint/trigger — a no-match pulse is now a clean no-op.
  - **MINOR (FIXED):** minted envoys were never removed from the Qing court (accumulation/save clutter). **FIX:** `QING_tribute_dismiss_envoy` sends the envoy back home to the sending tributary (set_home_country=var) at the end of both event options; documented that he holds no list membership so no on-death hook is needed.
  - **MINOR (FIXED):** the curt-dismiss option netted ZERO prestige (QING_tribute_receive's base +3 cancelled the option's -3). **FIX:** removed the prestige nudge from the shared receive helper (now prestige-neutral); the full-audience option carries the whole +7, the curt-dismiss the whole -3.
  - **REFUTED (no change):** double-count-vs-B17 (the two collectors target the same subjects but the embassy is a rare event bonus, not the quarterly base rate — intended); money-creation-on-add_treasury (the sender is credited the profit but not debited — an intended asymmetric-gift abstraction, not a bug at this scale).
- **DEFERRED (explicitly):** per-vassal investiture (冊封 on a tributary's new ruler), breakaway-crisis escalation from low prestige, per-vassal cadence differences, GP-interference embassies, and the L4 tributary GUI table — all in the 18h decomposition but out of this vertical slice. AI never runs it (CHI is_ai=no).

### #323 — Grand Canal Grain-Tribute Logistics (漕運) (DONE, reviewed)
- **SCOPE DECISION:** the mod ALREADY models the canal as BUILDINGS (漕運倉 depot, 河堤 dike) + silt/breach crises (se_QING_WORKS.txt, #121). The net-new slice is the LOGISTICS the canal existed for: the annual grain-tribute lifeline feeding the capital + banner garrisons. Modelled on the PROVEN 0..100 band idiom (like qing_banner_decay), NOT literal millions of 石 (no million-scale arithmetic precedent → overflow risk; explicitly chose bands over the decomposition's raw-tonnage formula).
- Built `se_QING_CANAL.txt` (NEW): `QING_canal_init` (seed qing_canal_condition=80 / qing_grain_reserve=75, 1763 zenith baselines), `QING_canal_update_condition` (target-drift from CONCRETE inputs — each depot/dike building + a staffed Works office lift it, corruption drags it), `QING_canal_run_grain_balance` (delivery=condition/20 vs a flat draw of 4 → reserve rises/falls), `QING_canal_quarterly_tick` (the public entry, hooked into QING_works_pulse), `QING_canal_relief_redirect` / `_hoard` (famine payloads). NEW qing_canal.1 famine-response dilemma + init + BOM loc.
- **CONCRETE COUPLINGS:** the existing #121 canal-depot/dike buildings now have a SYSTEMIC payoff (they raise canal condition → grain delivery); a low capital reserve (<40) accelerates qing_banner_decay (the capital-grain-shortage → banner-rot causation); a critical reserve (<20) fires the famine dilemma whose "hoard" branch feeds qing_sect_pressure (the famine → Taiping-recruiting-ground causation).
- Idioms proven: target-drift (QING_GOV_update_exam_ladder), QING_DECLINE_nudge auto-init+clamp 0..100, any_owned_province has_building presence checks, temp-var set/remove hygiene.
- **ADVERSARIAL REVIEW (3-agent workflow w20f0wd9x) — 0 CONFIRMED:**
  - The one MINOR raised (banner-decay coupling not gated on qing_high_qing_era) was VERIFIED-then-REFUTED (the era-suppression contract covers only passive time-creep, not shortfall-driven decay; and at the zenith the canal is healthy so the shortfall path can't bite anyway). Applied a belt-and-suspenders `NOT = { has_variable = qing_high_qing_era }` guard on the coupling regardless, to honour the decay-suppression design philosophy.
  - Arithmetic reviewer confirmed CLEAN: integer division delivery=condition/20 is intended (a healthy canal cond≈100 delivers 5 > draw 4 → granaries fill; decline drops delivery below 4 → they empty), no leak/overflow/order-hazard (init runs first at the tick top).
- **DEFERRED (explicitly):** literal 石-tonnage model + per-province Jiangnan quota sliders + sea-route-unlock toggle (Daoguang coastal shipping) + the L4 canal GUI panel (stacked reserve-vs-demand bar, quota table) — all in the 16h decomposition but out of this band-based vertical slice. AI never runs it.

---

### #324 — Heiress-claim coupling (the #312/#313 shared deferral, NOW BUILT + reviewed)
- **CONTEXT:** #312 and #313 both deferred "female-line law × marriage → pressable claim, needs a claim-generation layer." On investigation that layer LARGELY EXISTED (se_MARRIAGE.txt already has MARRIAGE_grant_dowry_claim, MARRIAGE_press_succession_claim, MARRIAGE_on_ruler_death_union inheritance). The real gap was wiring the #312 succession LAW into WHEN a marriage breeds a claim.
- Built `MARRIAGE_check_heiress_claim` (se_MARRIAGE.txt L2b, wired into MARRIAGE_pulse): where a wedded junior house's only heir is a DAUGHTER its own `agnatic_succession_law` bars (living daughter, no living son), our line carries her disinherited claim — the Habsburg/Burgundy dynamic. Grants a pressable claim (via the existing MARRIAGE_press_succession_claim) once per partner (marriage_heiress_claimed once-guard list). The MIRROR is by ABSENCE: a cognatic realm (the #312 reform) lets the daughter inherit, so no claim fires — the law concretely changes which marriages breed claims. Once-guard pruned in the union-death teardown loop alongside marriage_partners/alliance_partners.
- Runs AI-side too (like MARRIAGE_check_unions) — enriches AI dynastic wars, consistent with the existing union layer.
- **ADVERSARIAL REVIEW (4-agent workflow w58qvf60b) — 1 MAJOR (both reviewers, independently), FIXED:**
  - **MAJOR (FIXED):** the once-per-partner dedup guard read `is_target_in_variable_list` at the bare CANDIDATE (junior) scope, but the list lives on the SENIOR — so it inspected the junior's never-written list, the NOT always passed, and every ~5yr pulse re-appended a duplicate + re-fired the claim (unbounded list leak + log spam; add_claim idempotent so no war runaway). **FIX:** wrapped the read in `scope:heiress_senior = { ... target = prev }`, exactly mirroring the proven marriage_partners membership test three lines above.

### Other #312/#313/#315 deferrals — CONFIRMED as deliberate design (not build gaps)
- **Egyptian / agnatic-seniority / elective succession tiers (#312):** out of scope — task was female-line only; a separate feature, not a defect.
- **CHI-only reform GUI (#312):** intentional — the laws are engine-wide so `change_law` is valid, but only the Qing have a player surface; confirmed correct by the #312 review.
- **AI never uses the filter / non-ruling marriage paths (#313):** correct by design (`ai_is_valid = no`) — these are player-GUI affordances.
- **No southern-fleet unit spawn (#315):** deliberate concrete-over-abstract choice matching the sibling colonization branches (add_claim + migration-pull, no bespoke fleet).

## Run summary
All mandated tasks COMPLETE on branch `overnight-312-315-audits` (unpushed, per branch policy — develop is the push target, but this is a dedicated overnight branch): #312, #313, #315 (pre-existing commit), plus the Top-3 build candidates #321 (Exam hire-pool), #322 (Tributary embassies), #323 (Canal grain logistics). Each was built as a reviewed vertical slice, ran a deep adversarial Workflow review after completion, and had every CONFIRMED finding fixed before commit. All standing rules honoured (freekumquats identity, byte conventions, se_LOG wiring, oracle-verified idioms, concrete-over-abstract). DEFERRED pieces are logged per-task above. Boot-test still OWED (no in-game verification performed this run).
